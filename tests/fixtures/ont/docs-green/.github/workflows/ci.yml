name: ci
on: [push, pull_request]
jobs:
  gate:
    runs-on: ubuntu-latest
    steps:
      - run: bash scripts/check.sh
      - run: |
          cargo test --lib
      - if: false
        run: make disabled-step
