# Tests `make coverage` does not run. ONE full test path per line, matched EXACTLY
# (the target passes `--exact --skip <line>`). Blank lines and #-comments are ignored.
#
# #3839: this used to be 19 bare `--skip` substrings. libtest's --skip is a substring
# match on the FULL test path, so they removed 2,713 lib tests: `gpu_` ate every `wgpu_*`
# test and ~1,100 CPU tests that only mention GPUs, and `falsification` ate whole modules
# (1,084 tests), so tested code read as 0% covered. Measured on intel (no working GPU
# driver) @ fc942f6be under nextest: 2,702 of those 2,703 passed, the slowest in 41 s,
# all of them in 121 s wall. Only the entry below needs to stay out.
#
# Adding an entry needs a measured reason, written next to it. "Its name mentions a GPU"
# is not a reason: a test that needs a device it doesn't find should fail or skip itself.

# Slow by construction on any host (not a GPU test; the old `cuda` substring caught it
# by accident). build_provenance SHA-256-hashes current_exe twice per call and this test
# calls it twice, so it hashes the ~494 MB debug apr-cli test binary 4 times: >120 s
# under nextest on intel, and slower instrumented.
commands::test_llm_band::tests::cuda_without_the_server_feature_is_refused

# A timing assertion, which instrumentation invalidates: two back-to-back LZ4 runs must
# agree within 15x. Under llvm-cov on a loaded host they measured 26.21x, and the test
# failed Coverage Nightly 35800448700 (2026-09-23), which then wrote no lcov and
# reported no number at all. It still runs, uninstrumented, in the normal test gates.
benchmark::tests::test_f060_no_performance_regression

# Memory, measured: Coverage Nightly 36204739015 (2026-09-26, yoga, 30 GB RAM, three runner
# slots). These seven were still running, each for over 60 s, when earlyoom SIGTERMed the
# aprender-serve lib binary at 15.2 GB RSS (journalctl -u earlyoom, 01:11:14). The binary
# wrote no profile, so all of aprender-serve read as 0% and the total fell 90% -> 76%.
# By their own formulas, the ultra/super/mega caches are 8.6 / 17.2 / 34.4 GB, and
# falsify_kv_001's 13B case is 6.7 GB. They run, uninstrumented, in workspace-test.
gpu::streaming_kv::kv_contract_tests::falsify_kv_001_memory_formula
gpu::streaming_kv::kv_contract_tests::falsify_kv_002_monotonic_sequence_length
gpu::streaming_kv::kv_contract_tests::falsify_kv_002b_monotonic_layers
gpu::tests::gpu_compute::test_ultra_long_context_memory_bound
gpu::tests::gpu_compute::test_super_long_context_memory_bound
gpu::tests::gpu_compute::test_mega_long_context_memory_bound
gpu::tests::tests_11::test_forward_single_token_cpu_fallback_large_vocab
