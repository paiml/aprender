# Competitive-parity ratchet baseline. A SET, not a count.
#
# Written by scripts/check_competitive_parity.sh --update-baseline.
# Do not hand-edit: the mechanism is that losing a SPECIFIC row is
# visible even when every total is unchanged. A count ratchet is
# payable in the wrong currency -- delete the StandardScaler 0.69x row,
# add a cheaper one, and __MEASURED__ never moves. That is PMAT-733.
#
#   ROW=             must still EXIST. Droppable only when the entry
#                    point has left the live `apr` enumeration.
#   MEASURED_ROW=    SHRINK-NEVER. Must still be MEASURED, or carry a
#                    recorded `downgrades:` entry in the ledger. The key
#                    STAYS while the row exists, so a downgrade is a debt
#                    with a due date, not a one-off payment that the
#                    baseline absorbs and the next commit can delete.
#   VERDICT_ROW=     <VERDICT><TAB><entry_point>, ACCUMULATING. Every
#                    verdict a live row has ever been recorded as. A
#                    declared verdict outside this set for its row needs a
#                    `downgrades:` record naming from_verdict/to_verdict.
#                    The gate never checks the verdict VALUE -- a rule that
#                    admits only wins makes deleting a losing comparison the
#                    cheapest compliant action, which is PMAT-733 itself. It
#                    checks that CHANGING one is not free.
NON_WINS_MIN=5
IN_SCOPE_MIN=41

ROW=13:apr run --gpu
ROW=48:lib:aprender-core::StandardScaler::fit_transform
ROW=8:apr code
ROW=29:lib:aprender-core::Lasso::fit
ROW=51:apr run --gpu (concurrency=1 single-request decode)

MEASURED_ROW=8:apr code
MEASURED_ROW=13:apr run --gpu
MEASURED_ROW=29:lib:aprender-core::Lasso::fit
MEASURED_ROW=48:lib:aprender-core::StandardScaler::fit_transform

VERDICT_ROW=20:PARITY	apr run --gpu
VERDICT_ROW=19:UNMEASURED	apr code
VERDICT_ROW=62:UNMEASURED	apr run --gpu (concurrency=1 single-request decode)
VERDICT_ROW=35:WORSE	lib:aprender-core::Lasso::fit
VERDICT_ROW=54:WORSE	lib:aprender-core::StandardScaler::fit_transform
