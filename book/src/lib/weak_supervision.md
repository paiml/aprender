<!-- PCU: lib-weak_supervision | contract: contracts/apr-page-lib-weak_supervision-v1.yaml -->

# Module: `aprender::weak_supervision`

Public module of the `aprender-core` crate.

## Source

[`crates/aprender-core/src/weak_supervision.rs`](https://github.com/paiml/aprender/blob/main/crates/aprender-core/src/weak_supervision.rs) or directory.

## Example

```rust
use aprender::weak_supervision;
// See `cargo doc -p aprender-core --open` for full API reference.
```

## Full API

Run `cargo doc -p aprender-core --open` for the rendered rustdoc, or browse
[docs.rs/aprender](https://docs.rs/aprender) for the published version.
