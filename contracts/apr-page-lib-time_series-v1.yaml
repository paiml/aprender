metadata:
  version: 1.0.0
  kind: schema
  description: |
    PCU contract for book/src/lib/time_series.md (aprender::time_series module reference).
    Enforces: page exists, references the module, has a runnable example.
  references:
    - "docs/specifications/book-completeness-spec.md"

equations:
  pcu_header_present:
    formula: "file_contains(book/src/lib/time_series.md, 'PCU: lib-time_series')"
  module_mentioned:
    formula: "file_contains(book/src/lib/time_series.md, 'aprender::time_series')"
  example_block_present:
    formula: "file_contains_fenced(book/src/lib/time_series.md, language='rust')"

falsification_tests:
  - id: FALSIFY-PAGE-LIB-TIME_SERIES-001
    name: stub_exists
    prediction: "book/src/lib/time_series.md exists on disk"
    test_harness: "test -f book/src/lib/time_series.md"
    expected_output: "exit 0"
    if_fails: "Run scripts/gen-lib-chapter-stubs.sh to regenerate."

  - id: FALSIFY-PAGE-LIB-TIME_SERIES-002
    name: module_mentioned
    prediction: "page references the module at least once"
    test_harness: "grep -q 'aprender::time_series' book/src/lib/time_series.md"
    expected_output: "exit 0"
    if_fails: "Add a mention of aprender::time_series to the chapter."

  - id: FALSIFY-PAGE-LIB-TIME_SERIES-003
    name: runnable_example
    prediction: "page has at least one rust code block"
    test_harness: "grep -c '^```rust' book/src/lib/time_series.md"
    expected_output: "stdout >= 1"
    if_fails: "Add a rust code block with a runnable example."
