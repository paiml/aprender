metadata:
  version: 1.0.0
  kind: schema
  description: |
    PCU (Page Content Unit) contract for book/src/cli/kv-timeline-lint.md (apr kv-timeline-lint CLI reference).
    Enforces: page exists, references the command, has a runnable example.
  references:
    - "docs/specifications/book-completeness-spec.md"

equations:
  pcu_header_present:
    formula: "file_contains(book/src/cli/kv-timeline-lint.md, 'PCU: cli-kv-timeline-lint')"
  command_mentioned:
    formula: "file_contains(book/src/cli/kv-timeline-lint.md, 'apr kv-timeline-lint')"
  example_block_present:
    formula: "file_contains_fenced(book/src/cli/kv-timeline-lint.md, language='bash')"

falsification_tests:
  - id: FALSIFY-PAGE-CLI-KV_TIMELINE_LINT-001
    name: stub_exists
    prediction: "book/src/cli/kv-timeline-lint.md exists on disk"
    test_harness: "test -f book/src/cli/kv-timeline-lint.md"
    expected_output: "exit 0"
    if_fails: "Run scripts/gen-cli-chapter-stubs.sh to regenerate."

  - id: FALSIFY-PAGE-CLI-KV_TIMELINE_LINT-002
    name: command_mentioned
    prediction: "page references the command at least once"
    test_harness: "grep -q 'apr kv-timeline-lint' book/src/cli/kv-timeline-lint.md"
    expected_output: "exit 0"
    if_fails: "Add a mention of `apr kv-timeline-lint` to the chapter."

  - id: FALSIFY-PAGE-CLI-KV_TIMELINE_LINT-003
    name: runnable_example
    prediction: "page has at least one ```bash code block"
    test_harness: "grep -c '^```bash' book/src/cli/kv-timeline-lint.md"
    expected_output: "stdout >= 1"
    if_fails: "Add a ```bash code block with a runnable example."
