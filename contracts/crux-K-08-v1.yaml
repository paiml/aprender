# CRUX-K-08 — OpenTelemetry traces
# Promoted to PARTIAL_ALGORITHM_LEVEL at v1.1.0 under CRUX-SHIP-001 (2026-05-16):
# - classifier: crates/apr-cli/src/commands/otlp_classifier.rs (11 unit tests)
# - CLI surface: `apr otlp-lint --otlp-file FILE [--require-apr-span] [--require-genai-attrs] [--expect-trace-id HEX32]`
# - e2e: crates/apr-cli/tests/falsification_crux_k_08.rs (11 tests)
# Full discharge blocks on a live `apr serve` OTLP exporter wired to
# `OTEL_EXPORTER_OTLP_ENDPOINT` emitting `apr.inference` spans with
# gen_ai.* attributes and W3C trace-context propagation — tracked as
# BLOCKER-UPSTREAM-MISSING.

metadata:
  id: CRUX-K-08
  version: "1.1.0"
  created: "2026-04-18"
  updated: "2026-05-16"
  author: PAIML Engineering
  registry: true
  status: partial
  kind: kernel
  parent_contracts:
  - crux-competitive-research-ux-v1
  category: "K — Ecosystem & Integration"
  competitor: ecosystem
  demand_score: 4  # 1..5, high priority in pmat work
  intake_status: missing
  description: >
    OpenTelemetry traces. Root-cause workflow extracted from ecosystem UX — see master
    subspec §5.K and §2 Five Whys methodology for rationale. This
    draft contract exists to satisfy the Iron Rule "no contract → no user
    story"; the falsification body below is a placeholder and MUST be
    replaced with the competitor's canonical CLI transcript before promotion.

  references:
  - 'master: contracts/crux-competitive-research-ux-v1.yaml — §5 + §12'
  - github.com/triton-inference-server/server
  - github.com/ai-dynamo/dynamo
  - github.com/NVIDIA/TensorRT-LLM
equations:
  otlp_export_protocol:
    formula: |
      When OTEL_EXPORTER_OTLP_ENDPOINT is set, apr serve/apr run emit spans
      per OpenTelemetry Protocol (OTLP/HTTP or OTLP/gRPC):
        POST {endpoint}/v1/traces with body = ExportTraceServiceRequest proto
        (content-type: application/x-protobuf OR application/json)
      Each inference request produces at least one span tree rooted at
      "apr.inference" with attributes:
        apr.model          (string, model id)
        apr.tokens.prompt  (int64, prompt token count)
        apr.tokens.output  (int64, completion token count)
        apr.decode.tps     (double, tokens per second)
        gen_ai.system      (string, "apr")
        gen_ai.request.model (string, model name)
      Following the OpenTelemetry GenAI semantic conventions.
    domain: "OTEL_EXPORTER_OTLP_ENDPOINT URL + apr inference request"
    codomain: "OTLP ExportTraceServiceRequest payload (protobuf/JSON)"
    invariants:
    - "Span name 'apr.inference' is present on root span"
    - "Attributes follow gen_ai.* semantic conventions (system, request.model, usage.*)"
    - "TraceId is 16-byte hex; SpanId is 8-byte hex; both non-zero"
    - "Reference: https://opentelemetry.io/docs/specs/semconv/gen-ai/"

  trace_context_propagation:
    formula: |
      apr serve MUST honor incoming W3C Trace Context headers:
        traceparent: 00-<trace-id>-<parent-span-id>-<flags>
        tracestate:  <vendor>=<opaque>
      Generated spans for that request carry the same trace-id
      (parent span id from incoming header becomes parent of apr.inference root).
    domain: "HTTP request with W3C Trace Context headers"
    codomain: "exported spans whose trace_id matches incoming traceparent"
    invariants:
    - "Incoming traceparent.trace_id == emitted span trace_id"
    - "Incoming traceparent.span_id == emitted root span parent_span_id"
    - "Reference: https://www.w3.org/TR/trace-context/"

falsification_tests:
- id: FALSIFY-CRUX-K-08-001
  rule: "apr exports OTLP spans when OTEL endpoint is configured"
  prediction: "OTLP collector receives >=1 span with name 'apr.inference' after an inference request"
  test: |
    set -euo pipefail
    # Start a tiny OTLP/HTTP sink that records requests to /tmp/otlp.log
    python3 - <<'EOF' &
    import http.server, json, pathlib
    LOG = pathlib.Path("/tmp/otlp.log")
    LOG.write_bytes(b"")
    class H(http.server.BaseHTTPRequestHandler):
        def do_POST(self):
            n = int(self.headers.get("Content-Length","0"))
            body = self.rfile.read(n)
            with open("/tmp/otlp.log","ab") as f: f.write(body + b"\n---\n")
            self.send_response(200); self.end_headers()
    http.server.HTTPServer(("127.0.0.1",4318), H).serve_forever()
    EOF
    SINK=$!
    trap "kill $SINK 2>/dev/null || true" EXIT
    sleep 1
    OTEL_EXPORTER_OTLP_ENDPOINT=http://127.0.0.1:4318 \
    OTEL_EXPORTER_OTLP_PROTOCOL=http/json \
    OTEL_SERVICE_NAME=apr-test \
      apr serve --port 7850 --model test-model.apr &
    SRV=$!
    trap "kill $SRV $SINK 2>/dev/null || true" EXIT
    sleep 2
    curl -sS http://127.0.0.1:7850/v1/chat/completions \
      -H 'Content-Type: application/json' \
      -d '{"model":"test","messages":[{"role":"user","content":"hi"}]}' >/dev/null
    sleep 1
    grep -q 'apr.inference' /tmp/otlp.log
  if_fails: "apr serve does not export OTLP span named 'apr.inference' to configured endpoint"
  evidence_discharged_by:
    classifier_path: crates/apr-cli/src/commands/otlp_classifier.rs
    cli_path: crates/apr-cli/src/commands/otlp_lint.rs
    e2e_path: crates/apr-cli/tests/falsification_crux_k_08.rs
    e2e_tests:
    - falsify_crux_k_08_001_span_present_ok_on_apr_inference
    - falsify_crux_k_08_001_span_present_reports_missing_apr_inference
    sub_claim: |
      Algorithm-level necessary conditions on an already-captured OTLP/JSON
      `ExportTraceServiceRequest`: `classify_span_present` walks
      `resourceSpans[].scopeSpans[].spans[]` and returns `Ok { count }` only
      when at least one span has the requested name (`apr.inference`),
      and reports `SpanNameNotFound { names_seen }` listing the names that
      were emitted so divergence is debuggable. Reject conditions
      `NotAnObject` and `MissingResourceSpans` surface malformed bodies
      before they reach the live exporter.
    scope: "(body, span_name) -> OtlpSpanPresentOutcome (pure)"
    discharge_status: PARTIAL_ALGORITHM_LEVEL
    full_discharge_blocked_on: "live aprender-serve OTLP exporter emitting `apr.inference` spans"

- id: FALSIFY-CRUX-K-08-002
  rule: "GenAI semantic conventions present on inference span"
  prediction: "exported span attributes include gen_ai.system, gen_ai.request.model, apr.tokens.prompt, apr.tokens.output"
  test: |
    set -euo pipefail
    test -s /tmp/otlp.log
    # Accept either x-protobuf or JSON encodings: fall back to grep-for-key presence.
    for KEY in 'gen_ai.system' 'gen_ai.request.model' 'apr.tokens.prompt' 'apr.tokens.output'; do
      grep -q "$KEY" /tmp/otlp.log || { echo "missing attribute: $KEY"; exit 1; }
    done
  if_fails: "OTLP span missing one of the required gen_ai.* or apr.tokens.* attributes"
  evidence_discharged_by:
    classifier_path: crates/apr-cli/src/commands/otlp_classifier.rs
    cli_path: crates/apr-cli/src/commands/otlp_lint.rs
    e2e_path: crates/apr-cli/tests/falsification_crux_k_08.rs
    e2e_tests:
    - falsify_crux_k_08_002_genai_attributes_ok_on_full_body
    - falsify_crux_k_08_002_genai_attributes_reports_partial_set
    sub_claim: |
      Algorithm-level necessary conditions: `classify_genai_attributes`
      collects every `attributes[].key` across all spans in the OTLP body
      (the OTLP/JSON encoding uses a key-value array, not a map), then
      verifies the K08_REQUIRED_ATTRIBUTES set is a subset. Missing keys
      are reported lexicographically so diffs are stable across runs.
    scope: "(body, required_keys) -> OtlpAttributesOutcome (pure)"
    discharge_status: PARTIAL_ALGORITHM_LEVEL
    full_discharge_blocked_on: "live aprender-serve OTLP exporter populating gen_ai.* + apr.tokens.* attributes"

- id: FALSIFY-CRUX-K-08-003
  rule: "W3C trace-context propagation: incoming traceparent reused in exported span"
  prediction: "exported span trace_id == trace_id in incoming traceparent header"
  test: |
    set -euo pipefail
    # Canonical traceparent: version 00, trace-id 0af7651916cd43dd8448eb211c80319c, parent 00f067aa0ba902b7
    TRACE_ID=0af7651916cd43dd8448eb211c80319c
    PARENT_ID=00f067aa0ba902b7
    : > /tmp/otlp.log
    curl -sS http://127.0.0.1:7850/v1/chat/completions \
      -H 'Content-Type: application/json' \
      -H "traceparent: 00-${TRACE_ID}-${PARENT_ID}-01" \
      -d '{"model":"test","messages":[{"role":"user","content":"hi"}]}' >/dev/null
    sleep 1
    grep -q "$TRACE_ID" /tmp/otlp.log
  if_fails: "apr serve does not honor W3C traceparent (trace-id not propagated to exported span)"
  evidence_discharged_by:
    classifier_path: crates/apr-cli/src/commands/otlp_classifier.rs
    cli_path: crates/apr-cli/src/commands/otlp_lint.rs
    e2e_path: crates/apr-cli/tests/falsification_crux_k_08.rs
    e2e_tests:
    - falsify_crux_k_08_003_trace_propagation_ok_on_canonical_traceparent
    - falsify_crux_k_08_003_trace_propagation_rejects_short_id
    - falsify_crux_k_08_003_trace_propagation_reports_mismatch
    sub_claim: |
      Algorithm-level necessary conditions: `classify_trace_propagation`
      first validates the expected trace-id is 32 lowercase hex characters
      (W3C trace-context format) — rejects anything else with
      `InvalidExpectedTraceId` so caller bugs surface before the body is
      walked. Then it scans every `traceId` in the OTLP body for byte-equal
      match; on miss it reports `TraceIdNotFound { ids_seen }` listing the
      actual trace-ids so divergence is debuggable.
    scope: "(body, expected_trace_id) -> OtlpTracePropagationOutcome (pure)"
    discharge_status: PARTIAL_ALGORITHM_LEVEL
    full_discharge_blocked_on: "live aprender-serve OTLP exporter wiring W3C traceparent into emitted spans"

proof_obligations:
- type: invariant
  property: "apr inference emits OTLP span named 'apr.inference' when OTEL endpoint configured"
- type: invariant
  property: "Spans carry gen_ai.* semantic-convention attributes plus apr.tokens.prompt/output"
- type: invariant
  property: "W3C traceparent header is honored (trace_id propagated into exported spans)"
- type: equivalence
  property: "apr serve OpenTelemetry instrumentation matches GenAI semantic conventions and OTLP export protocol as implemented by opentelemetry-instrumentation reference libraries"
  reference: "https://opentelemetry.io/docs/specs/semconv/gen-ai/"

verification_summary:
  total_obligations: 4
  proven: 0
  tested: 0
  status: spec-complete

pmat_work_tracking:
  # Managed by master contract §12. If intake_status == "missing", a ticket
  # tagged `crux-crux-k-08` is auto-created by scripts/crux_bulk_pmat_work.sh.
  ticket_tag: crux-crux-k-08
  priority: high
  auto_created: true
