DRIVER v6 (2026-09-07) SUPERSEDES THE LINE BELOW WHERE THEY DIFFER. ROW PROCESS: P0 discover (`.pr/<row>/discover.json` via ~/.claude/skills/paiml-implement/scripts/discover.sh; a row whose A_i already pass at HEAD is vacuous — comment the issue, receipt, no PR) → P1 plan (`.pr/<row>/plan.md`, every A_i as a command in `.pr/<row>/accept.sh`, K̂ [U]) → P2 execute (RED test commit first, contract in contracts/ with kind: pattern and `pv validate` through `. scripts/pv_bin.sh`, accept.sh GREEN, mutant commit pushed → PR CI RED → revert → GREEN with both run ids) → P3 quorum (review-only rows one agy lane; N-lane rows three model families named in the brief; record `.pr/<row>/quorum.md`). PR STANDARD: one row per PR; branch agent/<row> from origin/main (DONE-IF the PR exists → update, never a second); title `<type>(<scope>): <row> — <claim in one line>`; labels pp-066, inst:<X>, <row> (create the row label if absent); body sections in order: Ticket · Claim · RED test (sha) · Acceptance (accept.sh output) · Mutation (RED run id → GREEN run id) · Contract (path, pv validate output) · Quorum (lanes, families, verdict) · Receipt (path) · Writes (files; no shared files: never docs/specifications/pp-066-dag.yaml, docs/roadmaps/roadmap.yaml, docs/specifications/PP-066-release-spec.md, README count lines — check_row_pr_write_set.sh enforces); `gh pr merge --auto --squash`; wait only with `gh pr checks <n> --required --watch --fail-fast`; receipt inside the PR {status: complete, model, tokens_used, wall_clock_s, turns} before arming. I15: every worker dispatch carries a stop condition; a done worker is told to stop. I16: hypotheses are refuted by measurements in a table. Verifiers through pins (`. scripts/pv_bin.sh`, `. scripts/pmat_bin.sh`, `. scripts/apr_bin.sh`), never PATH. Push before every stop; K=150 → SESSION END: push all, stop. Status line every turn: `[status] inst=<X> row=<id> phase=<p> …`.

PP-066 · driver v5.1 · INSTANCE B (Opus 5) · role: R-0a then R-6. Read docs/specifications/PP-066-release-spec.md §4.1 and docs/specifications/pp-066-dag.yaml first; the spec and the DAG are the standard. Invariants I1–I14 of the driver apply; you never `pmat work add`; you never write docs/specifications/**, docs/roadmaps/**, README.md; your PR carries labels `inst:B` and the row id; claim the row by labelling its issue `inst:B` (DONE-IF labelled by A → ask; by C → skip). One worktree per row via `apr-agent <row>` (DONE-IF exists → reuse: /home/noah/src/aprender-worktrees/R-0 holds PR #3004 for R-0a — resume it: rebase onto main, strip its README count bump (G-11a's ratchet lets the README lag), keep the R-0a half only (registry + `apr devices [--json] [--model <gguf>]` + contracts/apr-devices-schema-v1.yaml + FX-1..15 with must-RED twins + MockBackend), wait, arm). Design note docs/audits/r0-design.md → an N-lane quorum with THREE model families named in the brief (refuse a delegate that runs one family) before P2. Hosts: four via `make fleet-verify ROW=R-0a` once G-11b lands (until then lambda-labs is the one excepted ssh host). Mutant commit → PR CI RED → revert → GREEN, both run ids in the body. Receipt docs/audits/impl-<ticket>-receipt.md {status: complete, model, tokens_used, wall_clock_s, turns} inside the PR before arming. Then R-6: POSIX sh installer, sha256 + minisign per S0-19 (public key .github/pr-review.pub class), ends by printing `apr devices`, tests/install/cases.sh. STOP on: R-0a design quorum do-not-implement with a cited finding unresolved after two folds; any ruleset edit; K=150. Status line every turn: [status] inst=B row=<id> phase=<p> …
