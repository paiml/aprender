APR-PERF-GATE-001 v2.2 -- W1 cell on lambda, 2026-09-01.

A TRANSCRIPT, not a harness. Under I-9 this cell is spent and may not be re-run
to green, so what follows records exactly what was executed. It is deliberately
not a .sh: an evidence file that looks re-runnable invites someone to re-run it,
and the hardcoded binary path below is correct for THIS run and for nothing else
(there is no canonical apr path -- .cargo/config.toml redirects the target dir
and is gitignored, and four apr binaries have coexisted on this box).

--------------------------------------------------------------------- server --
/mnt/nvme-raid0/perf-gate/target-cuda/release/apr serve run \
  /home/noah/models/qwen2.5-coder-7b-instruct-q4_k_m.gguf \
  --port 18090 --gpu-layers all

verified before any measurement:
  apr --version                    -> apr 0.64.0 (745fa8588)   [a SHA, not +no-git]
  serve run --list-devices         -> "cuda    compiled in"
  git merge-base --is-ancestor 745fa8588 origin/main -> rc 0
  server log                       -> gpu-layers: requested=all resolved=28 total=28 (backend=cuda)
  GET /health                      -> {"status":"ok","compute_mode":"gpu","model_loaded":true}
  nvidia-smi --query-compute-apps  -> pid 4122729 holding 14480 MiB, GPU 100 %, 451.60 W
  binary sha256                    -> 9d0b08b015e22fb3e2cade8f0862d31b03e970c16ac8f2abf77140b7b8b63ed5
  model  sha256                    -> 509287f78cb4d4cf6b3843734733b914b2c158e43e22a7f4bf5e963800894d3c
  corpus sha256                    -> c88c9fc1985bd79ddcd2b6737f692fb69057c9b3822cd12a2b7a4c5ef93bd292

NOT used: /mnt/nvme-raid0/perf-gate/aprender/target/release/apr, which carries the
same 745fa8588 SHA and has NO CUDA. receipt.provenance.binary_sha256 proves which
of the two actually measured.

------------------------------------------------------------------- producer --
/mnt/nvme-raid0/perf-gate/target-cuda/release/apr test llm bench --band \
  --url http://127.0.0.1:18090 \
  --model qwen2.5-coder-7b-instruct-q4_k_m \
  --prompts crates/aprender-serve/benchmarks/qwen-coder/prompts-w1.jsonl \
  --bands 1,4,8,16 \
  --replicates 3 \
  --workload W1 \
  --host lambda \
  --accelerator rtx-4090 \
  --quantization q4_k_m \
  --compute-class cuda \
  --server-feature cuda \
  --tokenization server_usage \
  --tokenizer-sha256 c0382117ea329cdf097041132f6d735924b697924d6f6fc3945713e96ce87539 \
  --commit 745fa8588 \
  --comparator-owner perf-gate \
  --receipt evidence/perf-gate-001-w1-lambda

exit status 0. Stdout is producer-stdout.txt.

The corpus is origin/main's (body_words: 496), NOT PR #2821's retune. #2821 centres
the CLIENT count at 512 but moves server_usage to 520 -- exactly the band ceiling,
zero margin -- and server_usage is the only method this binary computes. Measured
here: all 1622 retained samples report prompt_tokens = 513, mid-band in 504..520.

--tokenizer-sha256 was passed as briefed and is SILENTLY DROPPED under
--tokenization server_usage; see findings.json, defect 1.

----------------------------------------------------------------------- gate --
bash scripts/perf_gate.sh --host lambda --phase merge   --workload W1 \
     --receipt evidence/perf-gate-001-w1-lambda/receipt.rN.json     # N = 1,2,3
bash scripts/perf_gate.sh --host lambda --phase release --workload W1 \
     --commit 745fa8588 \
     --receipt evidence/perf-gate-001-w1-lambda/receipt.r1.json

Outputs are gate-merge-rN.txt and gate-release-r1.txt.
