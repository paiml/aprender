{"model":"claude-sonnet-5","result":"```json\n{\"verdict\":\"PASS\",\"summary\":\"The delta re-applies 6698d343c's tree-first pv probe (PV_CLI_SRC lookup of the clap Census variant, falling back to PATH pv only when the tree source is absent) onto main's version of scripts/check_ont_ratchet.sh verbatim, byte-for-byte identical to the original commit's hunks. ONT-7 (contracts_without_valid_under / foreign_top_keys) is untouched and confirmed present at lines 115-119 and 327-335, unaffected by this insertion. `bash scripts/check_ont_ratchet.sh --self-test` measured 30 passed, 0 failed, matching the author's claim exactly, including the three #4046 rows. The probe fails closed: ont_consumer_present only returns via the tree branch when PV_CLI_SRC exists and matches the Census regex, else falls through to the PATH probe, and returns 1 when neither source has census. Deleting the tree-first branch (measured directly) flips exactly the two intended self-test rows to FAIL (28 passed, 2 failed: 'tree has census, stale PATH pv -> consumer' and 'tree lacks census, PATH pv has it -> no consumer'), while all other rows including ONT-7's stay green — proving those rows are non-vacuous and specifically pinned to the restored branch, not incidentally passing.\",\"findings\":[]}\n```"}
