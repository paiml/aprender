[package]
name = "aprender-train-canary"
version.workspace = true
edition = "2021"
publish = false

[dependencies]
burn = { version = "0.21.0-pre.2", features = ["wgpu"] }
trueno = { workspace = true, features = ["parallel", "gpu"] }
