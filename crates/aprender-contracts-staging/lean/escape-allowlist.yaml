# PVL-001 EV-6a (#4139): every compiler/logic escape in ProvableContracts/** that `pv discharge check` allows.
# {file, decl, kind, reason, ticket, confirmed_by}. `confirmed_by: pending` is PENDING: `pv discharge check --strict`
# (the EV-9 gate) stays RED until a person confirms the entry or the axiom is proved/deleted. The scanner and its
# exemptions are never confirmed by the same run (PVL-001 §5.3): these were DRAFTED from each axiom's own doc
# comment and are not confirmed.
- file: ProvableContracts/Theorems/MatMul/CooperativeTiling.lean
  decl: ProvableContracts.CooperativeMatrix.f16_accumulation_error_bound
  kind: axiom
  reason: "F16-input / F32-accumulate error bound. The file's comment: 'Status: axiom (numerical analysis result, not proved in Lean4)'. cooperative-matrix-gemm-v1 cited it as its lean_theorem; that claim is withdrawn in the same change."
  ticket: "#4139"
  confirmed_by: pending
- file: ProvableContracts/Theorems/Quantization/NF4Dequant.lean
  decl: ProvableContracts.NF4.nf4_lut
  kind: axiom
  reason: "The NF4 codebook itself (16 reals), axiomatized because the bitsandbytes constants are irrational in R. A DEFINITION posed as an axiom: every theorem over it is over an unspecified function."
  ticket: "#4139"
  confirmed_by: pending
- file: ProvableContracts/Theorems/Quantization/NF4Dequant.lean
  decl: ProvableContracts.NF4.nf4_lut_monotone
  kind: axiom
  reason: "NF4 codebook monotone. NOTE: its comment reads 'Status: proved', but it is an axiom -- nothing proves it."
  ticket: "#4139"
  confirmed_by: pending
- file: ProvableContracts/Theorems/Quantization/NF4Dequant.lean
  decl: ProvableContracts.NF4.nf4_lut_bounded
  kind: axiom
  reason: "NF4 codebook bounded in [-1, 1]. NOTE: its comment reads 'Status: proved', but it is an axiom -- nothing proves it."
  ticket: "#4139"
  confirmed_by: pending
