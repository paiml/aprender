import ProvableContracts.Theorems.Sampling.RepeatPenalty
import ProvableContracts.Theorems.Sampling.TemperatureScaling
import ProvableContracts.Theorems.Sampling.TopK

-- GENERATED by `pv challenge gen` (PVL-001 EV-7a) for contract `apr-cli-sampling-v1`. Do not edit: `pv challenge check` regenerates it and fails on any difference.
-- Each `PvlChallenge.X` restates the contract-bound theorem `X` with its proof replaced by `sorry`.

section
namespace ProvableContracts.Sampling
theorem _root_.PvlChallenge.ProvableContracts.Sampling.penalty_one_identity (l : ℝ) : applyPenalty l 1 = l := sorry
end ProvableContracts.Sampling
end

section
namespace ProvableContracts.Sampling
theorem _root_.PvlChallenge.ProvableContracts.Sampling.penalty_reduces_negative (l p : ℝ) (hl : l < 0) (hp : 1 ≤ p) : applyPenalty l p ≤ l := sorry
end ProvableContracts.Sampling
end

section
namespace ProvableContracts.Sampling
theorem _root_.PvlChallenge.ProvableContracts.Sampling.penalty_reduces_positive (l p : ℝ) (hl : 0 < l) (hp : 1 ≤ p) : applyPenalty l p ≤ l := sorry
end ProvableContracts.Sampling
end

section
namespace ProvableContracts.Sampling
open ProvableContracts
theorem _root_.PvlChallenge.ProvableContracts.Sampling.tempScale_mono_le {n : ℕ} (x : RVec n) (t : ℝ) (ht : 0 < t) (i j : Fin n) (h : x j ≤ x i) : tempScale x t j ≤ tempScale x t i := sorry
end ProvableContracts.Sampling
end

section
namespace ProvableContracts.Sampling
open ProvableContracts
theorem _root_.PvlChallenge.ProvableContracts.Sampling.tempScale_monotone {n : ℕ} (x : RVec n) (t : ℝ) (ht : 0 < t) (i j : Fin n) (h : x j < x i) : tempScale x t j < tempScale x t i := sorry
end ProvableContracts.Sampling
end

section
namespace ProvableContracts.Sampling
open ProvableContracts
theorem _root_.PvlChallenge.ProvableContracts.Sampling.tempScale_preserves_argmax {n : ℕ} (x : RVec n) (t : ℝ) (ht : 0 < t) (m : Fin n) (hm : ∀ j, x j ≤ x m) : ∀ j, tempScale x t j ≤ tempScale x t m := sorry
end ProvableContracts.Sampling
end

section
namespace ProvableContracts.Sampling
open ProvableContracts
theorem _root_.PvlChallenge.ProvableContracts.Sampling.topk_one_is_argmax {n : ℕ} (x : RVec n) (m : Fin n) (hm : ∀ j, x j ≤ x m) (i : Fin n) : kept x (x m) i ↔ x i = x m := sorry
end ProvableContracts.Sampling
end

section
namespace ProvableContracts.Sampling
open ProvableContracts
theorem _root_.PvlChallenge.ProvableContracts.Sampling.topk_separates {n : ℕ} (x : RVec n) (τ : ℝ) (i j : Fin n) (hi : kept x τ i) (hj : ¬ kept x τ j) : x j < x i := sorry
end ProvableContracts.Sampling
end

section
namespace ProvableContracts.Sampling
open ProvableContracts
theorem _root_.PvlChallenge.ProvableContracts.Sampling.topk_zero_keeps_all {n : ℕ} (x : RVec n) (b : ℝ) (hb : ∀ i, b ≤ x i) (i : Fin n) : kept x b i := sorry
end ProvableContracts.Sampling
end

section
namespace ProvableContracts.Sampling
open ProvableContracts
theorem _root_.PvlChallenge.ProvableContracts.Sampling.valid_temperature_nonneg (t : ℝ) (ht : 0 < t) : 0 ≤ t := sorry
end ProvableContracts.Sampling
end
