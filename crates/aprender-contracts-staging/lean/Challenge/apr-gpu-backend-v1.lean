import ProvableContracts.Theorems.Quantization.NF4Dequant

-- GENERATED by `pv challenge gen` (PVL-001 EV-7a) for contract `apr-gpu-backend-v1`. Do not edit: `pv challenge check` regenerates it and fails on any difference.
-- Each `PvlChallenge.X` restates the contract-bound theorem `X` with its proof replaced by `sorry`.

section
namespace ProvableContracts.NF4
theorem _root_.PvlChallenge.ProvableContracts.NF4.gpu_cpu_parity (nibbles : List (Fin 16)) (absmax : List ℝ) (blocksize : ℕ) (hbs : blocksize > 0) : dequant_blockwise nibbles absmax blocksize hbs = dequant_blockwise nibbles absmax blocksize hbs := sorry
end ProvableContracts.NF4
end
