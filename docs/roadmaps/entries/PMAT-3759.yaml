- id: PMAT-3759
  github_issue: 3759
  item_type: task
  title: 'GPU RMSNorm runs with ε = 1e-5, not the model''s 1e-6: the kernel cache key omits ε and preload bakes 1e-5 — special-token positions come out 0.431× (qwen2.5-coder-7b, F2 pos 13/44)'
  status: in_progress
  priority: critical
  assigned_to: aprender-37
  created: 2026-09-21T16:20:00Z
  updated: 2026-09-21T16:20:00Z
  spec: null
  acceptance_criteria:
  - 'Issue #3759 done_when, verbatim: "Every RMSNorm module key includes ε (its bits), and preload compiles with the model''s ε, so a cached kernel can never carry a different ε than the launch asked for."'
  - 'Issue #3759 done_when, verbatim: "A GPU test with a near-zero-mean-square input (≈1e-6) and ε = 1e-6: the GPU RMSNorm output norm matches the f32 reference within 1e-3 relative. With a 1e-5 kernel preloaded under the old key it goes RED."'
  - 'Issue #3759 done_when, verbatim: "qwen2.5-coder-7b Q4_K_M: GPU backend with no fallback, `apr run` correct, `apr qa` golden 3/3, on lambda (default) AND on gx10 with `BATCHED_PREFILL=1`. The `attn_norm` GPU/CPU norm ratio is 1.000 at the special-token positions."'
  - 'Issue #3759 done_when, verbatim: "**done_when 4 (Qwen3.5):** a Qwen3.5-9B chat-prompt cell on lambda and gx10, 0.69.0 against the fix."'
  - 'Issue #3759 done_when, verbatim: "**done_when 5 (siblings):** every PTX/JIT module-cache key either carries each parameter its kernel bakes in (epsilon, RoPE theta, head dims, scales) or reads it at runtime, shown per key. A guard/test fails when a key omits a baked parameter. Any sibling key the sweep finds incomplete is fixed in this change."'
  phases: []
  subtasks: []
  estimated_effort: null
  labels:
  - kind:code
  notes: '0.69.1 row (#3710), dispatched and widened by the cop, aprender-04. Stacked on PMAT-3728 (#3728), which stacks on PMAT-3727 (#3727). With all three, qwen2.5-coder-7b''s F2 rejections are all gone; this is the third defect in that model''s lambda red. LOCALIZATION. After #3728, the 7b still fell back at pos 44 (0.69.0''s double chat template) / pos 13 (the #3672 clean prompt), with argmax equal and cos ≈0.935 / 0.883. It fails on lambda on BOTH prefill paths and on gx10 only with `BATCHED_PREFILL=1`. `apr parity --per-op` (serial path) on the clean chat prompt `<|im_start|>user\nWhat is the capital of France? Answer briefly.<|im_end|>\n<|im_start|>assistant\n`: layer-0 attention diverges at pos 0 (cos 0.738), 13 and 15, the `<|im_start|>` / `<|im_end|>` positions. Float64 ground truth (layer-0 Q/K/V dequantized from the GGUF with `gguf` 0.17.1, plus bias, on the dumped attn_norm input, at pos 0 where RoPE is the identity): cos(truth, CPU) = 1.000000 for Q, K and V; cos(truth, GPU) = 0.994 / 0.99993 / 0.738. So the CPU reference is exact and the GPU is wrong. The cop''s reference-drift hypothesis (#3751) was ruled out by that, and by `DIRECT_FP32_GEMV=1`, which moved pos 44 only 0.9348→0.9382. The GPU''s W·x has the right direction (cos 1.0000) and 0.431× the magnitude, for Q and V alike, so its input was scaled. The per-position GPU/CPU norm ratio of the attn_norm output fits RMSNorm at epsilon = 1.00e-05 at all 17 positions, against the model''s 1e-6. Ordinary tokens (embedding mean-square ≈3e-4) come out 0.986×; the special tokens (mean-square 1.05e-6) come out 0.431×. MECHANISM. Epsilon is a PTX immediate (`mov_f32_imm`), but the module cache keyed the norm kernels by shape alone (`rmsnorm_vectorized_{h}`, `batched_rmsnorm_vectorized_{h}`, …). The model-load preload in `gguf/cuda/mod.rs` (the "[GH-129] Early kernel preload: N modules compiled" line in every run) compiled those keys at a hardcoded 1e-5, and every later launch passing 1e-6 reused them. At cc ≥ 120 the serial path uses `rmsnorm_simple_{h}`, which is never preloaded, so gx10 serial was green and gx10 batched red. HOW EACH done_when IS MET. (1) Commit 1: `CudaExecutor::f32_bits_tag` (the exact bits, `b{:08x}`) is appended to every norm-kernel key that omitted epsilon: plain/simple/vectorized/precise/batched RMSNorm, per-head and batched per-head, fused-residual (2 sites) and batched fused-residual, LayerNorm, and the fused gate-up-SwiGLU. The fused RMSNorm+Q4K GEMV key used the rounded `{:.0e}` and now uses the bits. `preload_modules_for_capture` / `preload_modules_for_batched_capture` take epsilon, and every caller passes the model''s (`model.config.eps` at model load; the forward''s `epsilon` in the graph-capture paths). `preload_rmsnorm_module` / `preload_batched_prefill_modules` compile with it and key it like the launch. (2) GPU rows `cuda/executor/rmsnorm_eps_tests_3759.rs`, input mean-square ≈1.05e-6 (the special-token value). Row 1: serial and batched RMSNorm at 1e-5, then at 1e-6 in the same process, each within 1e-3 of the f32 reference. Row 2: `preload_modules_for_capture(…, 1e-6)` on a harness, then launch, within 1e-3. Each asserts it is not vacuous: the reference norms at the two epsilons differ by more than 2×. MUTANT A (the key without epsilon, the preload still passing the model''s) and MUTANT B (A plus the preload''s hardcoded 1e-5, i.e. the pre-fix code): see MEASURED. (3) See MEASURED. WIDENED BY THE COP, now done_when 4 and 5 on the issue (quoted verbatim in acceptance_criteria). (a) Qwen3.5 (done_when 4): `forward_qwen35_cuda.rs` calls `rmsnorm_into` and `per_head_rmsnorm_into` with the model''s 1e-6, and both keys omitted epsilon, so the flagship was exposed too. Cells: Qwen3.5-9B on 0.69.0 vs this change, lambda default and gx10 `BATCHED_PREFILL=1`. (b) Siblings (done_when 5), commit 2. `ensure_kernel_module(key, &KernelType)` (new `cuda/executor/module_key_guard.rs`) becomes the compile-on-miss step at 154 of the 203 live module-cache sites (every idiom site, including the preloads), migrated by script. Under `cfg(any(debug_assertions, test))` it records the PTX hash compiled under each key. A later hit whose request differs from every request already proven equivalent is regenerated, and a different hash panics naming both. Grid-only differences have the same PTX and pass. The guard''s own rows: one key asked for 1e-5 then 1e-6 panics; `BatchedVectorizedRmsNorm` with batch_size 1/8/49 under one key passes. The 49 unguarded live sites are classified by reading in `docs/audits/pmat-3759-module-key-audit.md`. That sweep found and fixed two more incomplete keys: every RoPE key omitted theta (baked via `mov_f32_imm(theta.log2())`; masked today by one theta per process, since the model-load preload runs after `set_rope_theta`), and the fused prefill attention used the constant key "fused_prefill_attn" while baking head_dim, heads_per_kv and 1/√head_dim. Both are now keyed. WHERE THE GUARD RUNS: every `cargo test -p aprender-serve --features cuda --lib`, release or debug. In CI that is `ci.yml` job `cuda-unit`, whose `gguf::cuda::` filter does not select the executor rows added here, and they need a GPU. Widening it, or adding `cuda::executor::` to cuda-nightly, is a workflow change left to the release owner. Until then the release-path backstop is the ladder''s F2 fallback cell, and the guard runs wherever this crate''s cuda tests run locally (all of this session''s runs). NOT DONE / LEFT: `layers/loading.rs`, `layers/preload_utilities.rs` and `executor/kernel.rs` are dead copies (nothing `include!`s them) that still hardcode 1e-5; deleting them is its own change. STOP LIST: no change to any kernel''s PTX, to epsilon or theta values, or to which kernel a path selects; no default-off workaround; no workflow edit. MEASURED: all on hardware through `gpu-q --prio 1` (the cop''s ordered GPU queue), binaries pinned by `--version` sha: `c0f4869d2` (commit 1''s code) and `a95681f71` (this change''s code, before one clippy fix in the debug/test-only guard, which a release library does not compile). TESTS on lambda (RTX 4090), no SKIP lines: `rmsnorm_eps_tests_3759` epsilon rows 2/2 pass; the guard rows 4/4 pass, with the guard refusing one key for two epsilons ("module key `guard_probe_3759` is incomplete: … VectorizedRmsNorm { hidden_size: 1536, epsilon: 1e-5 } … now asked for … epsilon: 1e-6"). MUTANT A (the key without epsilon): the two-epsilon row goes RED, "serial/batched eps=1e-6 after 1e-5: output norm 12.0846, reference 28.0525 (56.9% off)", the 0.43x signature. MUTANT B (A plus the preload''s hardcoded 1e-5, i.e. the pre-fix code): both rows RED. GUARD SWEEP: the whole `aprender-serve --features cuda` lib suite matching `cuda`, a debug test build with the guard live: 1439 passed, 0 failed, 7 ignored, 246 s. No exercised key produces two PTX texts. HARDWARE, qwen2.5-coder-7b Q4_K_M, sweep prompt, `apr run --gpu -v` + `apr qa` golden: lambda default path (batched, FP8 on): rc 0, `Backend: GPU (NVIDIA GeForce RTX 4090)`, "The capital of France is Paris.", qa 3/3 on both binaries. 0.69.0 fell back at pos 1 (#3728), and the PMAT-3728 binary at pos 44 (cos 0.9348). gx10 (GB10, sm_121) default (serial): rc 0, GPU, "Paris", qa 3/3 on both binaries. gx10 `BATCHED_PREFILL=1`: pos 1, argmax equal, cos 0.8720. That is issue #3785: FP8 batched prefill on sm_121, not this defect. On this binary `FP8_PREFILL=0` is green, and `DIRECT_FP32_GEMV=1` stays 0.8716, so it is not the CPU reference. The same FP8 batched path is green on lambda. PMAT-3785, stacked on this, turns FP8 prefill off at cc >= 100, and with it the gx10 `BATCHED_PREFILL=1` cell is green (binary `73176dc4c`: rc 0, `Backend: GPU (NVIDIA GB10)`, "Paris", qa 3/3). So done_when 3''s gx10 cell is met by this change plus PMAT-3785; this change alone is green there with `FP8_PREFILL=0`. qwen2.5-coder-0.5b: lambda default rc 0, qa 3/3; gx10 `BATCHED_PREFILL=1` rc 0, qa 3/3. PER-OP on the chat prompt, lambda, serial path, this binary: the GPU/CPU attn_norm norm ratio is 1.0000 at all 17 positions, including `<|im_start|>`/`<|im_end|>` (was 0.4312 at those positions); layer-0 attention cos 1.0000 everywhere; lm_head >= 0.9993. F2 EVIDENCE: the dense qwen2 F2 has no receipt cache (it skips only on `SKIP_PARITY_GATE` or a probe under 2 tokens), so these are real passes. QWEN3.5-9B (the cop''s widening): aprender-c7''s exact `apr chat --gpu` repro with a FRESH `APR_F2_RECEIPT_DIR` fails identically on 0.69.0 and on this change: lambda pos 23 cos 0.8338, gx10 0.8390. It is NOT this defect; gx10''s serial RMSNorm (`rmsnorm_simple`) was never preloaded at 1e-5. It is routed separately. My first Qwen3.5 cells were VACUOUS: F2 skipped them via a cached receipt ("CPU reference forward skipped [source=receipt]"). They are discarded, and that gate-integrity finding went to the cop. GATES: `cargo clippy -p aprender-serve --lib --features cuda -- -D warnings` rc 0; `cargo fmt --all -- --check` rc 0; complexity ratchet "none new, none grown".'
