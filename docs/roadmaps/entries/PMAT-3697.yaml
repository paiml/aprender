- id: PMAT-3697
  github_issue: 3697
  item_type: task
  title: 'guard-tree python dependence, batch 1 of #3697: 4 guards python-free or UNMEASURED (t2_preflight_verdict, no_hosted_runners, nextest_ci_profile_no_fail_fast, roadmap_ids_unique) + scripts/lib/python_fleet_state.sh'
  status: in_progress
  priority: high
  assigned_to: aprender-0e
  created: 2026-09-21T15:20:00Z
  updated: 2026-09-21T16:40:00Z
  spec: null
  acceptance_criteria:
  - 'Issue #3697, verbatim: "**What lands (0.70):** per guard, one of: - (a) remove the python dependency (the root fix, the direction of #3694: no python in automation). For YAML reads, prefer `pv`/bashrs/Rust readers the repo already builds. - (b) where a python reader stays, classify its absence as fleet state: `UNMEASURED … reason=<no-yaml|no-python|no-toml-reader>`, exit 0, surfaced by guard_tree (#3651), never red. That is the ruling applied to fleet-pv (#3669''s andon) and to pathonly (#3692)."'
  - 'Issue #3697, verbatim: "My own `check_nextest_ci_profile_no_fail_fast.sh` (#3626) is on the no-python3 list. It reads TOML through a purpose-built fallback on 3.10, but with no interpreter at all it reports ENV rc 2, which guard_tree counts as FAIL: same class, (b) applies."'
  - 'Cop dispatch (aprender-04 to aprender-0e, 2026-09-21), verbatim: "Each guard becomes python-free, or UNMEASURED per the #3692 ruling. One PR per 3–4 guards so the reviews stay small." and "same proof shape (no-python3 PATH + debian:bookworm-slim)".'
  - 'NOT in this batch (it lands with the last batch, when it can be green): the issue''s "one guard-tree row: `guard_tree.sh --no-cargo` under a `python3 -S` stub and a no-python3 stub. It must end `0 failed`". Today 18 other guards still fail it.'
  phases: []
  subtasks: []
  estimated_effort: null
  labels:
  - kind:code
  notes: 'SCOPE: scripts/lib/python_fleet_state.sh (new, sourced), scripts/check_t2_preflight_verdict.sh, scripts/check_no_hosted_runners.sh, scripts/check_nextest_ci_profile_no_fail_fast.sh, scripts/check_roadmap_ids_unique.sh, and this fragment. The rest of #3697 (18 guards, and the stub-shaped guard-tree row) is later batches. (a) PYTHON-FREE. t2_preflight_verdict: the self-test''s mutant splice (replace decide() with the pre-#3561 absence-as-consent body) is awk over a quoted-heredoc mutant, cut from the `decide() {` line through the first line that is exactly `}`; a subject with no such close is ENV. The spliced file is BYTE-IDENTICAL to the python splice''s output under gawk 5.1, mawk 1.3.4 and busybox awk. no_hosted_runners: the scan is awk; POSIX ERE has no \b, so the token''s boundaries are spelled `(^|[^A-Za-z0-9_])…([^A-Za-z0-9_]|$)`. DIFFERENTIAL vs the python scan (cwd-relative paths, same baseline semantics, sorted stale list): identical stdout+rc on the real 21 workflows (with and without a baseline), on a 611-violation adversarial corpus (every token shape x 11 prefixes x 12 suffixes), and on two baseline corpora, under all three awks, EXCEPT a token touching a non-ASCII letter (e.g. ubuntu-latesté): python (Unicode \w) passed it, awk (ASCII word chars) flags it. 0 violations lost, 111 added, all with é. Rows 13-15 pin both boundaries and the ASCII choice (rows 13/14 agree with python; row 15 is the deliberate difference). (b) UNMEASURED. scripts/lib/python_fleet_state.sh: py_fleet_state <guard> [module...] probes the interpreter (PY_FLEET_PYTHON, default python3) by importing each module; only ModuleNotFoundError is `not installed`. rc 0 ok; rc 3 fleet state with ONE stderr line `UNMEASURED runner=<r> reason=no-interpreter interpreter=<i> guard=<g>` (absent, or exec 126/127) or `reason=no-module module=<m>`; rc 2 ENV for an interpreter that exists and dies or answers nothing. Reason names: the tree already says `no-interpreter` (#3692 pathonly, #3695 denominator), so that is kept over the issue''s example `no-python`, and `no-module module=yaml` names the module rather than coining one reason per module. Sourced, option-neutral (check_sourced_libs_option_neutral.sh OK: 9 libraries). Its case table py_fleet_state_self_test (5 stub rows + 2 real-interpreter rows that print UNMEASURED where there is none) runs inside each consuming guard''s --self-test, because a scripts/tests file would be dark without a workflow edit. nextest_ci_profile_no_fail_fast: judge() probes first; no interpreter -> rc 3 -> the run exits 0 with the UNMEASURED line and no PASS line; /bin/false still ENV rc 2; the old row `no interpreter at all -> ENV rc=2` is now `-> UNMEASURED rc=3` plus a run-level row; the reader table (both readers) is UNMEASURED where python is absent. roadmap_ids_unique: scan() needs python3 + PyYAML; either missing -> UNMEASURED exit 0; a dying python3 -> ENV; the 5 YAML rows are UNMEASURED where there is no reader; the missing-file row and two new fleet-state rows run everywhere. MEASURED, guard_tree.sh --no-cargo on 225b2a9ab + this batch, each shape a python3 stub first on PATH: normal python3 76 checks, 0 failed (unchanged); python3.10 -S 14 -> 12 FAIL rows (roadmap_ids_unique s+r gone); no python3 (stub exits 127) 29 -> 22 FAIL rows (all 7 rows of these 4 guards gone), and under PASS guard_tree surfaces the UNMEASURED lines (#3651). debian:bookworm-slim (sovereign-ci''s FROM base) + git, python3 NONE, jq NONE, mawk 1.3.4.20200120-3.1: t2_preflight_verdict self-test rc 0 (2 ok) run rc 0; no_hosted_runners self-test rc 0 (15 ok) run rc 0 `OK: 21 workflow files`; nextest self-test rc 0 (9 ok, 3 UNMEASURED) run rc 0; roadmap_ids_unique self-test rc 0 (8 ok, 3 UNMEASURED) run rc 0 with the UNMEASURED line. MUTANTS, each against a scratch copy, all RED: py_fleet_state mapping every failure to UNMEASURED -> roadmap_ids_unique self-test rc 1 (`a python3 that dies -> ENV exit 2, never UNMEASURED`, plus the helper''s crash rows); no_hosted_runners leading boundary dropped -> rc 1 (row 13 xubuntu-latest); t2 splice made a no-op -> rc 1 (`the pre-#3561 mutant PASSED the case table`). bashrs: check_shell_lint_ratchet.sh PASS (errors only may not rise).'
