- id: GH-4114
  github_issue: 4114
  item_type: task
  kind: code
  title: check_package_includes.sh skips cfg(test) modules — it passed a crate whose published-tarball cargo test cannot compile (3 includes); the gate must build the real .crate
  status: inprogress
  priority: medium
  assigned_to: null
  created: 2026-09-24T01:04:44.436321582+00:00
  updated: 2026-09-24T01:04:44.436321582+00:00
  spec: null
  acceptance_criteria:
  - 'the gate builds the REAL .crate: cargo package every publishable crate, unpack outside the tree, siblings patched to their tarballs (printed), cargo build --workspace --tests --keep-going; errors named by crate and file:line'
  - 'RED on the published aprender-serve 0.69.1 .crate (sha256 22a710f0...), naming the 3 includes (--negative-control)'
  - 'aprender-serve GREEN from its tarball on 4eb21dbac (the #4048 fix)'
  - 'a planted include_bytes of an excluded tests/ file inside a new #[cfg(test)] module is RED; the source reader does not list it'
  phases: []
  subtasks: []
  estimated_effort: null
  labels: []
  notes: 'Cop row (aprender-cf, 2026-09-24), filed by aprender-70. Release-time gate scripts/package_tarball_build.sh, declared in [package.metadata.dogfood].gates (no workflow edit; cargo-free guard_tree cannot run it). The PR-time half is rows 14-16 of check_package_includes.sh --self-test, which ci.yml guard-cargo already runs. MEASURED on lambda (compile only): main 49fe19c28 RED in 9 crates in 275 s with a warm target -> filed #4129 (include! outside the .crate) and #4130 (shipped integration tests with stripped path-only dev-deps). Negative control OK in 288 s. On 4eb21dbac aprender-serve is absent from the RED rows (582 s). A defect in the gate''s own first draft: tar restores archived mtimes and cargo decides freshness by mtime, so a warm target dir reused a DIFFERENT build of the same name and version (a false green, caught by row 15). Unpacking with tar -m fixes it.'
