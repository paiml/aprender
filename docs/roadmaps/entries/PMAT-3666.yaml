- id: PMAT-3666
  github_issue: 3666
  item_type: task
  title: 'APEX-2d: raster — NEW crates/aprender-viz/src/raster.rs svg_to_png behind `raster` (resvg default-features OFF, so no font database is compiled in; a roxmltree pre-scan refuses text/tspan/textPath/tref/altGlyph/textArea/font/font-face/foreignObject/image/feImage in the SVG namespace OR none; href resolver reads nothing; resvg render; row-wise demultiply into Framebuffer; pinned PngEncoder, never encode_png); error.rs Error::SvgElementRefused; lib.rs pub mod raster; NEW tests/raster_determinism.rs (11 tests + per-host raster-<arch>.json); NEW scripts/ci/raster-compare.sh (10-case self-test, 2 mutants) in ci.yml determinism-compare, raster step + upload path in determinism; svg_to_png split into the scale check + prescan_refuse + a PRIVATE render_unscanned, with ONE #[cfg(test)] unit test a_text_element_paints_nothing_because_no_font_database_is_compiled_in (no public API growth, no repo path so the tree-reader registry is unchanged); NEW scripts/ci/raster-fontdb-ban.sh (cargo-tree ban on fontdb/rustybuzz/ttf-parser/font*, 6-arm self-test incl. the as-shipped control and a contains-not-prefix arm) + its ci.yml step and a --lib raster:: step, both in determinism; tree_reader_tests.txt; Cargo.lock drops zune-jpeg 0.4 + fontdb/rustybuzz edges; png_identical RECORDED not asserted; NO publish (apex APEX-001 row EV-2d)'
  status: planned
  priority: medium
  assigned_to: null
  created: 2026-09-21T15:54:31Z
  updated: 2026-09-21T15:54:31Z
  spec: null
  acceptance_criteria:
  - 'accept (APEX-001 EV-2d): tests_at_least 3 -p aprender-viz --features raster --test raster_determinism; the two-host raster-receipt.json has (.hosts|length)==2 and (.png_identical|type)=="boolean" (recorded, not asserted). Each mutation names the arm it turns RED, because the two mutations are observable in different places: M1 (replace the prescan_refuse call) turns EXACTLY the 4 refusal tests in tests/raster_determinism.rs RED and leaves the other 7 green (measured 2026-09-22: 7 passed, 4 failed; plant and restore both sha256-verified). M2 (resvg default-features back ON plus opt.fontdb_mut().load_system_fonts()) turns raster::tests::a_text_element_paints_nothing_because_no_font_database_is_compiled_in RED and scripts/ci/raster-fontdb-ban.sh RED, and leaves all 11 integration tests GREEN (measured 2026-09-22). M2 is unobservable through svg_to_png BY DESIGN — the pre-scan refuses before usvg builds a tree — which is why the unit test calls render_unscanned and the ban reads the dependency graph instead; behaviour alone cannot pin the flag, since an EMPTY font database renders the same as a missing one. The earlier wording, that M2 turns the 4 refusal tests RED, was MEASURED FALSE (11 passed, 0 failed) and is superseded. Publishing aprender-viz stays operator-only (STOP(PUBLISH)).'
  phases: []
  subtasks: []
  estimated_effort: null
  labels:
  - kind:code
  notes: null
