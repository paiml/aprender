{"id":"E8-R2b-001","epic":4002,"row":"R-2b","date":"2026-09-26","finding":"apr distill cannot honor a recipe: every distill recipe must carry training.seed (no default) and learning_rate, but no distill path consumes either","evidence":["crates/apr-cli/src/commands/distill.rs execute_distillation: fixture path never reads data_path (existence check only) and takes no lr/seed/batch","crates/apr-cli/src/commands/distill.rs translate_to_distill_config: DistillYamlConfig.training.seed dropped (TrainingConfig has no seed field); DistillTrainingConfig is #[allow(dead_code)]","crates/aprender-train-distill/src/config.rs TrainingConfig {epochs,batch_size,learning_rate,warmup_steps,gradient_accumulation,mixed_precision}: no seed","run_cuda_backend takes temperature/alpha/epochs only: no lr, seed or batch"],"consequence":"under RECIPE-INV-005 (apply or refuse by name) `apr distill --recipe` would refuse every recipe at training.seed, so R-2b is blocked on trainer work, not CLI wiring","next":"add seed to aprender-train-distill TrainingConfig and thread seed+learning_rate through translate_to_distill_config and run_cuda_backend; then R-2b is a thin loader mirroring finetune_recipe.rs","status":"open"}
{"id":"E8-R2b-002","epic":4002,"row":"R-2b","date":"2026-09-26","resolves":"E8-R2b-001","status":"fixed-trainers","finding":"distill trainers now take the configured lr and seed: config-mode CPU student steps at training.learning_rate (was hardcoded 0.1); TrainingConfig.seed (serde default 42) survives translate_to_distill_config; cuda student via CudaTransformerTrainer::for_training(lr, seed). Flag-path cuda keeps prior defaults 1e-3/42.","evidence":["FALSIFY-RECIPE-007 contracts/apr-recipe-v1.yaml","mutant lr->0.1: falsify_recipe_007 FAILED (final 2.1702094 == 2.1702094)","mutant drop seed in translation: falsify_apr_distill_train_010 FAILED (seed 42 != 9)"],"open":"apr distill --recipe loader (data.train mapping) not yet wired; fixture flag path still trains nothing"}
{"id": "E8-R2c-001", "epic": 4002, "row": "R-2c", "date": "2026-09-26", "resolves": "E8-R2b-002 (recipe loader not wired)", "status": "fixed-loader", "finding": "apr distill --recipe FILE now drives the cuda backend: student, teacher, one hashed .bin shard, temperature (default 3.0 = CLI default), epochs, batch_size, learning_rate and seed are applied; non-distill kind, adapter rank/alpha and a non-.bin data.train are refused by name. ShardBatchIter accepts a single .bin file so exactly the hashed bytes are read.", "evidence": "FALSIFY-RECIPE-008; shard_reader 8/8, finetune_recipe 6/6, apr-cli distill 41/41; clippy -D clean apr-cli+aprender-train; apr-cli check --features cuda ok; mutants killed: single-file->parent-dir read (reads a.bin), batch_size->1 ((3,1,11)!=(3,8,11))", "open": "(1) eval.held_out is hashed but no consumer (finetune or distill) runs held-out eval: RECIPE-INV-005 gap on both; (2) run_recipe->run_cuda_backend batch_size/data threading is cuda-only and has no GPU test; (3) config mode (--config) still drops dataset.path and trains on SyntheticBatchSource; (4) a multi-shard corpus cannot be a recipe data.train (single-file hash) - design call if needed"}
{"id": "E8-R2d-001", "epic": 4002, "row": "R-2d", "date": "2026-09-26", "resolves": "E8-R2c-001 open (1), finetune half", "status": "fixed", "finding": "apr finetune --recipe hashed eval.held_out but validated on a seeded slice of data.train, so val_loss was measured on training data. Now InstructTrainer::with_held_out trains on all of data.train and validates on exactly eval.held_out; eval.metric accuracy is refused by name (trainer reports loss only); the wgpu trainer refuses eval.held_out by name; --recipe conflicts at parse time with --merge/--task/--adapters/--adapters-config, which never run that validation.", "evidence": "FALSIFY-RECIPE-009; instruct_trainer 6/6, apr-cli finetune_recipe+parse+finetune 61/61; clippy -D clean apr-cli+aprender-train; apr-cli check --features cuda ok; mutants killed: held-out->seeded split (test FAILED), metric check disabled (refusals test FAILED)", "open": "(1) distill --recipe still hashes eval.held_out without evaluating it (cuda backend has no held-out loss); (2) dispatch->execute_training held_out threading has no test short of a real APR model; (3) cfg(not training) execute_training stub signature already diverged from its caller (pre-existing)"}
{"id": "E8-R2e-001", "ticket": "#4002", "date": "2026-09-26", "status": "fixed", "finding": "apr distill --recipe hashed eval.held_out but never evaluated it; distill reported only train-batch KD loss", "fix": "Pipeline::with_eval_source runs one forward-only pass of whole batches over the held-out .bin shard after training -> TrainingMetrics.val_loss (JSON val_loss); load_distill refuses a non-.bin eval.held_out and eval.metric accuracy by name", "evidence": "FALSIFY-RECIPE-010; 3 mutants killed (eval trains student, eval draws 1 batch, shard check removed); a uniform-gradient mutant survived because softmax is shift-invariant, replaced by a non-uniform one", "open": ["cuda held-out path has no GPU test", "APR_DISTILL_SMOKE_SEQ_LEN env still sets seq_len outside the recipe"]}
