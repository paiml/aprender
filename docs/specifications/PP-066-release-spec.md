# PP-066 — 0.66 performance-parity release: implementation spec (v1.6)

**Status:** DRAFT v1.6 · 2026-09-05 · rewrite of `0_66-performance-parity-report.md` (the "report") into
paiml-implement units · v1.1 adjudicates `0_66-review.md` (six-report swarm, one explorer with tree access at
`d6c6c6f8` / `587ad0797`) — verdicts in §11 · v1.2 promotes the **backend registry** (R-0) to the first code ticket of 0.66 after the five-whys of 2026-09-05 (§1 F-25): the GPU defect is design, not configuration · v1.3 folds in **#2869** (no pre-built `apr` on tagged releases) and **PV-IMPROVE-001** (`docs/specifications/improve-provable-contracts.md` `[U]` path; the `pv` enforcement half is theater at `b1a6324b8`) — the first changes the artifact of record, the second changes what "contract per feature" is worth · v1.4 assimilates a third-party comparative study of GPU discovery in llama.cpp / llamafile / Ollama (`[X]`, §12) into R-0 as fourteen numbered requirements with a failure-catalogue case table — the discovery story is the product's first screen and it has to be right · governing inference spec `docs/specifications/PP-LLAMA-001-MASTER.md` (v3.0 in
hand; v3.1 `[U]`) · this document **adds tickets and re-scopes**; it does not restate PP-LLAMA-001 §4–§7.

**Repo:** `paiml/aprender`, default branch `main` (protected, required check `ci / gate`). Training track
ships in `crates/aprender-train` (`[lib] entrenar`), serve track in `crates/aprender-serve` (`[lib] realizar`),
compute in `crates/aprender-compute` (`[lib] trueno`) — workspace crates (APR-MONO), never standalone.

**Workflow per ticket (paiml-implement, AUTO-IMPL-SKILL-001):** P0 discovery (`discover.sh` → `discover.json`,
abort on vacuity) → P1 plan (phases with an acceptance **command** `A_i`, estimate `K̂` with `basis=` or `[U]`)
→ P2 execute (RED test commit first; every `A_i` re-run by the orchestrator — a worker PASS is a claim) → P3
quorum (`--quorum auto` once the agy slash-surface is verified; until then `--quorum never` + manual
`pr-review-quorum`) → PR → `ci / gate` → `pmat work complete`. Receipt: `docs/audits/impl-<ticket>-receipt.md`.
`pmat work add` mints ids; the `id` column below is the spec handle, not the ticket number.

**Marks:** `[V]` executed here (arithmetic, document reads) · `[C]` computed, arithmetic shown · `[A]` taken
from the report or master as written at `587ad0797` — re-verify at implementing HEAD · `[U]` unverified.
The report's private `[X]` ("from a receipt as written") is `[A]` here; `[X]` keeps its fleet meaning
(third-party, excluded from published claims).

---

## §0 Decisions required before ticket #1

None of these is resolved by this document. Each is a `pmat work add "DECISION: …"` ticket whose artifact
is one line in this section with `decided_by` and date. Tickets marked ⛔ below are blocked on the decision.

| id | decision | recommended default (applied below, marked *pending*) | evidence |
|---|---|---|---|
| **D-1** | 0.66 scope: all five tracks (report §11, 39 rows) or the split in §2 | **Split.** 0.66 = instrument chain + reference cells + refusal/install + two lambda speed rows + training *measurement* + guards. Backend lanes, kernel work, T-1..T-5, renames → 0.67 | `[C]` 39 rows / 49 days / 2 owner identities / 1 gx10 window at a time; master §9: "sequenced, never concurrent on one host" |
| **D-2** | Is the Unsloth throughput concession (`fable-architectural-review.md` §7c, 2026-07-05: "P3 Triton GPU tok/s race vs Unsloth — conceded") revoked? | **Not assumed revoked.** T-7 ships REPORTING with a self-ratchet seeded at achieved; arms on first P-5 PASS (P-6), never by date | report §6 T-7 vs review §7c; `[C]` 2,101/16,118 = 0.13× on the 1.5B |
| **D-3** | CI home for non-CUDA lanes: may `intel` (clean-room runner, 8 concurrent, memory-bound) host a wgpu lane and a wasm/browser harness; may `mini` (cowork-first) host nightly Metal? | **Escalate; no default.** APR-QUALITY-001 §9.1 already holds this open ⛔. B-W1, B-S*, B-M* are blocked on D-3 | guide §3 host table; `feedback_two_concurrent_workspace_tests_starve_the_box` |
| **D-4** | The report re-dates master §12 rows 19/20/21 (10-16→09-19, 10-23→09-26, 11-06→10-10) without an Appendix D amendment | **Master dates stand** until an amendment names who moved them and why | master §12 preamble; report §11 rows 1, 3, 10 |
| **D-5** | `[lib] name = "aprender"` declared by two manifests (root `Cargo.toml:692-694` and `crates/aprender-core/Cargo.toml:71-72`) `[A]` | **Undecided** — the report defers it; this spec files it as a ticket (G-2) and the guard reports it under its own row | report §6.7 decision row |
| **D-6** | AMD path: if Step-0 (§3 S0-3) shows a Radeon on `intel`, does 0.66 measure AMD through wgpu on that host? | **Yes if S0-3 confirms**, as a receipt on the existing lane; ROCm/HIP stays blocked (Polaris-class parts are outside ROCm support regardless) | §3 S0-3 |
| **D-7** | Refusal exit code: the report writes `exit 2` throughout; the tree maps `CliError::FeatureDisabled` → **9** (`crates/apr-cli/src/error.rs:106` at `da49c1081` `[V]`; v1.5 cited `:86-90,218` from the review); 2 is clap's usage-error code | **Keep 9** and have every test read the constant from `error.rs` rather than type a number — one refusal code, no second one invented for this spec | review MIN-07 |
| **D-9** | `cuda` in the published crate's default features (the why-1 countermeasure). Safe **only** behind R-0: with a registry, a missing `libcuda.so.1` is `backend: cuda unavailable reason=…` printed, never a crash or a silence | **Yes, if S0-14 passes on all four hosts and clean-room p1 is green** — the `cuda` feature has no link-time CUDA dependency (driver and cuBLAS are `libloading` dlopens `[A]`), so the cost is compile time and binary size, both measured and REPORTED, not gated. If S0-14 fails on any host, the default stays `cli` and W-I ships the documented `--features cuda` path with the registry's refusal | five-whys 2026-09-05; review §1.1 |
| **D-10** | Per-target feature set of the **release binaries** (#2869 makes them the artifact of record). `nightly.yml` already cross-compiles 5 targets `[A]`; which features each carries is S0-16 | `cli,cuda` on every target where the `cuda` feature compiles without a toolkit (S0-14 run per *target*, not per host: linux x86_64/aarch64 via `libcuda.so.1`, windows via `nvcuda.dll`, darwin if it compiles); `cli` otherwise. The registry (R-0) prints `cuda unavailable reason=DriverNotFound` on machines without the driver. `metal` joins darwin in 0.67 (B-M1) | #2869; review §1.1 |
| **D-11** | How much of PV-IMPROVE-001 rides 0.66. Its phases are 0 (truth, 2 d) · 1 (enforcement can fail, 1 w) · 2 (credit only for discharged proofs, 2 w) · 3 (corpus hygiene) · 4 (downstream) `[A]` | **Phase 0 (0.1–0.6) and 1.1/1.2 in 0.66**, as blockers of C12 — every PP-066 card claims "contract in the same PR", and at HEAD `pv lint` cannot fail on a bad one (threshold 0.00, `registry: true`, prose `test:`), so the claim is worth nothing until phase 0 lands. Phases 1.3–4.3 stay under epic #2556 with their own DAG (0.67 lane, §6). PV-IMPROVE's own decisions are referred to here as **PVI-D-1..5** (ladder, `kind: descriptor`, fleet dir, Lean nightly, delete dangling refs) and are not restated | PV-IMPROVE-001 §5, §8 |
| **D-8** | Rename sequencing (0.67): report rule 2 says one PR per crate, never batched; the review measured 4,097 in-tree `use` sites, 24 leaf crates carrying 52 of them, and proposes batching leaves | **Escalate.** Default: one PR per crate stands for the 27 non-leaf crates; leaf crates (≤ 9 sites) may batch ≤ 10 per PR only if each PR shrinks `check_crate_names.sh remaining` by exactly its crate count; `realizar` (1,428 sites) lands alone in a declared rename-freeze window | review MAJ-09 |

---

## §1 Findings register — deltas from the report

Severity: S1 ship-blocker (the spec cannot be executed as written) · S2 (a gate is theater or a number is
wrong) · S3 (hygiene). Every row names the fix and where it lands in this document.

| # | sev | finding | root cause (five whys, terminal) | fix → where |
|---|---|---|---|---|
| F-1 | S1 | Five specs in one document: inference speed, training parity (new definition), backend promotion, CUDA doc, 51-crate rename. paiml-implement lands one ticket per run; a 39-row DAG spanning five artifacts has no unit of work | "release" used as the organizing unit instead of "artifact with a falsifier" | Split into tracks with independent DAGs; D-1 → §2 |
| F-2 | S1 | Zero `pv` contract paths named in 39 rows; the §2 seven-clause "first-class" definition has no gate, so row 35's "7/7" is a sentence | contract-per-feature stated in the preamble, never applied per row | Every card names its contract; `check_backend_firstclass.sh` (B-G1) computes N/7 from the tree and fails on an over-claim → §5 |
| F-3 | S1 | Row 17 (reference cells CONFORMANT) is blocked by rows 1,2,3,8,9,10 — all speed — and by **none** of the master's instrument rows (0a–0e, 6, 7, 12, 15). Consequence: no conformant receipt until six speed workstreams land; every `AbRecord` produced meanwhile has nothing to reconcile against (master §10) | the report's DAG was written from the report's own rows, not joined to the master's | Row 17 blockers = master instrument chain; speed rows land after and reconcile → §5 I-track, S-track |
| F-3b | S2 | Three master expiries silently moved earlier by 4–5 weeks (rows 19, 20, 21) | no amendment discipline in the report | D-4; master dates restored pending → §5 S-1, S-2 |
| F-4 | S1 | T-7 gates `train_tok_per_sec` LCB95 ≥ 1.0 vs Unsloth on both hosts by 2026-10-23, from 0.13× measured on the 1.5B `[C]`, with five levers and no registered prediction for their *combined* effect. Row 18 schedules "ARMED" — P-6 says a gate arms on its first PASS, which cannot be dated. Contradicts the 2026-07-05 concession | target set from ambition; P-6 inherited in §2 and violated in §11 | D-2; T-7 REPORTING + self-ratchet → §5 T-3 |
| F-5 | S1 | `intel` and `mini` assigned CI lanes (B-W1, B-S1/2, B-M1..4) while the four-host CI-home escalation is open ⛔ (APR-QUALITY-001 §9.1). `lambda-labs` is "not a CI runner, do not revive" in the dogfood skill yet `cuda-nightly.yml` runs on it per the report `[A]` | assignment substituted for decision | D-3; S0-5 resolves the lambda-runner conflict → §3 |
| F-6 | S2 | B-X case table has five rows and is a written list. Missing: `(build=none, ask=cuda)`, `(build=none, ask=wgpu)`, and every `metal` row once B-M1 adds it to `BACKEND_VALUES`. A written surface list is the defect class the dogfood skill exists to kill (36/77/103/111) | table authored, not derived | Case table = `build × host-device × request`, generated from the built binary and the registry → §5 R-0 |
| F-7 | S2 | §6.7 renames sit in a perf spec and block nothing; the cost that sequences them (`use <old>::` site count) is UNMEASURED by the report's own admission; downstreams `apr-cookbook`, `qwen-train-canary`, `pmat` (depends on `aprender-*`, `batuta-common`) break on `trueno`→`aprender_compute` | scope by adjacency | Guard (G-1) and the count (G-3) in 0.66; all renames 0.67 → §6 |
| F-8 | S2 | Statistics: (a) τ_loss "declared per dataset" with no derivation — an invented threshold; (b) W5 "20 replicates" has no basis; (c) W-A "≥0.85 alone, ≥1.0 after W-B" assumes lever independence, which master §11 refuses; (d) T-0 is 20 training runs of a 7B with no time budget and a gx10 window schedule that puts it fourth behind rows due later | instrument-first rule stated, not applied | τ_loss from Unsloth-vs-Unsloth seed variance (T-1); W5 replicate count from measured CV (R-4); W-A prediction marked `[U]` joint; T-0 budget with `basis=` → §5, §7 |
| F-9 | S3 | Cites PP-LLAMA-001 v3.1 §12 row 22 (top-2 logprob margin on the wire). **Resolved by S0-1 (v1.6):** the tracked `docs/specifications/PP-LLAMA-001-MASTER.md` is v3.1 with row 22 at line 364; v1.5's "v3.0 in hand, no row 22" read an untracked `-v3.md` copy | version drift between the review's explorer checkout and the tree | none — W-H's instrument is specified; §10 row corrected |
| F-10 | S2 | gx10 window order (§8: W-G, W-B, W-E, T-0, T-1, W-H) contradicts expiries: T-0 due 09-26 is scheduled after W-E due 10-10 | DAG slack computed on dates, not on the physical resource | One gx10 queue with WIP=1, ordered by expiry → §2 |
| F-11 | S3 | Arithmetic checked `[V]`: ceilings 215/58 tok/s ✓; 74 %/48 % ✓; 9,476 MiB ✓; 0.13× ✓; KV 448 MiB/slot ✓ (28×4×128×2×4096×4 B); 3.4 ms ✓ (inputs for 215/58, 74 %/48 %, 9,476 MiB, 3.4 ms: receipt evidence/parity-http/findings.json; 0.13× = 2,101/16,118, both inputs (UNRECEIPTED [C] — provenance only: evidence/reports/0.66-parity-report-provenance.json)). `max_batch ≥ 22` has no `basis=` | — | `basis=` added or `[U]` → §7 |
| F-12 | S1 | No row has an acceptance **command**; falsifiers are sentences. paiml-implement P1 requires `A_i` as a command | report written as an audit, not a plan | Every card carries `A:` → §5 |
| F-13 | S3 | `[X]` redefined locally ("from a receipt as written") — collides with the fleet mark `[X]` = third-party excluded | local convention | `[A]` here; marks uniform across specs |
| F-14 | S2 | qwen-coder-deploy bar `agg(4) ≥ 3 × dec(1)`: the comparator scores `484.7 / 171.5 = 2.83×` `[C]` on the same cell and day. A bar llama.cpp fails is not a parity bar; gating it at 0.67 would fail the comparator | prior art adopted unread against the comparator column | REPORTED in 0.66; re-derive the bar from the comparator's own scaling before any gating → §8 |
| F-15 | S3 | "Ollama matches llama.cpp within noise" is supported by Ollama's own drift series (300.7…313.5), not by a paired same-day comparison | column confusion | B-M3's re-derivation rule is correct; the lambda premise is `[U]` and is measured in R-4 (W5 runs both) |
| F-16 | S2 | §6.6 makes a hosted MCP server (`api.copilot.nsight.ngc.nvidia.com`) the oracle for every NVIDIA API statement — a SaaS dependency in a gate/doc path (doctrine: zero external SaaS in the gate path) | convenience became the oracle | Vendor the pinned CUDA Driver/Runtime API docs under `evidence/cuda-docs/` with sha256; cite by artifact; MCP is a lookup aid → §5 D-1 |
| F-17 | S2 | W-I changes the crates.io install path (`default = ["cli"]`, `cuda` opt-in) and never names the clean-room hard gate | publish path treated as a feature | `make -C machines/clean-room clean-room-p1` in `A` → §5 R-2 |
| F-18 | S1 | "No AMD host in the fleet" `[A]` is likely false: `intel` is the Mac Pro 2019 chassis (Xeon W-3245; guide §3) running Linux (systemd, ufw, Docker) — that chassis ships an AMD Radeon Pro. If present, the "x86 + Vulkan" lane *is* AMD via RADV, B-A1's acquisition anchor is wrong, and B-W1's software-rasteriser falsifier is the wrong check (the right one is `deviceType == DISCRETE_GPU && vendorID == 0x1002`) | `reference_compute_host_topology` memory never derived from `lspci`; stale-numeric class | S0-3 → §3; D-6 |
| F-19 | S2 | B-X's mutation ("restore the `any()`") is fine; the rename guard's mutation ("re-introduce an old `[lib] name`") is **vacuous at landing** — all 51 are old names and allow-listed, so nothing changes | mutation written for the end state, run at the start state | Mutation = a non-allow-listed manifest with a non-conformant name; second mutation = delete one allow-list line → §5 G-1 |
| F-20 | S3 | Expiry slack claims re-checked `[C]`: min 6 days holds on the DAG as dated (31→35, 14/15→18). Not a finding on its own; superseded by F-10 (physical contention) | — | — |
| F-21 | S3 | W-D "reserve sized from the measured peak" needs `GET /v1/effective-config` (`vram_peak`, master row 6) — not in W-D's blockers | join miss | W-D (0.67) blocked by master row 6 → §6 |
| F-22 | S1 | No WIP limit anywhere; every host carries 3–6 concurrent rows in the report | heijunka absent | §2 WIP caps with basis |
| **F-25** | **S1** | **Root dysfunction is design, not configuration** (five whys, 2026-09-05). (1) published binary runs CPU because `default = ["cli"]`; (2) because hardware selection is bound to the *build* (a Cargo feature), so one binary had to serve every installer; (3) because runtime discovery exists only at the kernel layer (`aprender-gpu/src/driver/sys.rs` dlopens `libcuda.so.1` `[A]`) and never became a product stage — `accel.rs:28` answers `--gpu` with `cfg!`, a build fact, not a device it found; (4) because every backend arrived as its own feature + kernel family (PTX, cuBLAS, wgpu-f32, 13 MSL, wasm SIMD) chosen by `cfg!` and env knobs — there is no backend **registry**; `gpu/backend.rs:10` carries it as a comment ("future: Metal, HIP"); (5) because the gate that would have forced a registry — install the *published* artifact, ask for GPU, read the backend back — was not a required check until 2026-09-05. The report's B-X and W-I both patch why-1/why-2 and leave why-3/why-4 standing; #2841 (`mini` declared `metal` with no path) is the class recurring | producer was the gate; no artifact-install gate ⇒ design drift invisible for two releases | **R-0** registry + refusal as ticket #1 of 0.66; R-1 folded into it; D-9 default-feature flip conditional on S0-14; C11; B-G1 clauses 2/3/5 derived from the registry → §5 |
| **F-26** | **S1** | **"Contract in the same PR" is theater at HEAD.** PV-IMPROVE-001 measured at `b1a6324b8` `[A]`: `pv lint` PASS with 0 errors / 1,082 warnings and a score threshold of **0.00**; 500 of the 1,258 top-level contracts self-exempt via `registry: true` (S0-18: 501 grep hits, one a comment; v1.5 said 481); 2,474 of 2,704 `test:` fields are prose; `#[contract]` at all 10 real sites names a YAML that does not exist and `option_env!` injects nothing (#2699); 1,991 generated macro arms are `() => {{}}` and 329 of 688 call sites invoke one; Lean credit 181 declared / 8 discharged; Kani 1,257 declared / 47 exist / 0 ever run. So v1.0–v1.2 of this spec required a contract per card and offered a check that cannot fail | the contract system maximised a declared score (README count, kaizen penetration) and every enforcement layer fails open for the crates.io stranger (PV-IMPROVE §3) | Contract discipline block at the top of §5; PVI phase 0 + 1.1/1.2 in 0.66 (Track P); C12; every card's `pv` command runs through `scripts/pv_bin.sh`, never the `pv` on PATH |
| **F-27** | **S1** | **The artifact of record is wrong.** #2869 `[A]`: the only documented install is `cargo install aprender`; `binary-release.yml` fires on `release: published` but attaches only `pv-*`; `nightly.yml` builds `apr` for 5 targets and attaches them only to the `nightly` prerelease; `gh release view v0.65.2` shows no `apr-*` asset. Every user compiles the workspace on their own toolchain, so "the published artifact" is whatever their `cargo` produced — which is why yesterday's pre-publish `VERDICT PASS` and post-publish 0.59× (evidence/parity/LEDGER.md row 7, decode 0.5945 / prefill 0.0053 on the default-feature install; row 8, the `--features cuda` re-install, 0.6919 / 0.1777) measured different binaries and neither was the thing a user runs | no release asset ⇒ no artifact identity ⇒ no gate can bind to it | R-5 (assets on tagged releases, gated before promotion), R-6 (install script that verifies and ends by printing the registry), R-7 (README order); C4 re-based on the release asset; C13 |
| **F-28** | **S1** | **The required check is unreadable and non-strict** (`pmat comply check` at HEAD, 2026-09-05 `[V]`; branch-protection API read `[V]`). CB-2100: the required context `ci / gate` resolves into `paiml/.github/.github/workflows/sovereign-ci.yml@main`, whose steps this repository cannot read, so 9 severity=error rules (CB-050, CB-060, CB-1100, CB-124, CB-200, CB-2100, CB-2101, CB-2102, CB-302) are unreachable from any required check, and `workspace-test` — read in full — gates nothing in the CB roster. CB-1700: `required_status_checks.strict=false` (a stale branch can merge) and no `required_pull_request_reviews` block; `allow_force_pushes` and `allow_deletions` already read `false`. CB-1701: no required status check is known to run a blocking `cargo deny check` over advisories/bans/licenses/sources. CB-1658: ten derivation-completeness violations — `contracts/work/GH-663..672.cot.yaml` missing while each ticket's `cot-digest.json` exists (`[V]` in the main checkout, which has `.pmat-work/`; a worktree without it reads "No .pmat-work directory" and the rule goes silent), colliding with PVI-3.1 (PVI-D-2). #2830: `perf_gate.sh` returns `VERDICT PASS` on a c=1-only receipt whose `arm_a_scaling` emitted nothing — a gate that passes on silence | the gate the tag depends on lives in another repository, is not strict, and can be satisfied by an empty arm; nothing this repo owns can be *shown* to run through it | **S0-23**, **C0**; tickets under #2873: strict protection with force-push/deletions kept off (the `strict=true` PATCH is pre-authorized, S0-23); a cargo-deny job defined in this repo and named in the required contexts; `sovereign-ci.yml` pinned by sha and emitting a comply manifest pmat can read, or its gate steps vendored into `ci.yml`; #2830 polarity (an arm that emits nothing is REPORT/FAIL, never PASS) |

What the report gets right and this spec keeps verbatim: measured baseline (§1.1–1.4), the mechanism map
(§4 G1–G8), the "correct before fast" ordering, the per-backend refusal (B-X), owned-cell-not-silence for
missing hardware, the crate-rename migration rule (guard → shim → one PR per crate), and every kill-if
falsifier, which become `A_i` mutations below.

---

## §2 Scope (pending D-1)

**0.66 tag target:** 2026-10-23 `[A]`. **Cycle:** 49 days `[C]`.

| lane | contents | why here |
|---|---|---|
| **0.66 — instrumented and honest** | master §12 instrument chain (0a–0e, 1, 3, 6, 7, 9, 10, 12, 14) → gx10 shakedown (15) → lambda reference cell (18); **R-0 backend registry + runtime discovery + refusal-by-enumeration (ticket #1; absorbs R-1 B-X)**; R-2 W-I + D-9 defaults + clean-room; R-3 T-6 banner; R-4 W5 cold; **R-5/R-6/R-7 release assets + install script + README (#2869)**; **P-0 PVI phase 0, P-1 PVI 1.1/1.2 (D-11)**; S-1 W-A; S-2 W-C; S-3 GEMV microbench (bench only); T-0h harness, T-0 four WT receipts, T-1 τ_loss, T-3 gate REPORTING; B-A1 (re-shaped by S0-3); B-G1 scorecard gate; G-1 rename guard; G-2 D-5 ticket; G-3 downstream count; D-1 CUDA doc (non-blocking) | every row is either an instrument, a refusal, a guard, a measurement, or a lambda-only speed row already dated in the master — plus one design row, R-0, because F-25 shows the refusal cannot be made correct without it |
| **0.67 — speed and lanes** | PVI phases 1.3–4.3 under #2556; W-B kernel, W-D, W-E, W-G, W-H; T-1..T-5 (training levers), T-7 arming; B-W1..B-W5, B-M1..B-M4, B-S1..B-S4, B-A2; §6.7 renames (51 PRs); W-F resident attach; qwen-coder-deploy bar gating | each needs a conformant receipt or a decision (D-2, D-3) that does not exist yet |
| **removed** | nothing. Rows move; none is deleted | PP-16 / "owned cell, not silence" applies to obligations too |

**WIP caps (heijunka).** Per host, ≤ 1 speed row in flight (basis: master §9 "scheduler and kernel work are
sequenced, never concurrent on one host, because concurrent changes confound every A/B"). gx10 is one queue,
ordered by expiry: master 15 (shakedown) → T-0 → S-3's gx10 leg → master 21 (0.67). `intel` carries no new
lane until D-3. Merge queue: one workspace-test at a time (`feedback_two_concurrent_workspace_tests_starve_the_box`).

---

## §3 Step-0 — discovery (P0, read-only, before any `pmat work add` in §5)

One ticket: `pmat work add "PP-066 S0: discovery ledger at HEAD" --description "Falsify the twenty-three premises the 0.66 plan depends on; emit docs/audits/pp-066-s0-ledger.md with a verdict per premise"`. Abort on vacuity: an empty answer is a finding, not a blank.

| id | premise to falsify | command (on the named host) | flips |
|---|---|---|---|
| S0-1 | master is **committed** on `main` (review segment 2 says it is uncommitted at the explorer's checkout — `[U]` which tree), is v3.1, and has §12 row 22 (logprob margin) | `git ls-files docs/specifications/PP-LLAMA-001-MASTER.md`; `grep -n '^| \*\*22\*\*' docs/specifications/PP-LLAMA-001-MASTER.md; grep -n '^| 3\.[01]' docs/specifications/PP-LLAMA-001-MASTER.md` (the row id is bold in the master; v1.5's unbolded pattern printed nothing) | if uncommitted: **I-00** (commit the master) becomes ticket #1 — PP-29's `spec_conformance.sh` (master row 0e) cannot run against a file that is not in the tree; W-H instrument (0.67); F-9 |
| S0-2 | master rows 0a–0e, 1, 3, 6, 7, 9, 10, 12, 14 landed or not (0a expires **2026-09-12**, 0b/0c/0e/1/3 **09-19**) | the master's §12 status column (`grep -nE '^\| \*?\*?(0a|0b|0c|0d|0e|1|3|6|7|9|10|12|14)\*?\*? \|' docs/specifications/PP-LLAMA-001-MASTER.md`; `pmat work list` has no `--status all`, and no master row is a pmat ticket — S0-2); `test -x scripts/spec_conformance.sh`; `grep -n 'gpu-layers all' scripts/llama_pin.toml` | §5 I-track: which cards are open |
| S0-3 | `intel` has no GPU (report: "no AMD host") | on intel: `lspci -nn \| grep -Ei 'vga\|3d\|display'`; `vulkaninfo --summary 2>/dev/null \| grep -E 'deviceName\|deviceType\|vendorID\|driverName'` | F-18; B-A1's anchor; B-W1's falsifier; D-6 |
| S0-4 | `intel` wgpu adapter (if S0-3 finds a device) is hardware, not lavapipe | on intel: `cargo run -p apr-cli --features wgpu -- serve run --backend wgpu --list-adapters` (`[U]` flag exists; else the B-W1 job adds it) | B-W1 shape |
| S0-5 | which hosts are live self-hosted runners; is lambda a runner (skill says retired 2026-05-10; report says `cuda-nightly.yml` runs on it) | `gh api repos/paiml/aprender/actions/runners --jq '.runners[] \| [.name,.status,(.labels[].name)] \| @tsv'`; `grep -n 'runs-on' .github/workflows/cuda-nightly.yml` | F-5; D-3 inputs |
| S0-6 | `perf-matrix.yaml` cell state as the report tabulates it (§1.5 A) | `python3 -c "import yaml,sys;d=yaml.safe_load(open('scripts/perf-matrix.yaml'));print({h:(v.get('compute_class'),v.get('status')) for h,v in d['hosts'].items()})"` | B-A1, B-M2 preconditions |
| S0-7 | refusal is per-build (`accel.rs:28`, `serve/mod.rs:236` use `cfg!(any(…))`; `dispatch.rs:173` is the one per-backend check; `BACKEND_VALUES` has 3 values) | `pmat query --regex 'cfg!\(any\(feature = "cuda"' --path crates/apr-cli/src`; `pmat query --literal 'BACKEND_VALUES' --path crates/apr-cli/src` | R-0 case table size |
| S0-8 | existing tickets for W-*/T-*/B-* already minted (avoid duplicates) | `pmat work list \| grep -Ei 'PP-\|PERF-\|parity\|unsloth\|wgpu\|metal\|wasm\|rename'` | §5 ids |
| S0-9 | the training baseline reproduces: canary README summary says 2.5×, its table says 0.13× — both figures (UNRECEIPTED [C] — provenance only: evidence/reports/0.66-parity-report-provenance.json) | `cat ../qwen-train-canary/README.md \| grep -n 'tok/s'` (sibling checkout); also `git ls-files crates/aprender-train-canary` — the canary is an **excluded in-tree crate** at `587ad0797` (root `Cargo.toml:109-114` `[A]` review MAJ-08), so which copy is live is itself a premise | T-0 premise; README defect ticket if confirmed |
| S0-10 | in-workspace `use trueno::` sites resolve through **dependency-key renames** (`trueno = { package = "aprender-compute", … }`), not through `[lib] name` — if so, a `[lib]` rename alone breaks nothing in-workspace and the real rename is dep-key + feature-string + lib, three layers | `git grep -c 'package = "aprender-' -- '*/Cargo.toml' Cargo.toml`; `git grep -nE '"(trueno\|realizar\|entrenar\|batuta\|pacha\|alimentar)(/\|")' -- '*/Cargo.toml'` | G-1 rule set; 0.67 rename cost |
| S0-11 | `apr finetune` silently clamps `--max-seq-len` to 512 — **partially falsified (S0-2026-09-05)**: the instruct path honours the flag since #2247 (`finetune.rs:322-338`); only the wgpu pipeline still hardcodes `512` at `finetune.rs:717` (v1.5 cited `:317,718` from the review) — a silent-pass defect that would make T-0's apr arm run a different config than its receipt claims | `pmat query --literal 'max_seq_len' --path crates/apr-cli/src/commands/finetune.rs` | T-2 (new card) precedes T-0 |
| S0-12 | `crates/aprender-gpu/src/backend/metal_shaders.rs` holds 13 MSL kernels and `MetalBackend` via `manzana::metal` (`[A]` review MIN-05; the report says "no Metal-specific kernel or backend module") — does it build and enumerate a device on `mini`? **FALSIFIED (2026-09-05):** no `metal` feature, no dispatcher, no `manzana`; the MSL are source strings and `MetalBackend` reports unavailable on mini itself | on mini: `cargo test -p aprender-gpu -- metal` and `--features wgpu -- metal` (there is **no** `metal` feature — S0-12) | B-M1 shape (0.67): **falsified** — `metal_shaders.rs` holds MSL source strings only, `MetalBackend` is a placeholder whose own test asserts it is unavailable, and no `manzana` dependency exists; Apple GPUs are reached only through wgpu's Metal backend |
| S0-13 | `Q4K_GEMV_SHADER` exists in `crates/aprender-compute/src/backends/gpu/shaders/basic_ops.rs` (`[A]` review §2.3) while (per the review) the serve wgpu path dequantizes everything to f32 and `OwnedQuantizedModelWgpu` is a stub — **S0-13 (2026-09-05):** `Q4K_GEMV_SHADER` is already consumed by compute (`wgsl_forward.rs:424`); the stub is the MoE forward (`gguf/wgpu_backend/mod.rs:156-209`, "M-GPU-MOE-2.0 stub"); `crates/aprender-serve/src/api/handlers.rs` does not exist (the handlers are `ollama_handlers.rs` / `openai_handlers.rs`) and no serve-side dequant-to-f32 site was found — B-W0 names its real site before it opens | `pmat query --literal 'Q4K_GEMV_SHADER' --path crates`; `pmat query --literal 'UnsupportedOperation' --path crates/aprender-serve/src/gguf/wgpu_backend` | B-W0 (0.67) is a wiring job, not a shader job |
| S0-14 | the `cuda` feature builds on hosts with **no CUDA toolkit** (driver + cuBLAS are dlopened, so it should) | on intel and mini: `cargo build -p apr-cli --features cuda --release 2>&1 \| tail -3; echo exit=$?`; on the built binary: `ldd target/release/apr \| grep -ci 'cuda\|cublas'` (Linux) / `otool -L … \| grep -ci cuda` (macOS) → expected **0** | **D-9**: cuda in defaults is viable iff this passes on all four hosts; R-2's shape |
| S0-15 | what the `cuda` feature costs: clean build wall time and binary size, `--features cli` vs `--features cli,cuda`, on intel (the clean-room runner) | `time cargo build -p aprender --release --no-default-features --features cli`; same with `cli,cuda`; `stat -c %s target/release/apr` for each (`cli` is the root facade's feature; `apr-cli` has none — S0-15) | D-9's REPORTED cost line with `basis=` |
| S0-16 | `nightly.yml`'s 5-target matrix: which features per target, which runner builds it (GH-hosted or clean-room), whether the aarch64-linux artifact carries `cuda` today | `grep -nE 'features\|runs-on\|target:' .github/workflows/nightly.yml .github/workflows/binary-release.yml`; `gh release view nightly --json assets --jq '.assets[].name'` | D-10; R-5 is an extension of an existing matrix, not a new one |
| S0-17 | the 0.65.2 commit: yesterday's receipts say `8e1e9ad40`, PV-IMPROVE-001 says `b1a6324b8` | `git rev-parse v0.65.2^{commit}; git log --oneline 8e1e9ad40..b1a6324b8` | the S0 discovery baseline is the **tag** commit; the other is post-tag `main` |
| S0-18 | PV-IMPROVE-001's headline numbers reproduce with a `pv` built from the tree (never the one on PATH) | `scripts/pv_bin.sh`; `pv lint contracts/ \| tail -3` (expect 0 err / 1,082 warn / PASS); `grep -c 'registry: true' contracts/*.yaml` (expect 481) | F-26 is `[A]` until this runs |
| S0-19 | what PP-21 receipt signing uses (minisign, `ssh-keygen -Y`, other) and where the public key lives | `pmat query --literal 'PP-21' --path scripts`; `grep -rn 'minisign\|ssh-keygen -Y\|signify' scripts/ .github/workflows/` | **Falsified (S0-19):** PP-21 uses per-host symmetric keys (`perf_receipt_sign.sh`, `openssl rand -hex 32`), pr-review uses minisign with a committed public key. R-6 verifies release assets with the **public-key** scheme (minisign, `.github/pr-review.pub` class); no **third** signing scheme |
| S0-20 | which CUDA libraries apr touches and how: driver API only via `libcuda.so.1` dlopen (`driver/sys.rs` `[A]`) — or does any path dlopen/link **cudart** (`libcudart.so.N`, the version-pinned runtime that produces llama.cpp's driver-mismatch aborts)? And cuBLAS: `libcublas.so.<major>` name resolved at runtime, which majors accepted | `pmat query --regex 'libcudart\|cudart' --path crates`; `pmat query --regex 'libcublas[^"]*' --path crates/aprender-gpu/src` | REG-2, REG-5: the registry reports driver and cuBLAS as separate capabilities |
| S0-21 | what `cuMemGetInfo` reports on the two unified-memory hosts (gx10 GB10, mini M4 — the latter via wgpu-Metal adapter limits): total, free, and whether "free" moves when host RAM is consumed | on gx10: `apr devices --json` (post R-0) beside `free -g` under a 40 GB `stress-ng --vm` load; on mini: adapter `limits.max_buffer_size` beside `vm_stat` | REG-6 `mem_kind: Unified` and the reserve arithmetic (report §4 G3) need a measured basis on unified hosts |
| S0-22 | multi-GPU and mixed-vendor facts on the fleet: device counts per host, and whether `intel` presents both an AMD display device and a CPU-only wgpu adapter | `nvidia-smi -L` on lambda/gx10; `vulkaninfo --summary` on intel (S0-3) | REG-9/REG-10 fixtures use real hosts where possible |
| S0-23 | the required check is strict and readable (F-28): CB-1700/1701/2100 fail at HEAD; `required_status_checks.strict` is `false`; force pushes and deletions are already off | `pmat comply check \| grep -E 'CB-(1700\|1701\|2100)'`; `gh api repos/paiml/aprender/branches/main/protection --jq '{strict:.required_status_checks.strict,contexts:.required_status_checks.contexts,force:.allow_force_pushes.enabled,del:.allow_deletions.enabled}'` | C0. If `strict` reads `false`: `gh api -X PATCH repos/paiml/aprender/branches/main/protection/required_status_checks -F strict=true` is pre-authorized (epic #2873) and is recorded in the ledger with the before/after read. Force-push/deletion flags, the cargo-deny job, the comply manifest and #2830 are tickets under #2873 |

**Copying commands out of these tables:** a `\|` inside a command cell is markdown-table escaping for `|`; the DAG (`docs/specifications/pp-066-dag.yaml`, `A:` fields) carries every command unescaped — copy from there. **Output:** `docs/audits/pp-066-s0-ledger.md`, one row per premise, verdict ∈ {CONFIRMED, FALSIFIED, UNREACHABLE}
with the command output pasted. FALSIFIED on S0-1, S0-3, S0-5 or S0-10 re-shapes §5 before the first code ticket opens; S0-23 reading `strict=false` triggers the pre-authorized flip and opens the C0 tickets.

---

## §4 0.66 release criterion (what the tag means)

The tag is cut when **all** hold; each is a command that must exit 0 on the release commit. Nothing arms by date. **C0 is credited first — one reading, stated so it cannot be read two ways (v1.6):** C0 is a *precondition of crediting*, not of *working*. Every other criterion's command may run at any time and its rows may be implemented in any order the DAG allows; but a C-row's result is written as `[U]` — never as PASS — until C0 holds, because the check that would credit it cannot yet be shown to run. Nothing in this sentence sequences a ticket behind C0; the DAG's `TAG-0.66.0` row, blocked by C0-1..C0-4, is where C0 precedes the cut structurally. The plan quorum of 2026-09-05 read the v1.5 sentence as temporal precedence; this is the falsifier of that reading.

| # | criterion | check |
|---|---|---|
| **C0** | **The required check is readable and strict** (F-28, S0-23): CB-1700, CB-1701 and CB-2100 pass in `pmat comply check`; #2830's gate no longer returns PASS on an empty scaling arm; the nine rules F-28 lists are shown reachable from a **named** required context. No other C-criterion is credited until C0 holds | `pmat comply check \| grep -E 'CB-(1700\|1701\|2100)'` prints no ✗; `gh api repos/paiml/aprender/branches/main/protection --jq .required_status_checks.strict` prints `true`; the #2830 ticket's `perf_gate.sh --selftest` row (c=1-only receipt ⇒ not PASS) is GREEN at HEAD and was RED on the mutation in its PR body; CB-2100's output names each of the nine rules under a required context |
| C1 | `evidence/parity/LEDGER.md` carries a **CONFORMANT** lambda W1 row (master row 18) and a gx10 shakedown row (row 15); per-(band, metric) `armed_by` set **where P-5 passes**, remaining metrics REPORTING with their ratio printed | `scripts/perf_gate.sh --phase release` receipt validates; `[ "$(grep -c CONFORMANT evidence/parity/LEDGER.md)" -ge 2 ]` |
| C2 | No `perf-matrix.yaml` cell declares a `compute_class` no build reaches (PP-16); every non-MEASURED cell has an owner and exactly one of `expires`/`expires_after` | `scripts/check_backend_firstclass.sh --matrix-only` exit 0 |
| C3 | `--backend <x>` on every enumerated surface (`apr run`, `apr chat`, `apr serve run`, and any surface `--help` enumeration adds) is resolved **against the registry's Ready set**, refused otherwise with the registry's reason and the refusal code (D-7); case table derived from build × host-device × request, never a written list | `cargo test -p apr-cli --test backend_refusal_case_table` exit 0 on all four hosts (the host axis of the table is real, not mocked, on the dogfood run) |
| C4 | Post-publish dogfood on **all four hosts installs the release asset through the install script** (R-6) — the path a user takes, the binary a user gets — then records `apr devices --json` and the effective-config `backend`: `cuda` on lambda/gx10, `cpu` with `cuda unavailable reason=DriverNotFound` printed on intel/mini; `--gpu` with no Ready GPU exits with the refusal code (D-7). `cargo install aprender` is dogfooded on one host as the source-build path | `scripts/check_multiplatform_dogfood.sh --require-resolved-backend cuda` exit 0 on both |
| C5 | Four WT receipts exist (`evidence/train-parity/{lambda,gx10}/{apr,unsloth}.r1.json`), schema-valid, interleaved, with τ_loss derived (T-1); training gate REPORTING, ratchet seeded | `scripts/train_parity.sh --validate evidence/train-parity` exit 0 |
| C6 | Guards landed and observed RED once in CI: `check_crate_names.sh` (allow-list = 51, renames = 0), `check_backend_firstclass.sh` | both scripts exit 0 at HEAD; each has a PR with a mutation commit → RED → revert in its history |
| C7 | Zero published ratios without a receipt; `check_no_claim_literals.sh` universe includes `docs/specifications/` (master row 10) | script exit 0 |
| C8 | Clean-room p1 green before publish (guide §5.3 hard gate) | `make -C machines/clean-room clean-room-p1` exit 0 |
| C9 | Every 0.66 ticket has `docs/audits/impl-<ticket>-receipt.md` with `partial=false` | `ls docs/audits/impl-*-receipt.md \| wc -l` = ticket count; `grep -L 'partial=false'` empty |
| C11 | The registry is the only authority on backends: `apr devices --json` prints ≥ 1 `Ready` entry on every host (cpu at minimum) with `source` and `status.reason`; the dogfood `backend` value equals a registry entry; **0** `cfg!(feature = "cuda"\|"wgpu")` reads in `apr-cli` backend decisions; on lambda/gx10 the printed cuda device name equals `nvidia-smi -L` (mechanism engaged); **the failure-catalogue case table (R-0 §REG, 12 fixtures) is green and each fixture has been observed RED once** | `scripts/check_backend_registry.sh` exit 0 on all four hosts; `pmat query --regex 'cfg!\(.*feature = "(cuda\|wgpu)"' --path crates/apr-cli/src` → 0 hits outside the registry module |
| C12 | **Contract-per-card is real, not declared.** Every PP-066 contract: `kind: pattern` (in the held set), **no** `registry: true`, **no** hand-typed `verification_summary`, **no** `lean_theorem`/`kani_harnesses` entries unless the theorem/harness exists and resolves, every `falsification_tests[].test` an executable invocation that is the card's registered mutation, `pv` from `scripts/pv_bin.sh`. `pv lint` runs as its own CI step with a shrink-only warning baseline (PVI-0.6) and proof credit is discharged-only (PVI-0.3) | `scripts/pv_bin.sh lint contracts/ --binding contracts/aprender/binding.yaml --strict-test-binding` exit 0 with the PP-066 contracts in the strict set; `scripts/check_contract_test_binding.sh` baseline unchanged (13 lines, may not grow); per-contract `pv score` REPORTED in the ticket receipt (floor = PVI-2.6's ratchet when it lands) |
| C13 | The tagged release carries `apr-*` assets for the 5 targets with `.sha256` and a signature over the manifest (S0-19's mechanism); the install script installs from `/releases/latest/download/`, verifies both, and ends by printing the registry | `gh release view v0.66.0 --json assets --jq '[.assets[].name]'` lists 5 `apr-*` tarballs + checksums + signature; `curl -LsSf …/install.sh \| bash` on each host exits 0 and its last lines are `apr devices` output |
| C10 | The obligation DAG is data, not prose, and its invariants hold: 0 cycles, 0 zero-slack blocker pairs (min 6 days), no per-host queue position later than a later-expiring row, every row has an owner and exactly one expiry form | `scripts/check_dag_invariants.sh docs/specifications/pp-066-dag.yaml --min-slack-days 6` exit 0 (G-4) |

No row of §5 past its expiry without a RED (andon). **v1.6:** at HEAD `evidence/parity/derived_expiries.json` read master row 1 as expiring 2026-09-02 — the scanner took the first date in the cell, not the `Expires **2026-09-19**` marker — and derived rows 13/15/18/19 to the same day, with no RED anywhere (S0-2). DAG row I-26 fixes the parse and wires the RED; until it lands, the andon is `[U]`. A RED on the non-blocking track (D-1
CUDA doc, G-3 count) is a finding against its owner, not a hold on the tag.

---

## §5 0.66 tickets — paiml-implement units

Card fields: `pmat work add` line · repo/branch · blockers · host · expiry · `K̂` (`basis=` or `[U]`; first run
seeds `docs/audits/impl-estimates.jsonl`) · **A** acceptance command(s), exit 0 = done · **contract** (`pv`, in
the same PR; `metadata.kind: pattern` unless stated) · **mutation → RED** and what it must *not* trip
(discrimination) · **quorum** class. Owner rule as the report: `perf-gate` for receipts/cells/armed gates,
`spec-owner` for code/guards/docs/decisions.

**Contract discipline for every card (F-26).** Until PV-IMPROVE-001 phase 2 lands, `pv validate`/`pv lint` cannot reject a hollow contract, so PP-066 holds its own contracts to the phase-2 shape now and the review lane checks it: `kind: pattern`; no `registry: true`; no `verification_summary` block (it is an output of `pv`, never an input); `falsification_tests[].test` is an invocation (`cargo test -p … --test … <name>`, `scripts/…`, `pv …`) and `prediction` carries the prose; the test named is the card's **mutation → RED** and it is in the strict set of `check_contract_test_binding.sh`; no `lean_theorem` or `kani_harnesses[]` unless the referent exists at HEAD (PVI-2.1/2.3 rule applied early — a dangling name is inert text that lowers trust in the real ones); `#[contract]` attributes are **not credited** as enforcement until PVI-1.1 makes them fail closed — the card's `A` command and its CI wiring are the enforcement. Guards over contracts or obligations land as `pv` or `pmat comply` rules, bash only wires them into CI (PV-IMPROVE principle 8; applies to G-4 and D-1 below).

### §5.0 — the DAG, rendered (v1.6)

The cards below are prose; the obligation set is **data** in `docs/specifications/pp-066-dag.yaml` (G-4). The block between the markers is rendered by `python3 scripts/render_dag.py render` and `--check` refuses drift in `ci / gate`; edit the YAML, re-render, paste. Where a card and the DAG disagree, the DAG wins and the card is a defect.

<!-- dag:table:begin (rendered by scripts/render_dag.py; do not edit by hand) -->

_Rendered from `docs/specifications/pp-066-dag.yaml` (epic #2873, 100 rows). Edit the YAML, run `python3 scripts/render_dag.py render`, paste; `--check` refuses drift._

### Track I

| id | title | blockers | issues | host | expiry | owner | quorum | issue | pmat | status |
|---|---|---|---|---|---|---|---|---|---|---|
| I-1 | master row 1: land #2809; Blackwell guard on prefill_multi_prompt (gpu_profile.rs::multi_prompt_prefill_allowed); perf041 in cuda-nightly.yml with missing marker = RED; PP-26 witness in the receipt | G-11 | #2809 | gx10 | 2026-09-19 | serve | 3-lane | #2882 | PMAT-1057 | open |
| I-15 | gx10 shakedown cell: W1, n>=5 interleaved, at a commit containing 0b, 0c, 1, 6, 7, 12 -> a new LEDGER row | I-1, I-24, I-25, G-11 | #2819, #2834, #2846 | gx10 | 2026-09-26 (= I-1 + 7 d) | perf-gate | review-only | #2883 | PMAT-968 | open |
| I-18 | lambda reference cell: W1, n>=5 interleaved, both lanes, derived ladder, armed_by set for every band that PASSES | I-15, R-2, G-11 | #2817, #2818, #2832, #2816, #2815, #2844 | lambda | 2026-10-03 (= I-15 + 7 d) | perf-gate | review-only | #2884 | PMAT-969 | open |
| I-16 | W3 open-loop workload (report row 19, master row 16) | G-11 | — | lambda | 2026-10-23 | perf-gate | review-only | #2885 | PMAT-970 | open |
| I-17 | W4 token sweep (report row 19, master row 17; expiry tightened 10-23 -> 10-16 as the blocker of S-2, v1.6 amendment) | G-11 | — | lambda | 2026-10-16 | perf-gate | review-only | #2886 | PMAT-971 | open |
| I-24 | parity_block.py: a zero COMPARATOR band median is a named refusal, not a traceback (#2735) | G-11 | #2735, #2876 | any | 2026-09-19 | perf-gate | review-only | #2887 | PMAT-972 | open |
| I-25 | --workload bound to the prompt corpus: receipt.workload accepted only when corpus_sha256 equals the loaded prompts file and its _meta.corpus equals the label (#2756) | G-11 | #2756, #2876 | any | 2026-09-19 | perf-gate | review-only | #2888 | PMAT-973 | open |
| I-26 | PP-29 scanner: a §12 row expiry is the `Expires **date**` marker, never the first date in the cell; rows past expiry go RED (spec_conformance.sh and the §4 andon), and derived_expiries.json is regenerated | G-11 | — | any | 2026-09-19 | spec-owner | review-only | #2889 | PMAT-974 | open |

### Track C0

| id | title | blockers | issues | host | expiry | owner | quorum | issue | pmat | status |
|---|---|---|---|---|---|---|---|---|---|---|
| C0-1 | cargo-deny (advisories, bans, licenses, sources) as a blocking job defined in this repo and named in required_status_checks.contexts (CB-1701) | G-11 | #2806 | any | 2026-09-19 | spec-owner | review-only | #2890 | PMAT-975 | open |
| C0-2 | ci / gate readable: sovereign-ci.yml pinned by sha and emitting a comply manifest pmat can read — or its gate steps vendored into ci.yml — so the nine unreachable severity=error rules are shown reachable from a named required context (CB-2100) | G-11 | #2734, #2646 | any | 2026-09-26 | spec-owner | teamwork | #2891 | PMAT-976 | open |
| C0-3 | the ten contracts/work/GH-663..672.cot.yaml derivations exist (CB-1658), reconciled with PVI-3.1 (contracts/work -> .pmat-work) | U-1, G-11 | — | any | 2026-09-19 | spec-owner | review-only | #2892 | PMAT-977 | open |
| C0-4 | perf_gate.sh arm_a_scaling: an arm that emits nothing on a c=1-only receipt is REPORT/FAIL, never PASS; selftest rows both polarities (#2830) | G-11 | #2830, #2876 | any | 2026-09-19 | perf-gate | review-only | #2893 | PMAT-978 | open |
| C0-5 | PRQ-013 single base-owned quorum workflow (pull_request_target/merge_group): the receipt/quorum check that gates a PR is defined by the base, never by the PR head | — | #2982 | any | 2026-09-19 | spec-owner | review-only | #2982 | PMAT-1054 | complete |
| C0-6 | mutation step exceeds the job timeout (134-mutant set, #2872 receipt run): a cancelled required step records NotRun{Timeout}, never PASS, and the step fits its job | G-11 | #2983 | any | 2026-09-19 | spec-owner | review-only | #2983 | PMAT-1055 | open |
| C0-7 | receipt terminal marker + check_receipt_complete.sh: impl receipts carry status: complete\|partial in front matter, are written .tmp then mv, and a resume treats partial as not complete | — | #2984 | any | 2026-09-19 | spec-owner | review-only | #2984 | PMAT-1056 | complete |
| U-1 | pmat#1200: `pmat work cot derive` emits hollow obligations (empty statement/hypothesis for v5.0 contract.json steps; CB-1658 passes the hollow file) — fix upstream, pmat release (Noah publishes), forjar pin bump on four hosts + the runner, scripts/pmat_bin.sh bump PR, ratchet re-baseline stamped (PMAT-1061) | — | — | upstream (paiml/paiml-mcp-agent-toolkit) | 2026-09-13 | pmat | none (upstream) | #3015 | — | open |

### Track R

| id | title | blockers | issues | host | expiry | owner | quorum | issue | pmat | status |
|---|---|---|---|---|---|---|---|---|---|---|
| R-0 | [premises discharged: S0-7 CONFIRMED, S0-14 CONFIRMED] R-0a (split 2026-09-06, design quorum 3/3): BackendRegistry::discover() + apr devices [--json] + contracts/apr-devices-schema-v1.yaml + the hermetic fixture case table and MockBackend — the registry EXISTS and prints one line per backend with source and status.reason; the resolution rewiring (--backend/--gpu against the Ready set, effective-config, zero cfg! reads in decisions) is R-0b (#3002) | G-11 | #2378, #2507, #2586, #2661, #2779, #2790, #2792, #2846 | any (verified on all four) | 2026-09-19 | spec-owner | N-lane design (>=3: systems, Apple/Metal-forward, evidence) before P2 | #2904 | PMAT-989 | open |
| R-2 | [premises discharged: S0-14 CONFIRMED, S0-15 measured] W-I: cuda joins default features iff S0-14 passed on all four hosts (D-9) and only behind R-0; apr serve run --gpu refuses via the registry with one line; check_multiplatform_dogfood reads apr devices --json and the effective-config backend; compile-time and size cost REPORTED with basis= (S0-15); clean-room p1 before publish | R-0, DEC-D-9, G-11, R-0b | #2696, #2788, #2855, #2571 | all four (dogfood) | 2026-10-02 | spec-owner | 3-lane | #2905 | PMAT-990 | open |
| R-3 | T-6 honest training banner: -m lora --gpu-backend cuda refuses or trains on the GPU; the cuBLAS-backward line prints only when a cuBLAS backward ran | G-11 | — | lambda, gx10 | 2026-09-19 | spec-owner | 3-lane | #2906 | PMAT-991 | open |
| R-4 | W5 CLI wall-clock workload, cold arm: apr run / ollama run / llama-cli for a 32-token completion; replicate count from measured CV; both hosts | G-11 | — | lambda, gx10 | 2026-09-19 | perf-gate | teamwork on the workload definition; review-only on the diff | #2907 | PMAT-992 | open |
| R-5 | apr-* assets for the 5 nightly.yml targets attached to every tagged release, built per D-10; sha256 + minisign-signed manifest (S0-19: public-key scheme); release cut as prerelease; hosts pull the asset, run C4 dogfood and bench_host_receipt.sh H12 on their target; the promotion job flips --prerelease=false from four green receipts | R-0, DEC-D-10, G-11, R-2 | #2869, #2585, #2850 | build on the nightly.yml runners; dogfood on all four | 2026-10-09 | spec-owner | teamwork on the promotion protocol; review-only on the diff | #2908 | PMAT-993 | open |
| R-6 | one-line installer served at /releases/latest/download/install.sh: detect OS/arch, download the matching asset from the latest non-prerelease tag, verify .sha256 and the minisign-signed manifest (public key pinned in the script; the PP-21 symmetric keys cannot be used — S0-19), place on PATH, then run apr devices | R-5, G-11 | #2869 | all four (C4 path) | 2026-10-16 | spec-owner | 3-lane; no third-party verification service in the path | #2909 | PMAT-994 | open |
| R-7 | README leads with the installer one-liner; cargo install aprender second; the backend line generated from the registry kinds | R-6, G-11 | #2869 | any | 2026-10-23 | spec-owner | review-only | #2910 | PMAT-995 | open |
| R-0b | backend resolution reads the registry: accel.rs:28, serve/mod.rs:236 and :656 stop consulting cfg!(feature = …); --backend/--gpu/--device refused by enumeration with the registry's reason; BACKEND_VALUES widened to the five REG-11 kinds (a static clap vocabulary) with the refusal at runtime as Unavailable(NotCompiled); GET /v1/effective-config reports the resolved entry + discovered_at (split of R-0 per the design quorum 2026-09-06) | R-0, G-11, L0-1 | #2378, #2507, #2586, #2661 | any (verified on all four) | 2026-09-26 | spec-owner | 3-lane (the design was judged with R-0; this half is review-only on the diff) | #3002 | PMAT-1060 | open |
| R-8 | nightly-latest-dogfood.yml: install from latest on four hosts via the R-6 installer, apr devices --json, C14 (check_model_parity.sh --manifest), diff vs the release receipt; drift files an issue. Non-blocking for the tag, required to close the epic | R-5, G-11 | — | all four (forjar/make) | 2026-10-16 | spec-owner | review-only | #3019 | — | open |

### Track P

| id | title | blockers | issues | host | expiry | owner | quorum | issue | pmat | status |
|---|---|---|---|---|---|---|---|---|---|---|
| P-0.3 | proof credit from evidence only: l3_kani_proved/l4_lean_proved integers ignored; Kani credit = resolving #[kani::proof] (47), Lean credit = sorry-free resolving theorem (8); declared vs discharged columns (FALSIFY-PVI-003) | DEC-D-11, G-11 | #2732 | intel (CI runner) for P-0.6, else any | 2026-09-19 | spec-owner | review-only | #2911 | PMAT-996 | open |
| P-0.6 | pv lint as its own CI step with --min-score 0.48 (basis=measured mean score 0.48 Grade D at b1a6324b8, #2870 ledger — a floor at the current value, never an aspiration), shrink-only warning baseline (1,082), Gate 7 no longer skipped (FALSIFY-PVI-006) | P-0.3, DEC-D-11, G-11 | — | intel (CI runner) for P-0.6, else any | 2026-09-26 | spec-owner | review-only | #2912 | PMAT-997 | open |
| P-0.2 | derived verification_summary (#2648 b): pv validate errors on drift; the 17 known drifts fixed (FALSIFY-PVI-002) | DEC-D-11, G-11 | — | intel (CI runner) for P-0.6, else any | 2026-09-26 | spec-owner | review-only | #2913 | PMAT-998 | open |
| P-0.4 | honest headline: kernel · descriptive · fleet figures derived by pv status --headline; README checked as a floor (#2630) (FALSIFY-PVI-004) | P-0.3, DEC-D-11, G-11 | — | intel (CI runner) for P-0.6, else any | 2026-09-26 | spec-owner | teamwork | #2914 | PMAT-999 | open |
| P-0.5 | freeze the archived upstream: port the two stranded commits + the dirty manzana binding; remove the ../provable-contracts/lean fallback; ARCHIVED-UPSTREAM.md; untrack the 10 .pv/ caches (FALSIFY-PVI-005) | DEC-D-11, G-11 | — | intel (CI runner) for P-0.6, else any | 2026-09-26 | spec-owner | review-only | #2915 | PMAT-1000 | open |
| P-0.1 | one proof ladder (upstream numbering: L4 Kani, L5 Lean; PVI-D-1) in proof_status.rs and the three docs; ladder_consistency test (FALSIFY-PVI-001) | DEC-D-11, G-11 | — | intel (CI runner) for P-0.6, else any | 2026-10-03 | spec-owner | teamwork | #2916 | PMAT-1001 | open |
| P-1.1 | #[contract] fails closed (#2699 in full): generated registry, compile_error! on missing name/equation/unparseable predicate, optional is the only silent spelling and warns, lib.rs:26 fixed; trybuild case table (FALSIFY-PVI-007; expiry 10-10 -> 10-03 so P-1.2 keeps its slack, v1.6 amendment) | P-0.6, DEC-D-11 | — | intel (CI runner) for P-0.6, else any | 2026-10-03 | spec-owner | N-lane design (macro fail-closed semantics) | #2917 | PMAT-1002 | open |
| P-1.2 | repoint or author the 10 dangling #[contract] sites (compile errors once P-1.1 lands; ships in the same PR as P-1.1 by construction, carded separately so the spec card resolves) | P-1.1, DEC-D-11 | — | intel (CI runner) for P-0.6, else any | 2026-10-10 | spec-owner | review-only | #2918 | PMAT-1003 | open |

### Track S

| id | title | blockers | issues | host | expiry | owner | quorum | issue | pmat | status |
|---|---|---|---|---|---|---|---|---|---|---|
| S-3 | W-B bench: cargo bench batched Q4_K GEMV at M in {1,4,8} vs M in {16,32} cuBLAS GEMM; per-token time table; no kernel change; prediction t(M=4)/t(M=16) <= 1.3 registered before the run (basis= report §10 registered prediction, spec §7; kill if >= 1.5) | I-1, G-11 | #2694 | lambda | 2026-09-26 | perf-gate | review-only | #2919 | PMAT-1004 | open |
| S-3g | S-3 on gx10 (queue slot after T-0) | S-3, I-15, G-11 | — | gx10 | 2026-10-10 | perf-gate | review-only | #2920 | PMAT-1005 | open |
| S-1 | [premises discharged: master rows 6 and 9 LANDED per S0-2] W-A per-token host work out of the decode loop: one model.write() per step, embed on device, no per-slot try_send; AbRecord with registered prediction <= 3.5 ms/tok (basis= master §9 #5 / §12 row 19 registered prediction, spec §7) | I-1, G-11 | #2694 | lambda | 2026-10-16 | perf-gate | N-lane root cause (>=3: performance physics, systems, evidence) | #2921 | PMAT-1006 | open |
| S-2 | W-C prefill without synchronous copies: pinned pre-allocated activation/KV staging, one async copy per layer, allocation outside the request; prediction copies+allocs < 10% of CUDA API time (basis= master §9 #3 / §12 row 20 registered prediction, spec §7); prefill > 5,700 tok/s registered (basis= 0.55x of the comparator median 10,399 in evidence/parity-http/findings.json; REPORTING; nothing arms) | I-17, G-11 | #2693, #2697, #2765, #2766 | lambda | 2026-10-23 | perf-gate | N-lane root cause (>=3) | #2922 | PMAT-1007 | open |
| S-0 | speed lane on perf-solo: the beat/speed lane runs on perf-solo and its receipt names its own host (PR #2720 direction accepted under D-3; diff re-derived from HEAD, #2720 closed as prior art with prior-art/2720 kept) | G-11 | — | perf-solo | 2026-09-26 | perf-gate | review-only | #3016 | — | open |

### Track T

| id | title | blockers | issues | host | expiry | owner | quorum | issue | pmat | status |
|---|---|---|---|---|---|---|---|---|---|---|
| T-0h | training-parity harness: train_parity.sh + receipt schema in evidence/train-parity/; recipe extended to Qwen2.5-7B-Instruct; interleaved A,B,A,B; clocks locked; requested vs effective seq/mb read back from the engine (requested != effective => INVALID-CONFIG); pilot descends the config ladder; memory prediction registered | G-11 | — | lambda (dev), both (run) | 2026-09-19 | spec-owner | teamwork on the recipe; review-only on the diff | #2923 | PMAT-1008 | open |
| T-2 | apr finetune --max-seq-len honoured or refused with one line and the refusal code on every path; the effective value printed and written to the receipt; the wgpu instruct pipeline (finetune.rs:717) no longer hardcodes 512 (instruct path already honours since #2247) | G-11 | #2526 | any (CPU test) | 2026-09-19 | spec-owner | review-only | #2924 | PMAT-1009 | open |
| T-1 | tau_loss for the canary dataset from Unsloth-vs-Unsloth seed variance (seeds 42,43,44), 200 steps; tau = 3 x MAD of final_loss; written into the dataset manifest with basis=seed-variance-n3 | T-0h, G-11 | — | gx10 | 2026-09-26 | perf-gate | review-only | #2925 | PMAT-1010 | open |
| T-0 | the four WT receipts: Unsloth QLoRA and apr finetune -m qlora on Qwen2.5-7B-Instruct, both hosts, n=5 interleaved, paired at the highest ladder rung apr completes; first two training LEDGER rows | T-0h, T-1, T-2, G-11, L0-1 | — | lambda, gx10 | 2026-10-03 | perf-gate | review-only | #2926 | PMAT-1011 | open |
| T-3 | training gate REPORTING + self-ratchet: train_tok_per_sec, peak_vram, \|dloss\|<=tau as WT cells in perf-matrix.yaml; ratchet seeded at the T-0 achieved value; arms only on the first P-5 PASS, never by date (D-2 pending) | T-0, R-3, DEC-D-2, G-11 | — | both | 2026-10-17 | perf-gate | teamwork (policy artifact) | #2927 | PMAT-1012 | open |

### Track B

| id | title | blockers | issues | host | expiry | owner | quorum | issue | pmat | status |
|---|---|---|---|---|---|---|---|---|---|---|
| B-A1 | hosts.intel.wgpu cell in perf-matrix.yaml: compute_class wgpu, status UNMEASURED with owner, expiry anchored on D-3 (S0-3 found two AMD W5700X + llvmpipe on intel) | DEC-D-3, G-11 | — | any (YAML edit) | 2026-09-19 (= DEC-D-3 + 7 d) | perf-gate | review-only | #2928 | PMAT-1013 | open |
| B-G1 | check_backend_firstclass.sh: clauses 1-7 per backend computed from the tree (workflows, BACKEND_VALUES, perf-matrix.yaml, receipts, dogfood, formats, pins); prints N/7; FAIL if any doc claims a backend first-class with N<7; vacuity floor | R-0, R-2, G-11 | — | any | 2026-10-09 | spec-owner | teamwork on the clause-to-artifact mapping; review-only on the diff | #2929 | PMAT-1014 | open |

### Track G

| id | title | blockers | issues | host | expiry | owner | quorum | issue | pmat | status |
|---|---|---|---|---|---|---|---|---|---|---|
| G-7 | cargo_classify.sh wired into workspace-test: an ENV verdict (double-spawn ENOENT, vanished .rlib/.o, ENOSPC, dep-info ENOENT) fails the job with ENV: in the annotation and names no test; a CODE verdict names the test | G-11 | #2822, #2828, #2812, #2775, #2646, #2877 | any | 2026-09-19 | spec-owner | review-only | #2894 | PMAT-979 | open |
| G-6 | check_roadmap_diff_additive.sh: a PR roadmap.yaml diff consists only of added entries (id set grows, no existing entry line changes); wired beside check_roadmap_completion_is_cited.sh | C0-5 | #2874 | any | 2026-09-26 | spec-owner | review-only | #2895 | PMAT-980 | complete |
| G-5 | check_target_universe.sh: for every workspace member (cargo metadata, root ∪ crates/facades) and every cargo target under the feature sets that compile it, the set CI builds/lints/runs is derived from the workflows and compared — omission FAILs; cargo clippy --workspace --all-targets in reporting mode with a frozen per-crate shrink-only baseline; a make target whose help says CI and that no workflow invokes FAILs | G-11 | #2482, #2500, #2655, #2674, #2751, #2784, #2808, #2878 | any | 2026-09-19 | spec-owner | review-only | #2896 | PMAT-981 | open |
| G-1 | check_crate_names.sh: [lib] name == directory (- -> _) over the cargo-metadata member set (root 79 + facades 3); every other [package] manifest found by find (102 today) is a member or in scripts/crate_names_nonmembers.txt or FAIL; allow-list 51 + the D-5 row; dep-key (depkeys_remaining) and feature-string (features_remaining, 21 today) counters; fail-closed on unparseable manifests | G-11 | #2470, #2492 | any | 2026-09-26 | spec-owner | review-only | #2897 | PMAT-982 | open |
| G-8 | ratchets block regressions, not edits (class #2879): the claim-literal ratchet admits a PAIRED MOVE without a guard edit (#2856 option 1: an added coordinate whose text equals, at the comparand, a coordinate the same diff removes, count not rising; re-admission and copies stay refused; one case-table row per polarity, C7); the pre-commit hook does not re-gate files a merge brings in from main (#2750); G2.3 ungated ratchet + G2.1 touch rule let a newly-found defect be recorded (#2757); the cognitive-127 dispatch that blocks apr verify exposure is decomposed, not exempted (#2494) | G-11 | #2856, #2879, #2750, #2757, #2494 | any | 2026-09-26 | spec-owner | review-only | #2898 | PMAT-983 | open |
| G-9 | cbtop verdict polarity: status PASS / ci_result green over its own grade F (or a profiler that returned nothing) is a named FAIL, never PASS (#2730; class #2876) | G-11 | #2730, #2876 | any | 2026-09-26 | spec-owner | review-only | #2899 | PMAT-984 | open |
| G-2 | DECISION D-5: [lib] name = aprender declared by root Cargo.toml and crates/aprender-core — keep the front door or rename to aprender_core; the guard allow-list line cites the decision | — | — | any | 2026-09-19 | spec-owner | none | #2900 | PMAT-985 | complete |
| G-3 | rename cost measured at HEAD: use-site count (review said 4,097 [A]) by the committed regex, pmat query as second estimator, dep-key and feature-string counts, sibling checkouts, crates.io reverse deps -> docs/audits/crate-rename-cost.md with basis= | G-11 | — | intel | 2026-10-03 | spec-owner | none | #2901 | PMAT-986 | open |
| G-4 | the obligation DAG as data with invariants in CI: scripts/check_dag_invariants.sh + scripts/lib/dag_invariants.py over docs/specifications/pp-066-dag.yaml (0 cycles, >=6 days slack blocker->blockee, per-host queue ordered by expiry, owner present, exactly one expiry form, rows past expiry listed); scripts/render_dag.py renders §5/§6 tables byte-identical; pmat rule owed upstream [U] | — | — | any | 2026-09-19 | spec-owner | review-only | #2902 | PMAT-987 | complete |
| G-10 | check_hardcoded_paths.sh --full-if-capable arms itself at pmat >= 3.32.0 and the fleet runs 3.31.0: under pmat 3.37.0 pristine main reports 317 shipped findings against a baseline of 277, so the day the fleet upgrades every PR goes red (found 2026-09-05 running the guard-runner-labels steps locally; the guard must name the pmat version it armed under and the baseline must carry a measured, versioned basis) | — | — | any | 2026-09-06 | spec-owner | review-only | #2999 | PMAT-1059 | open |
| G-11 | shared-file write contention: a row PR never writes pp-066-dag.yaml, roadmap.yaml, the rendered spec block or a README count line (check_row_pr_write_set.sh); DAG status DERIVED from the receipt (dag_status.py; render_dag.py; dag_invariants.py D7); README counts a ratchet (lag allowed, overstatement RED, --exact for the orchestrator); one orchestrator docs commit per merge writes the shared files | G-10 | — | any | 2026-09-12 | spec-owner | review-only | #3012 | PMAT-1062 | open |
| G-10b | analyser pin guard: scripts/check_pmat_pinned.sh — every analyser reference under scripts/ and .github/workflows/ resolves through scripts/pmat_bin.sh; scripts/pmat_unpinned_baseline.txt 281 shrink-only; wired case table then live; extends apr-pinned-analyser-ratchet-v1 (PIN-OB-005) | G-10, G-11 | — | any | 2026-09-19 | spec-owner | review-only | #3013 | PMAT-1063 | open |
| G-10c | analyser reference sweep: the 281 unpinned references under scripts/ and .github/workflows/ resolve through scripts/pmat_bin.sh (presence probes become ENV failures; dogfood/verifier_pin/ci.sh take the pin; workflow installs at the pin); baseline to 0 | G-10b, G-11 | — | any | 2026-09-26 | spec-owner | review-only | #3014 | PMAT-1064 | open |
| G-11b | one-call state (scripts/pp066_state.sh: head row, complete receipts, PR states, main reds with ticket ids, U-1 poll), the session docs commit (scripts/session_docs_commit.sh), the .pr/<row>/accept.sh convention in pp-066-plan.md, make fleet-verify ROW=<row> (forjar/make only) | G-10 | — | any | 2026-09-19 | spec-owner | review-only | #3018 | — | open |

### Track D

| id | title | blockers | issues | host | expiry | owner | quorum | issue | pmat | status |
|---|---|---|---|---|---|---|---|---|---|---|
| SPEC-1.6 | spec v1.6: the 14 defects recorded by the S0 ledger and the #2872 review; S0-3/S0-12/S0-19 consequences (D-6 yes; B-M1 from wgpu-Metal; R-6 public-key scheme, "no third scheme"); expiry moves P-0.6 -> 09-26, T-0 -> 10-03; Track P sources = #2870 until the PVI doc lands; G-4 checker home; §5/§6 tables rendered from the DAG | — | — | any | 2026-09-12 | spec-owner | review-only | #2903 | PMAT-988 | complete |
| D-2doc | qwen-train-canary README: the summary figure ("Gap vs unsloth: 2.5x") is derived from the table or deleted; the in-tree crates/aprender-train-canary/README.md carries the same table (S0-9 README-defect ticket) | G-11 | — | any | 2026-09-26 | spec-owner | review-only | #2930 | PMAT-1015 | open |
| D-1doc | [spec id D-1 in §5 Track D; keyed D-1doc here because D-1 is also a §0 decision id] docs/specifications/cuda-backend-architecture.md: PTX backend, sm_121 JIT, fused NF4 paths, cuBLAS + CUBLAS_GEMM_THRESHOLD, FP8 prefill on sm_89 (#2765), graph-capture cuBLAS bypass, select_prefill_path, CU_STREAM_DEFAULT; every API: sentence cites a pinned vendored doc artifact or carries UNVERIFIED{reason,date} | G-11 | #2765 | any | 2026-10-03 | spec-owner | teamwork (a doc) | #2931 | PMAT-1016 | open |

### Release cut

| id | title | blockers | issues | host | expiry | owner | quorum | issue | pmat | status |
|---|---|---|---|---|---|---|---|---|---|---|
| TAG-0.66.0 | the release cut: v0.66.0-rc1 as prerelease, promotion from four host receipts (R-5), crates.io cascade from the release workflow — cut only when C0..C13 hold by their own commands; C0 first | I-1, I-15, I-18, I-16, I-17, I-24, I-25, I-26, C0-1, C0-2, C0-3, C0-4, G-7, G-6, G-5, G-1, G-8, G-9, G-3, G-4, SPEC-1.6, R-0, R-2, R-3, R-4, R-5, R-6, R-7, P-0.3, P-0.6, P-0.2, P-0.4, P-0.5, P-0.1, P-1.1, P-1.2, S-3, S-3g, S-1, S-2, T-0h, T-2, T-1, T-0, T-3, B-A1, B-G1, D-2doc, D-1doc, C0-5, C0-6, C0-7, G-11, G-2, G-10, DEC-D-2, DEC-D-3, DEC-D-8, DEC-D-9, DEC-D-10, DEC-D-11, G-10b, G-10c, U-1, S-0, R-0b, L0-1, G-11b, R-8 | #2850, #2869 | clean-room, then all four | 2026-10-30 | spec-owner | release policy (R-5 teamwork) | #2932 | PMAT-1017 | open |

### Decisions

| id | title | blockers | issues | host | expiry | owner | quorum | issue | pmat | status |
|---|---|---|---|---|---|---|---|---|---|---|
| DEC-D-2 | DECISION D-2: Unsloth throughput concession not assumed revoked; T-3 ships REPORTING | — | — | any | 2026-10-10 | spec-owner | operator (STOP class for D-3, D-8) | #2933 | PMAT-1018 | complete |
| DEC-D-3 | DECISION D-3 (escalate): CI home for non-CUDA lanes — may intel host a wgpu lane and a wasm harness; may mini host nightly Metal | — | — | any | 2026-09-12 | spec-owner | operator (STOP class for D-3, D-8) | #2934 | PMAT-1019 | complete |
| DEC-D-8 | DECISION D-8 (escalate): rename sequencing for 0.67 — one PR per crate vs batched leaves | — | — | any | 2026-10-16 | spec-owner | operator (STOP class for D-3, D-8) | #2935 | PMAT-1020 | complete |
| DEC-D-9 | DECISION D-9: cuda in the published crate default features — yes iff S0-14 (held on intel/mini), R-0 merged, clean-room p1 green | — | — | any | 2026-09-19 | spec-owner | operator (STOP class for D-3, D-8) | #2936 | PMAT-1021 | complete |
| DEC-D-10 | DECISION D-10: per-target feature set of the release binaries (R-5 runs S0-14 per target) | — | — | any | 2026-10-02 | spec-owner | operator (STOP class for D-3, D-8) | #2937 | PMAT-1022 | complete |
| DEC-D-11 | DECISION D-11: PV-IMPROVE-001 phase 0 + 1.1/1.2 ride 0.66 as C12 blockers | — | — | any | 2026-09-12 | spec-owner | operator (STOP class for D-3, D-8) | #2938 | PMAT-1023 | complete |

### 0.67 lane (carried)

| id | title | blockers | issues | host | expiry | owner | quorum | issue | pmat | status |
|---|---|---|---|---|---|---|---|---|---|---|
| W-B-kernel | multi-warp Q4_K GEMV, M in {1,4,8} | S-3, I-1 | #2349, #2604, #2605, #2611, #2728, #2761, #2880 | lambda -> gx10 | 2026-12-31 | perf-gate | per card when opened | #2939 | PMAT-1024 | open |
| W-D | f16 KV + per-layer cosine witness; reserve from measured peak | S-2 | #2695 | lambda, gx10 | 2026-12-31 | perf-gate | per card when opened | #2940 | PMAT-1025 | open |
| W-E | scheduler: slots_admitted concurrent decodes under captured graphs, one graph per M-bucket or padding | S-1, W-B-kernel, I-18 | #2782 | lambda | 2026-12-31 | perf-gate | per card when opened | #2941 | PMAT-1026 | open |
| W-G | GB10: NEON Q4_K GEMV; batched prefill default on sm_121 after master 21; pre-compiled kernels (trueno#203) | W-B-kernel | #2567, #2786, #2787, #2835, #2880 | gx10 | 2026-12-31 | perf-gate | per card when opened | #2942 | PMAT-1027 | open |
| W-H | batch-invariant reduction; logprob margin on the wire (master row 22) | W-E | #2770, #2810 | both | 2026-12-31 | perf-gate | per card when opened | #2943 | PMAT-1028 | open |
| W-F-attach | apr run attaches to a running apr serve; W5 resident arm | R-4 | — | both | 2026-12-31 | perf-gate | per card when opened | #2944 | PMAT-1029 | open |
| T67-1 | pre-compiled sm_121 kernels for training (trueno#203) | T-0 | — | gx10 | 2026-12-31 | spec-owner | per card when opened | #2945 | PMAT-1030 | open |
| T67-2 | fused RMSNorm/RoPE/SwiGLU/chunked CE with their backward kernels (review 2.5) | T-0 | — | gx10 | 2026-12-31 | spec-owner | per card when opened | #2946 | PMAT-1031 | open |
| T67-3 | NF4-in-the-matmul: kernel design + registered per-kernel prediction (mma.sync + register dequant) before code; cuBLAS has no NF4 GEMM (seg-2 2.4) | T67-2 | — | gx10 | 2026-12-31 | spec-owner | per card when opened | #2947 | PMAT-1032 | open |
| T67-4 | bf16 + activation checkpointing; offload double-buffered and overlapped | T-0 | — | gx10 | 2026-12-31 | spec-owner | per card when opened | #2948 | PMAT-1033 | open |
| T67-5 | sequence packing (T-2 already covers --max-seq-len) | T-2, T-0 | — | gx10 | 2026-12-31 | spec-owner | per card when opened | #2949 | PMAT-1034 | open |
| T-7-arm | first P-5 PASS on WT arms the training gate | T-3, T67-1, T67-2, T67-3, T67-4, T67-5 | — | both | 2026-12-31 | spec-owner | per card when opened | #2950 | PMAT-1035 | open |
| B-W0 | quantized-weight upload path in serve: Q4K_GEMV_SHADER (already consumed by compute wgsl_forward.rs:424) wired into OwnedQuantizedModelWgpu; chunked to maxStorageBufferBindingSize; SUBGROUPS grant recorded; f32 dequant path only as a <=1B test arm (#2826: 0.123 tok/s on mini) | DEC-D-3, R-0 | #2826 | intel | 2026-12-31 | spec-owner | per card when opened | #2951 | PMAT-1036 | open |
| B-W1 | wgpu lane on intel: the job, adapter selected by name/type and printed (two AMD W5700X, RADV; llvmpipe present so the rasteriser falsifier is live — S0-3/S0-4) | B-W0, DEC-D-3, R-0, R-2, B-A1 | — | intel | 2026-12-31 | spec-owner | per card when opened | #2952 | PMAT-1037 | open |
| B-W2 | Vulkan comparator pin (llama.cpp Vulkan build) in llama_pin.toml for the intel lane | B-W1 | — | intel | 2026-12-31 | spec-owner | per card when opened | #2953 | PMAT-1038 | open |
| B-W3 | PP-26 witnesses on the wgpu lane (m=1 vs m=c) | B-W1, I-1 | — | intel | 2026-12-31 | spec-owner | per card when opened | #2954 | PMAT-1039 | open |
| B-W4 | W1 receipt on the wgpu lane; "wgpu loses to CPU" is a recorded outcome | B-W2, B-W3 | — | intel | 2026-12-31 | spec-owner | per card when opened | #2955 | PMAT-1040 | open |
| B-W5 | clauses 5-6 for wgpu in check_backend_firstclass.sh (dogfood + formats) | B-W4, B-G1 | — | intel | 2026-12-31 | spec-owner | per card when opened | #2956 | PMAT-1041 | open |
| B-M1 | metal as a Ready registry entry on mini via the wgpu-Metal adapter (no MSL dispatcher exists — S0-12); NA -> UNMEASURED only when the registry prints Ready on mini (#2841 by construction) | DEC-D-3, R-0 | #2841, #2779 | mini | 2026-12-31 | spec-owner | per card when opened | #2957 | PMAT-1042 | open |
| B-M2 | llama.cpp-Metal + Ollama comparator pins on mini | B-M1 | #2464, #2572 | mini | 2026-12-31 | spec-owner | per card when opened | #2958 | PMAT-1043 | open |
| B-M3 | PP-26 witnesses and the W1 receipt on mini (needs B-W0; #2826 measured the f32 wgpu path at 0.123 tok/s — UNRECEIPTED [C], provenance only: evidence/reports/0.66-parity-report-provenance.json) | B-M2, B-W0 | #2826 | mini | 2026-12-31 | spec-owner | per card when opened | #2959 | PMAT-1044 | open |
| B-M4 | clauses 5-6 for metal in check_backend_firstclass.sh | B-M3, B-G1 | — | mini | 2026-12-31 | spec-owner | per card when opened | #2972 | PMAT-1045 | open |
| B-S1 | 0.5B GGUF+APR decode under wasmtime (wasm bound computed from bytes per receipt; W1 excluded by address space) | DEC-D-3, R-0 | #2806 | CI runner | 2026-12-31 | spec-owner | per card when opened | #2973 | PMAT-1046 | open |
| B-S2 | browser harness for the wasm32 build | B-S1 | — | CI runner | 2026-12-31 | spec-owner | per card when opened | #2974 | PMAT-1047 | open |
| B-S3 | wasm size budget + self-ratchet | B-S1 | — | CI runner | 2026-12-31 | spec-owner | per card when opened | #2975 | PMAT-1048 | open |
| B-S4 | clauses 5-6 for wasm32 in check_backend_firstclass.sh (clause 4 N/A at m=1, printed) | B-S2, B-S3, B-G1 | — | CI runner | 2026-12-31 | spec-owner | per card when opened | #2976 | PMAT-1049 | open |
| B-A2 | vendor-neutral Vulkan lane receipt on AMD silicon (intel W5700X); ROCm/HIP row with its blocker | B-A1, B-W4 | — | intel | 2026-12-31 | spec-owner | per card when opened | #2977 | PMAT-1050 | open |
| PVI-1.3-4.3 | PV-IMPROVE phases 1.3-4.3 under #2556: empty macro arm, scoped codegen, PV-ENF-003, one binding registry, Kani/Lean nightly receipts, registry:true removed, prose test -> prediction, score-floor ratchet, corpus scope, downstream smoke | P-1.1, P-0.2, P-0.5 | — | any | 2026-12-31 | spec-owner | per card when opened | #2978 | PMAT-1051 | open |
| renames | §6.7: 51 [lib]/package renames phased per D-8; shim package only; dep keys and feature strings updated per PR (G-1 counters) | G-1, G-3, DEC-D-8 | #2470 | any | 2026-12-31 | spec-owner | per card when opened | #2979 | PMAT-1052 | open |
| bar-gating | qwen-coder-deploy TTFT/ITL/agg(4) bar GATED after re-derivation per F-14 | I-18, W-E | — | lambda | 2026-12-31 | perf-gate | per card when opened | #2980 | PMAT-1053 | open |

### Track L0

| id | title | blockers | issues | host | expiry | owner | quorum | issue | pmat | status |
|---|---|---|---|---|---|---|---|---|---|---|
| L0-1 | [P0] Qwen2.5-1.5B on cuda computes a different function from cpu (cosine 0.9418, \|Δlogit\|max 5.38; hidden 1536 / heads 12 / kv 2) and apr then runs cpu under --gpu: model manifest evidence/models/supported.yaml derived (never typed); RED-first horizon test (1.5B RED / 7B GREEN before any kernel edit, must-RED twin); five whys by apr parity SPC to the op, then the fix; REG-15 model admission (forced backend never downgrades; unforced prints selected: cpu (reason: parity FAILED …); effective-config parity block; SKIP_PARITY_GATE prints override:, marks receipts INVALID-CORRECTNESS, asserted unset in dogfood and ci / gate); threshold 0.98 [U] -> measured n>=5 per known-good pair with basis=; contract apr-gpu-cpu-parity-v1; dogfood P6 falsifier; C14 = scripts/check_model_parity.sh --manifest in apr-dogfood --release, C4 post-publish, R-8 nightly; every existing (1.5B, cuda) receipt relabelled INVALID-CORRECTNESS | G-10 | — | lambda, gx10 (fleet-verify) | 2026-09-19 | perf-gate | N-lane root cause (three model families recorded) | #3017 | — | open |

<!-- dag:table:end -->

### Track I — instrument chain (inherited from PP-LLAMA-001 §12; carded by this spec's DAG — master Appendix E is a landing checklist and carries no `pmat work add` lines, S0-2)

S0-2 marks each landed/open (2026-09-05: 0a–0e, 3, 6, 7, 9, 10, 12, 14 LANDED; row 1 OPEN); the open rows are carded in
`docs/specifications/pp-066-dag.yaml` (I-1, I-15, I-16, I-17, I-18) and filed as issues under the epic. They are the blockers of C1 and the report omitted them.

| master row | deliverable | expiry (master) | 0.66 role |
|---|---|---|---|
| 0a | `run_gate` obeys `$phase`; `phase_guard_*` selftests | 2026-09-12 | blocker of 7, 18 |
| 0b | `--stream`, `ignore_eos`, `completion_tokens == n_predict` fatal, `stream_mode` witness | 2026-09-19 | blocker of 4, 7, 15, 16, 17 |
| 0c | receipt signing; `kv` block | 2026-09-19 | blocker of 15 |
| 0d | `--gpu` → `--gpu-layers all` in `llama_pin.toml` | 2026-09-12 | PP-15 |
| 0e | `scripts/spec_conformance.sh` in `ci / gate` | 2026-09-19 | PP-29; blocker of 7 |
| 1 | land #2809; Blackwell guard on `prefill_multi_prompt`; `perf041` nightly, missing marker = RED | 2026-09-19 | PP-26; blocker of 13, 15, 19 |
| 3 | `/props` at the pin; per-band `llama_pin.toml` template + expiry; `-b 1` refused | 2026-09-19 | comparator contract |
| 6 | `GET /v1/effective-config`, `started_utc`, `slots_admitted`, derived ladder | 2026-10-02 | blocker of 15; W-D (0.67) |
| 7 | the JOIN: `ComparatorStatus::Measured`, `--comparator-url`, zero-GPU fixture | derived | blocker of 8, 15 |
| 9 | `AbRecord` type | 2026-09-25 | blocker of S-1, S-2 |
| 10 | `check_no_claim_literals.sh` universe widened; withdrawn tables receipted or deleted | 2026-09-25 | C7 |
| 12 | perf workflow `concurrency: perf-<host>`; pin expiry; client sha | 2026-10-02 | blocker of 15 |
| 14 | `scripts/measure_bandwidth.sh` per host | 2026-10-16 | PP-23 roofline |
| **15** | **gx10 shakedown cell**, W1, n ≥ 5 interleaved | derived (blocked by 0b, 0c, 1, 6, 7, 12) | first gx10 queue slot |
| **18** | **lambda reference cell**, W1, n ≥ 5, both lanes, arm-what-passes | derived (15 clean at `--phase merge`) | C1 |

Report row 19 (W3, W4) = master rows 16, 17, perf-gate — carried; W4 (I-17) tightened 2026-10-23 → 2026-10-16 as the blocker of S-2 (v1.6; §12 rule).

### Track R — runtime discovery, refusal, install

**R-0 · backend registry: probe → enumerate → load → print → refuse** (the why-3/why-4 fix; **ticket #1 of 0.66**; absorbs report row 20 / B-X)
- `pmat work add "R-0: BackendRegistry — enumerate cpu/cuda/wgpu at startup from the machine (dlopen libcuda, wgpu adapters, cpu always); one printed line per backend with source and status.reason; apr devices [--json]; --backend/--gpu resolved against the Ready set and refused otherwise with the registry's reason; GET /v1/effective-config reports the resolved entry; zero cfg! reads in apr-cli backend decisions" --description "Five whys 2026-09-05 (F-25): accel.rs:28 answers --gpu with a build fact. llama.cpp/Ollama answer it with a device they found (load_backend: … / inference compute library=…). aprender-gpu already dlopens libcuda.so.1 (driver/sys.rs) and cuBLAS (cublas_sys.rs) — the mechanism exists at the kernel layer and is being promoted to a product stage. Extends gpu/backend.rs, whose doc comment already names Metal and HIP as future entries"`
- aprender (`crates/apr-cli`, `crates/aprender-serve/src/gpu/backend.rs`, `crates/aprender-gpu/src/driver`) · `feat/r0-backend-registry` · blockers: S0-7, S0-14 · host: any CPU to build; verified on all four · expiry **2026-09-19** · `K̂` `[U]` (first design row; estimate after P1 plan)
- **shape (prescriptive, not a design doc — P1 plans it):** `BackendRegistry::discover() -> Vec<BackendEntry { name, device_name, vendor_id, device_type, mem_bytes, compute_class, source: CompiledIn | Dlopen(path), status: Ready | Unavailable(reason) }>`. Order: `cpu` (always Ready, with ISA); `cuda` (feature compiled? else `Unavailable(NotCompiled)`; dlopen `libcuda.so.1`/`nvcuda.dll` → `cuDeviceGetCount` → per-device entry; failure → `Unavailable(DriverNotFound(path))`); `wgpu` (adapters requested; software rasteriser → `Unavailable(SoftwareRasterizer(lavapipe|llvmpipe))`; else Ready with adapter name and backend kind). `metal` and `hip` are registered kinds with `Unavailable(NotCompiled)` today so the printed list is the same shape on every host. Resolution: `--backend x` ⇒ `x ∈ Ready` or refuse printing `status.reason`; `--gpu` ⇒ first Ready non-cpu or refuse; `--gpu-layers N` accepted only on a Ready GPU entry. Startup prints one line per entry, e.g. `backend: cuda ready device="NVIDIA GeForce RTX 4090" cc=8.9 mem=24GiB source=dlopen(/usr/lib/x86_64-linux-gnu/libcuda.so.1)`. `perf-matrix.yaml compute_class` is defined as **what this line reports**, which retires PP-16's "declare a class no build reaches" defect by construction
- **A (all four hosts, in the dogfood run):** `cargo test -p apr-cli --test registry_failure_catalogue` (FX-1..FX-14 below; each fixture has a must-RED twin committed under `tests/fixtures/registry/defective/`) · `scripts/pv_bin.sh validate contracts/apr-devices-schema-v1.yaml` and every `apr devices --json` output validates against it · `apr devices --json | jq -e '[.[]|select(.status=="ready")]|length>=1'` · `cargo test -p apr-cli --test backend_refusal_case_table` — rows generated from `{build features} × {host device facts from the registry} × {request ∈ BACKEND_VALUES}`, verdict `refuse ⇔ request ∉ Ready`, so a cuda build on a host with no `libcuda` refuses `--backend cuda` with `DriverNotFound` (a row `cfg!` could never express); vacuity floor ≥ 3 surfaces (`run`, `chat`, `serve run` — `accel.rs:114` already tests three) · `scripts/check_backend_registry.sh` (C11): printed device name on lambda/gx10 equals `nvidia-smi -L`; `GET /v1/effective-config .backend` equals the resolved entry · `pmat query --regex 'cfg!\(.*feature = "(cuda|wgpu)"' --path crates/apr-cli/src` → 0 hits outside `registry.rs` · exit code read from `CliError::FeatureDisabled.exit_code_value()` at test time (D-7)
- **contract:** `contracts/apr-backend-registry-v1.yaml` (kind: pattern) — invariants: (i) `cpu ∈ Ready` always; (ii) `resolved ∈ Ready`; (iii) every entry carries `source` and, if not Ready, a non-empty `reason`; (iv) `effective-config.backend == resolved`; (v) no build-fact read in resolution. Falsification tests: the case table; the `nvidia-smi -L` cross-check; the three-surface drift case
- **mutation → RED (three):** (i) make resolution consult `cfg!(feature = "cuda")` instead of the enumeration → the `(build=cuda, host=no-libcuda, ask=cuda)` row goes GREEN wrongly → table RED; (ii) print `Backend: CUDA` from the request instead of the entry → C11 cross-check RED on a CPU-only host; (iii) restore `cfg!(any(cuda, wgpu))` at `accel.rs:28` → `(build=wgpu, ask=cuda)` RED. Discriminates: the surface-enumeration check stays GREEN under all three (presence, not verdict); no perf receipt is produced
- **quorum:** N-lane on the design (min 3: systems, Apple/Metal-forward, evidence) **before** P2 — this is the one architecturally significant row of 0.66 and the guide's §5.1.8 applies. The three systems reviewed are llama.cpp/ggml, Ollama and llamafile (§12, prior-art register); the lanes verify §12's lessons against those projects' current source before P2 opens, because the register was assembled from a third-party synthesis (`[X]`)

**R-0 requirements — the discovery story (REG-1..REG-14).** Each carries the prior-art lesson it comes from (§12) and the fixture that falsifies it. The fixture ids are the rows of `tests/fixtures/registry/` and the case table `cargo test -p apr-cli --test registry_failure_catalogue` is part of R-0's `A`.

| id | requirement | lesson (§12) | fixture / falsifier |
|---|---|---|---|
| REG-1 | **Discovery runs in the control plane before any engine or model is touched**, is side-effect-free, and `apr devices` works with no model argument. A driver fault during discovery never takes the process down: every driver call returns `Result`, never aborts | Ollama's Go-side preemptive discovery keeps the API up when the driver is broken; llama.cpp's in-engine `ggml_backend_register` mismatch is a fatal abort | FX-1: `LD_PRELOAD` a stub `libcuda.so.1` whose every entry returns `CUDA_ERROR_UNKNOWN` → `cuda` entry `Unavailable(DriverError(code))`, exit 0 for `apr devices`, refusal code for `--gpu`, **no panic, no signal** |
| REG-2 | **Driver API only.** The `cuda` entry is Ready on `libcuda.so.1` / `nvcuda.dll` (ships with the driver, ABI-stable). apr never dlopens or links `libcudart` (S0-20). The runtime-vs-driver version mismatch that aborts llama.cpp builds cannot occur | llama.cpp pre-built binaries expect the exact `libcudart` of the build host; llamafile avoids it by JIT-compiling; apr avoids it by never needing it | FX-2: host with driver and **no** CUDA toolkit installed (intel/mini after S0-14) → `cuda ready` if a device exists, else `Unavailable(NoDevice)`; `ldd apr` shows no `cudart` |
| REG-3 | **No runtime compilation, no payload extraction.** One statically-shaped binary; PTX is generated in Rust and JIT-ed by the driver; nothing is written to `$TMPDIR` and nothing is compiled with a toolchain at run time | llamafile's `nvcc`/`hipcc`/Xcode probe (silent CPU when absent); Ollama's runner extraction to `/tmp` (quarantined by endpoint security) | FX-3: run with `$TMPDIR` read-only and `PATH=/nonexistent` → identical `apr devices` output to the normal run; `strace -f -e execve` shows no child compiler |
| REG-4 | **Per-vendor independence.** Each backend probes on its own; one vendor's failure never changes another's entry. An AMD or Intel device with no backend appears as `Unavailable(NoBackend(vendor))` — listed, not ignored | Ollama #11849: an AMD iGPU with no ROCm made the daemon fall back to CPU globally and ignore the working NVIDIA card; `OLLAMA_INTEL_GPU=true` opt-in hides Intel silicon by default | FX-4: fixture with a fake AMD PCI device + no HIP backend + a Ready cuda stub → `cuda ready` **and** `amd unavailable reason=NoBackend`; FX-4b on `intel` (S0-3): the Radeon appears as a wgpu entry or as `NoBackend`, never absent |
| REG-5 | **Capabilities are per-entry and printed**, ggml-`caps` style: `{async, unified_memory, graph_capture, cublas, dp4a, subgroups}`. cuBLAS is a **capability of the cuda entry**, not a second backend: `libcublas.so.<major>` absent ⇒ `cublas=false`, prefill dispatch takes the PTX path and prints `prefill: ptx (cublas unavailable: libcublas.so.12 not found)`. Silent path substitution is forbidden | `ggml_backend_dev_props.caps`; apr's own two-family dispatch (`select_prefill_path`, `CUBLAS_GEMM_THRESHOLD`) is today decided by env and `cfg!`, invisibly | FX-5: hide `libcublas` from the loader → `cuda ready cublas=false`, the printed prefill path changes, the golden output does not; mutation: dispatch reads `cfg!` again → fixture RED |
| REG-6 | **Memory is measured at discovery and re-measured at admission**, never cached across requests: `mem_total`, `mem_free` (`cuMemGetInfo`; adapter limits for wgpu), `mem_kind: Discrete \| Unified`, and `other_process_bytes` where NVML exposes it. On Unified hosts (gx10, mini) `mem_total` is the shared pool and the printed reserve says so | Ollama re-reads NVML per load but caches VRAM state across driver updates (desync until restart); ggml reads `memory_free` per device; llamafile caps Metal at RAM − safety buffer | FX-6: two consecutive `apr devices` calls with a 20 GB allocation held by another process between them → `mem_free` differs by ≥ 15 GB; S0-21 supplies the unified-host expectation |
| REG-7 | **The reserve is printed and derivable.** `reserve_bytes` appears in `apr devices` and `effective-config` with its `basis=` (measured peak per master row 6, or the 3.5 GB default marked `[U]` until then). A reserve that exceeds `mem_free` yields `Unavailable(ReserveExceedsFree{reserve, free})`, never a silent CPU run | `OLLAMA_GPU_OVERHEAD` misconfiguration makes Ollama believe the GPU is full and run CPU with no diagnosis | FX-7: `APR_RESERVE_BYTES=999G apr serve run --gpu` → refusal naming both numbers; without `--gpu` → cpu chosen **and** the reason line printed |
| REG-8 | **Default selection is explicit and printed; override is loud.** With no flag: first Ready non-cpu entry that fits, else cpu, and the line `selected: cpu (reason: …)` always prints. `--backend x` / `--gpu` / `--device N` force, and failure is a refusal with the reason, never a downgrade. `APR_BACKEND=` env behaves as the flag and prints `override: APR_BACKEND=…` when active | llamafile's silent fallback is only made loud by `--gpu nvidia`; `OLLAMA_LLM_LIBRARY` overrides autodetect; both are the right escape hatch with the wrong default | FX-8: `--device 7` on a one-GPU host → refusal `NoSuchDevice(7 of 1)`; `APR_BACKEND=cpu` on lambda → cpu used, override line present; mutation: drop the `selected:` line → fixture RED |
| REG-9 | **Multi-GPU: enumerate all, select one.** Every device is an entry with its index; 0.66 places the model on one device (`--device`, default 0); tensor split is **not** in 0.66 and `--tensor-split` is refused with `NotImplemented` (an owned refusal, not a missing flag) | llama.cpp splits evenly across same-vendor cards by default; Ollama isolates per-GPU silos; both are choices apr has not measured | FX-9: fixture with two cuda stub devices → two entries, model on `--device 1` reports index 1 in `effective-config`; `--tensor-split 1,1` → refusal code |
| REG-10 | **Never mix vendors in one graph.** Cross-vendor placement is structurally impossible: a graph is bound to exactly one registry entry | mixed AMD+NVIDIA in one llama.cpp process segfaults; Ollama binds to the primary driver and drops the other | FX-10: two Ready stub entries of different vendors → `--device` must name one; a request naming both → refusal |
| REG-11 | **Same output shape on every host.** `apr devices` always prints the full kind list `{cpu, cuda, wgpu, metal, hip}` — Ready entries and `Unavailable(reason)` rows alike — so the *absence* of a backend is an explicit line the user reads, and the dogfood's expectation is a fixed schema | llamafile users "unaware that the software silently abandoned GPU acceleration"; Ollama's log line `no compatible GPUs were discovered` is the right idea, buried in a log | FX-11: on the CPU-only fixture the JSON has exactly 5 kinds with 1 Ready; schema `contracts/apr-devices-schema-v1.yaml` validates every run on every host |
| REG-12 | **Discovery is re-run at every start; nothing is persisted.** No cache file, no memoised device list; `discovered_at` is in `effective-config` | Ollama's stale VRAM cache after a driver/kernel update requires `systemctl restart ollama` | FX-12: run once with a Ready cuda stub, once with the stub removed → second run shows `Unavailable(DriverNotFound)`; `strace -e openat` shows no registry cache file read |
| REG-13 | **Extensible by trait, not by feature flag.** A backend is a `Backend` impl registered at startup; a proxied backend (a VirtGPU/APIR-style forwarder) must be implementable without touching resolution code. This is a design constraint on the trait, verified by a mock backend in the test crate, not a 0.66 deliverable | ggml's VirtGPU backend implements the same registry interface and forwards enumeration across the hypervisor; the registry design made that possible | `MockBackend` in `tests/` registers, enumerates two fake devices, and resolves through the same path as cuda — with no `cfg!` |
| REG-14 | **Automatic layer split is 0.67, and says so.** 0.66 places all-or-nothing on one device; `--gpu-layers N` is accepted only as `all` (`N ≥ layers`) and otherwise refused with `NotImplemented(partial_offload → 0.67 W-D)`; the VRAM-fit arithmetic Ollama does preemptively lands with master row 6's `vram_peak` and W-D's measured reserve | Ollama's `num_gpu` math from measured VRAM vs llama.cpp's hand-tuned `-ngl`; apr will do the former once the peak is measured, not invented | FX-14: `--gpu-layers 3` → refusal naming the 0.67 row; `--gpu-layers all` ≡ default |

The printed block on `lambda` after R-0 (normative shape; values illustrative):
```
apr 0.66.0  discovery 2026-10-01T09:12:03Z
backend: cpu    ready        isa=avx512  threads=32
backend: cuda   ready        device[0]="NVIDIA GeForce RTX 4090" cc=8.9 mem=24564MiB free=23012MiB kind=discrete caps={async,graph_capture,cublas,dp4a} source=dlopen(/usr/lib/x86_64-linux-gnu/libcuda.so.1)
backend: wgpu   unavailable  reason=NotCompiled
backend: metal  unavailable  reason=NotCompiled
backend: hip    unavailable  reason=NoBackend
selected: cuda device[0]  reserve=3584MiB basis=[U] default  prefill=cublas decode=ptx
```
The same block on a CPU-only box differs in exactly two lines (`cuda … unavailable reason=DriverNotFound(libcuda.so.1)` and `selected: cpu (reason: no ready gpu)`). That diff is the whole #2696 fix, and the installer (R-6) ends by printing it.


**R-1 · B-X** — *retired into R-0.* Report row 20's case table is R-0's acceptance test; a per-backend refusal built on `cfg!` would have been the why-3 defect with a better table. The id stays so `roadmap.yaml` references resolve.

**R-2 · W-I: GPU-capable install, D-9 defaults, dogfood reads the registry** (report row 4; G8; why-1 countermeasure)
- `pmat work add "W-I: per D-9 cuda joins default features iff S0-14 passed on all four hosts (else documented --features cuda); apr serve run --gpu refuses via the registry with one line; check_multiplatform_dogfood reads apr devices --json and the effective-config backend, not the flag; compile-time and binary-size cost REPORTED with basis=; clean-room p1 before publish" --description "#2696 class. The countermeasure at why-1 is only safe once R-0 makes absence of libcuda a printed reason rather than a silence"`
- aprender · `feat/wi-gpu-install` · blockers: **R-0**, S0-14, S0-15, D-9 · host: all four (dogfood) · expiry **2026-09-26** (moved from report row 4's 09-12: blocked by R-0; recorded here per D-4 discipline) · `K̂` `[U]`
- **A:** **`make -C machines/clean-room clean-room-p1` exit 0** — the publish-path hard gate, named first · `scripts/check_multiplatform_dogfood.sh --require-resolved-backend cuda` exit 0 on lambda **and** gx10, `--require-resolved-backend cpu` exit 0 on intel and mini with the cuda entry printed as `unavailable reason=DriverNotFound` (not absent) · `apr serve run --gpu <m>` on a build/host with no Ready GPU exits with the refusal code and the reason line · `docs/audits/wi-cost.md` with S0-15's numbers and `basis=`
- **contract:** `contracts/apr-gpu-install-v1.yaml` — invariants: (i) `--gpu` with no Ready GPU refuses; (ii) dogfood `backend` is read from `apr devices --json` / effective-config, never from the invocation; (iii) if defaults include `cuda`, a host without the driver still starts and prints the unavailable line
- **mutation → RED:** make the dogfood record the request → `--require-resolved-backend` RED on intel/mini. Discriminates: R-0's table is unaffected (it tests the binary, not the dogfood script)
- **quorum:** 3-lane review-only

**R-3 · T-6 honest training banner** (report row 7)
- `pmat work add "T-6: -m lora --gpu-backend cuda refuses or trains on the GPU; the cuBLAS-backward line prints only when a cuBLAS backward ran" --description "feedback_apr_finetune_lora_cpu_only"`
- aprender (`crates/aprender-train`) · `fix/t6-honest-banner` · blockers: — · host: lambda, gx10 · expiry 2026-09-12 · `K̂` `[U]`
- **A:** `cargo test -p aprender-train --test banner_truth` — asserts the banner string is emitted iff a device-side backward kernel launched (counter from the executor), and `--gpu-backend cuda` on a CPU-only build exits with the refusal code (D-7)
- **contract:** `contracts/apr-train-banner-truth-v1.yaml`
- **mutation → RED:** print the banner unconditionally → RED on the CPU-only arm. Discriminates: no perf receipt is produced by this test
- **quorum:** 3-lane review-only

**R-4 · W5 CLI wall-clock workload, cold arm** (report row 5, G7 — measurement half; resident attach → 0.67)
- `pmat work add "W5: apr run / ollama run / llama-cli wall-clock for a 32-token completion, cold; replicate count from measured CV; both hosts" --description "Establishes the daemon gap before the resident-attach feature exists"`
- aprender · `feat/w5-cli-wallclock` · blockers: master 0b (sampler pin) · host: lambda, gx10 · expiry 2026-09-19 · `K̂` `[U]`
- **A:** `scripts/perf_w5.sh --arm cold --pilot 5` prints CV per engine; replicate count `n = ceil((1.96·CV/0.05)²)` `[C]` for a ±5 % half-width, written into the receipt with `basis=pilot`; then `scripts/perf_w5.sh --arm cold --n $n` → `evidence/w5/<host>/receipt.json`; the receipt names all three binaries by sha256 and the Ollama daemon build. Ollama-vs-llama.cpp paired delta is REPORTED here (closes F-15's `[U]`)
- **contract:** `contracts/apr-w5-wallclock-v1.yaml` — invariant: no wall-clock figure without `n` and its basis in the same receipt
- **mutation → RED:** drop `basis` from the receipt → validator RED. Discriminates: no ratio gate reads W5
- **quorum:** teamwork on the workload definition (a spec change), review-only on the diff

**R-5 · `apr` binaries on tagged releases, gated before promotion** (#2869 ask 1; closes F-27's artifact-identity gap; the pre-publish gate PMAT-964/963 lacked)
- `pmat work add "#2869 R-5: apr-* assets for the 5 nightly.yml targets attached to every tagged release; built per D-10 features; sha256 + signed manifest; release is cut as prerelease, hosts pull the asset and run the post-publish dogfood (C4) and bench_host_receipt.sh (H12) on their target, and only then is --prerelease=false set" --description "binary-release.yml is scoped to pv only [A]; nightly.yml already knows the matrix [A]. release: published fires after the fact, so the andon is the prerelease flag: promotion is the gate, not the tag"`
- aprender · `feat/r5-apr-release-assets` · blockers: S0-16, S0-19, D-10, **R-0** (the asset must print its registry), clean-room · host: build on the runner S0-16 names; dogfood on all four · expiry **2026-10-09** · `K̂` `[U]`
- **A:** **`make -C machines/clean-room clean-room-p1` exit 0** first (publish path) · cut `v0.66.0-rc1` as prerelease → `gh release view v0.66.0-rc1 --json assets --jq '[.assets[].name]'` lists `apr-{x86_64,aarch64}-unknown-linux-gnu`, `apr-{x86_64,aarch64}-apple-darwin`, `apr-x86_64-pc-windows-msvc` tarballs + `.sha256` + one signed manifest · on each host: `scripts/check_multiplatform_dogfood.sh --from-release v0.66.0-rc1` (downloads the asset for the host's target, verifies, runs `apr devices --json`, records the asset sha256 in the receipt — this is the artifact identity PP-18 needs) · `bench_host_receipt.sh` H12 on the aarch64 asset on gx10 and mini before promotion · `gh release edit v0.66.0-rc1 --prerelease=false` only after all four receipts are green; the promotion step is a workflow job that reads the receipts, not a hand command
- **contract:** `contracts/apr-release-assets-v1.yaml` — invariants: (i) `assets(tag) ⊇ 5 targets × {tarball, sha256}` + manifest signature; (ii) `prerelease=false ⇒ ∃ 4 host receipts with `asset_sha256 == manifest[target]`; (iii) H12 floor evaluated on the asset, on its target class, before promotion
- **mutation → RED (two):** (i) promote with one host receipt missing → the promotion job refuses; (ii) tamper one byte of the aarch64 tarball after signing → the host's `--from-release` verify step RED and no receipt is written. Discriminates: the crates.io publish cascade is untouched by both
- **quorum:** teamwork on the promotion protocol (a release-policy artifact), review-only on the diff

**R-6 · one-line installer that verifies and tells the truth** (#2869 ask 2)
- `pmat work add "#2869 R-6: scripts/install.sh served at /releases/latest/download/install.sh: detect OS/arch, download the matching apr-<target>.tar.gz from the latest non-prerelease tag, verify .sha256 and the minisign-signed manifest (the repository's public key, pinned in the script — PP-21's per-host receipt keys are symmetric and cannot be handed to a user, S0-19), place on PATH, then run apr devices so the user's first screen is the registry" --description "The installer is where #2696's silence would recur; its last line is 'backend: cuda unavailable reason=libcuda.so.1 not found' on a CPU box, and that is the feature"`
- aprender · `feat/r6-install-script` · blockers: R-5, S0-19 · host: all four (it is the C4 path) · expiry **2026-10-16** · `K̂` `[U]`
- **A:** `bash tests/install/cases.sh` — case table: each of the 5 targets resolves to the right asset name; a bad sha256 → non-zero and nothing on PATH; a bad signature → non-zero; a prerelease-only tag is skipped for "latest"; a host with no `curl` fails with one line; `--version <tag>` pins; the script never writes outside `$PREFIX`; `shellcheck -S error` clean; POSIX `sh` (bash 3.2 on macOS is the trap PMAT-965 found — the installer must not need bash 4) · on each host the C4 run uses this script and its stdout ends with the `apr devices` block
- **contract:** `contracts/apr-installer-v1.yaml` — invariants: (i) no binary lands on PATH without both verifications passing; (ii) the fingerprint the script checks equals the key R-5 signs with (one key, S0-19); (iii) exit 0 ⇒ `apr devices` ran
- **mutation → RED:** flip one hex digit of the pinned fingerprint → every install RED. Discriminates: R-5's asset checks stay GREEN (they verify the release, not the script)
- **quorum:** 3-lane review-only; **no third-party verification service in the path** (sovereign doctrine: no sigstore/Rekor/Fulcio round-trip at install; the key ships in the script and the repo)

**R-7 · README leads with the installer** (#2869 ask 3)
- `pmat work add "#2869 R-7: README install section: curl one-liner first, cargo install aprender second as the source-build / --features path; both guarded by check_readme_claims.sh; the README line naming the backends a build carries is generated from the registry's kinds, not typed"`
- aprender · `docs/r7-readme-install` · blockers: R-6 · host: any · expiry 2026-10-23 (with the tag) · `K̂` `[U]`
- **A:** `scripts/check_readme_claims.sh` exit 0; `check_no_claim_literals.sh` exit 0; the install section contains the exact one-liner `tests/install/cases.sh` exercises (a test greps README for it — drift = RED)
- **contract:** none (a doc; the two guards are the contract)
- **mutation → RED:** change the README one-liner's URL → the grep test RED
- **quorum:** review-only

### Track P — provable contracts: make `pv` able to say no (PV-IMPROVE-001 phase 0 + 1.1/1.2; pending D-11)

One `pmat work add` per PVI row, ids `PVI-0.1..0.6`, `PVI-1.1`, `PVI-1.2`, each carrying its `FALSIFY-PVI-nnn` gate id as the doc specifies; `docs/specifications/improve-provable-contracts.md` is **not in the tree** (issue #2870 is its tracking ticket and the only committed text), so the cards' `A`/mutation are taken from #2870's phase text and stated in the DAG rows until the document lands. What this spec adds is the ordering and the tie to C12.

| id | deliverable (PV-IMPROVE §5) | gate | blockers | expiry | why 0.66 |
|---|---|---|---|---|---|
| **P-0.3** | proof credit from evidence only: `l3_kani_proved`/`l4_lean_proved` ignored; Kani credit = resolving `#[kani::proof]` (47), Lean credit = sorry-free resolving theorem (8); *declared* vs *discharged* columns | FALSIFY-PVI-003 | — | 2026-09-19 | the honest baseline every later number is measured against; **first** |
| **P-0.6** | `pv lint` as its own CI step with `--min-score 0.48`, shrink-only warning baseline (1,082), Gate 7 no longer skipped | FALSIFY-PVI-006 | P-0.3 | 2026-09-26 (v1.6: 09-19 → 09-26, zero slack vs P-0.3; §12 rule) | C12's instrument; a PP-066 contract that adds a warning is RED |
| **P-0.2** | derived `verification_summary` (#2648 b): `pv validate` errors on drift; 17 known drifts fixed | FALSIFY-PVI-002 | — | 2026-09-26 | PP-066 contracts carry no summary block; this makes that the rule, not a convention |
| **P-0.4** | honest headline: three derived figures (kernel · descriptive · fleet) from `pv status --headline`, README checked as a **floor** (#2630) | FALSIFY-PVI-004 | P-0.3 | 2026-09-26 | the same "no claim without a receipt" rule this release applies to tok/s |
| **P-0.5** | freeze the archived upstream: port the two stranded commits + dirty `manzana` binding; remove the `../provable-contracts/lean` fallback; `ARCHIVED-UPSTREAM.md`; untrack 10 `.pv/` caches | FALSIFY-PVI-005 | — | 2026-09-26 | one source of truth; `manzana` is the crate R-0's `metal` entry will cite |
| **P-0.1** | one ladder (upstream numbering L4 Kani / L5 Lean; PVI-D-1 default) in `proof_status.rs` and the three docs; `ladder_consistency` test | FALSIFY-PVI-001 | — | 2026-10-03 | documentary; pairs with P-0.4 |
| **P-1.1** | `#[contract]` fails closed (#2699 in full): generated registry, `compile_error!` on missing name/equation/unparseable predicate, `optional` is the only silent spelling and warns, `lib.rs:26` fixed; `trybuild` case table | FALSIFY-PVI-007 | P-0.6 | 2026-10-03 (v1.6: 10-10 → 10-03 so P-1.2, carded separately, keeps its slack) | without it `#[contract]` on any 0.66 function is decoration; after it, R-0's `discover()` can carry `#[contract("apr-backend-registry-v1", …)]` and mean it |
| **P-1.2** | repoint or author the 10 dangling `#[contract]` sites (same PR as P-1.1: they become compile errors) | the build | P-1.1 | 2026-10-10 | — |

- **host:** any; `intel` for the `pv lint` CI step (already the clean-room runner; no new lane — D-3 not implicated). **K̂:** PV-IMPROVE's "≈ 2 days / ≈ 1 week" are its author's estimates, `[U]` here; `impl-estimates.jsonl` after P-0.3
- **quorum:** teamwork on P-0.1/P-0.4 (public-claim changes), N-lane on P-1.1 (macro fail-closed semantics are a design), review-only otherwise
- **refusals carried from PV-IMPROVE §10:** no existing blocking gate lowered; no contract's mathematical content decided here; APR-MONO not re-litigated

### Track S — speed rows (`AbRecord`, no ratio announced; PP-12)

**S-1 · W-A per-token host work out of the decode loop** (master row 19; report row 1; G1)
- `pmat work add "W-A (PP-LLAMA-001 §9 #5): one model.write() per step, embed on device, no per-slot try_send; AbRecord with registered prediction <= 3.5 ms/tok" --description "generate_batched_streaming.rs; prediction written into the probe before the run"`
- aprender (`crates/aprender-serve`) · `perf/wa-host-work` · blockers: master 1, 6, 9 · host: lambda · expiry **2026-10-16 (master; D-4)** · `K̂` `[U]`
- **A:** `scripts/perf_ab.sh --subject wa --delta code --interleave 5` → `AbRecord` with `pred_ms_per_tok <= 3.5` recorded **before** the run and `measured.lcb95_ms_per_tok <= 3.5` after; PP-26 witness unchanged at m=1 (the greedy stream is byte-identical)
- **contract:** `contracts/apr-perf-wa-v1.yaml` — invariants: exactly one `model.write()` per step (counted by a test hook); m=1 greedy stream identical pre/post
- **mutation → RED:** reintroduce the per-token `write()` → the counter invariant RED. Discriminates: PP-26 witness stays GREEN (correctness unchanged)
- **quorum:** N-lane on the root cause (min 3): performance physics, systems, evidence
- **prediction status:** the "≥ 0.85 after W-A alone" ratio is `[U]` **joint** with W-B (master §11: no causal independence); only the ms/tok prediction is registered

**S-2 · W-C prefill without synchronous copies** (master row 20; report row 3; G2)
- `pmat work add "W-C (PP-LLAMA-001 §9 #3): pinned pre-allocated activation/KV staging, one async copy per layer, allocation outside the request; prediction copies+allocs < 10% of CUDA API time" --description "#2697 nsys: cuStreamSynchronize 57.4% of API time; 1,018 sync copies and 904 allocs over the whole session (startup + 8 requests), i.e. ~127 copies / ~113 allocs per prefill [C]"` — receipt: evidence/parity-http/findings.json `fixed_cost_root_cause.api_time` (#2697: `cuStreamSynchronize.pct` 57.4, `cuMemcpyHtoD_v2.calls` 1018, `cuMemAlloc_v2.calls` 904)
- aprender (`crates/aprender-serve`) · `perf/wc-prefill-async` · blockers: master 9 · host: lambda · expiry **2026-10-23 (master; D-4)** · `K̂` `[U]`
- **A:** `nsys profile … scripts/perf_w4.sh --tokens 64,256,512,2048` (W4 sweep, master row 17) → `evidence/nsys/wc.json` with `api_share.copies_allocs < 0.10`; `AbRecord` with `prefill_tok_s > 5700` registered before the run; golden output identical to pre-change
- **contract:** `contracts/apr-perf-wc-v1.yaml` — invariant: `cuMemAlloc` calls per prefill ≤ layers (allocation outside the request); the per-prefill count is measured on a single-request trace, never divided out of a session total (review MIN-03)
- **mutation → RED:** allocate per layer per token → allocation-count invariant RED. Discriminates: witness GREEN
- **quorum:** N-lane root cause (min 3)
- note: 5,700 tok/s is 0.55× of the comparator `[C]` (5,700/10,399; 10,399 is the comparator prefill median, receipt: evidence/parity-http/findings.json, comparator prefill median; 5,700 is the registered prediction, not a measurement). `prefill_ratio` at c=1 stays REPORTING after W-C; this row does not arm anything and the release notes say so

**S-3 · W-B Q4_K GEMV microbenchmark** (master row 13 first half; report row 2 — bench only; kernel → 0.67)
- `pmat work add "W-B bench: cargo bench batched Q4_K GEMV at M in {1,4,8} vs M in {16,32} cuBLAS GEMM; publish the per-token time table; no kernel change" --description "Master §7.3 DESIGNED, NOT ARMED; this row arms the instrument"`
- aprender (`crates/aprender-serve`, `crates/aprender-gpu`) · `bench/wb-gemv` · blockers: master 1 · host: lambda, then gx10 (queue slot 3) · expiry 2026-09-26 · `K̂` `[U]`
- **A:** `cargo bench -p aprender-serve --bench q4k_gemv -- --save-baseline wb0` → `evidence/bench/wb0.json` with per-M medians and MADs; prediction `t(M=4)/t(M=16) ≤ 1.3` registered before the run (report §10) — kill if ≥ 1.5, which sends the kernel row (0.67) back to a profile
- **contract:** `contracts/apr-bench-q4k-gemv-v1.yaml` — invariant: bench output has ≥ 5 samples per M and reports MAD
- **mutation → RED:** run 1 sample per M → RED. Discriminates: nothing else reads the bench
- **quorum:** review-only

### Track T — training: measure first (pending D-2)

**T-0h · training-parity harness**
- `pmat work add "T-0h: train_parity.sh + receipt schema in evidence/train-parity/; extend qwen-train-canary recipe to Qwen2.5-7B-Instruct; interleaved A,B,A,B; clocks locked; schema validator" --description "No 7B training number exists for either engine; the harness lands before the number"`
- aprender · `feat/t0-train-parity-harness` · blockers: — · host: lambda (dev), both (run) · expiry 2026-09-19 · `K̂` `[U]`
- **A:** `scripts/train_parity.sh --validate tests/fixtures/train-parity/*.json` exit 0 · `scripts/train_parity.sh --dry-run --model Qwen2.5-7B-Instruct --seq 2048 --mb 4 --rank 16 --alpha 16 --targets q,k,v,o,gate,up,down --steps 200 --seed 42 --n 5 --interleave` prints the 20-run plan with a **time budget** `[C]` = `n × engines × hosts × t_run` where `t_run` is measured by one pilot run per (engine, host) and written with `basis=pilot`
- receipt fields (normative): engine sha/version, host, clocks, dataset sha256, **requested** and **effective** seq / micro-batch (read back from the engine, not from the CLI — S0-11's silent clamp is the defect this field exists for; requested ≠ effective ⇒ `INVALID-CONFIG`), LoRA config, steps, seed, `train_tok_per_sec`, `peak_vram_bytes`, `final_loss`, interleave order, `started_utc`, `partial`
- **memory prediction registered before the pilot** `[C]`: apr's f32 uncheckpointed tape at seq 2048 / mb 4 stores ≥ 7 projections × 28 layers × (4 × 2048 × 3584 × 4 B ≈ 117 MB) ≈ 23 GB of activations before attention scores — it will not fit 24 GB (review MAJ-06's arithmetic, re-checked here). So the pilot **descends** the config ladder {seq 2048/mb 4, 2048/1, 1024/1, 512/1} and stops at the first that completes; that config is the paired baseline for **both** engines (parity is same-config), and Unsloth additionally runs its own max config as REPORTED. The 4090 cell says which rung it sits on; a rung is never inferred
- **contract:** `contracts/apr-train-parity-receipt-v1.yaml` — invariants: paired fields identical across the A/B pair except `engine`; `interleaved: true` or the receipt is INVALID
- **mutation → RED:** drop `interleaved` → validator RED. Discriminates: no perf receipt reads this schema
- **quorum:** teamwork on the recipe (spec), review-only on the diff

**T-1 · τ_loss derived, not declared** (closes F-8a)
- `pmat work add "T-1: tau_loss for the canary dataset from Unsloth-vs-Unsloth seed variance (seeds 42,43,44), 200 steps; tau = 3 x MAD of final_loss; written into the dataset manifest with basis=" --description "An invented tolerance is a vacuous witness"`
- aprender · `feat/t1-tau-loss` · blockers: T-0h · host: gx10 (queue slot 2, bundled with T-0's window) · expiry 2026-09-26 · `K̂` `[U]`
- **A:** `scripts/train_parity.sh --tau-derive --seeds 42,43,44` → `evidence/train-parity/tau_loss.json` with the three losses, MAD, τ and `basis=seed-variance-n3`
- **contract:** folded into `apr-train-parity-receipt-v1.yaml` (τ must cite `tau_loss.json` by sha256)
- **mutation → RED:** hand-type τ → validator RED (no sha). Discriminates: yes
- **quorum:** review-only

**T-2 · `--max-seq-len` honoured or refused, never clamped** (the T-5 half that is a silent-pass defect; 0.66 regardless of D-2)
- `pmat work add "T-5a: apr finetune --max-seq-len is honoured, or refused with one line and the refusal code; the effective value is printed and written to the receipt; no silent clamp" --description "finetune.rs:717 (the wgpu instruct pipeline) hardcodes 512 and drops the CLI value; the instruct path honours it since #2247 (S0-11); T-0's apr arm cannot be paired until every path's effective config is the config it claims"`
- aprender (`crates/apr-cli`, `crates/aprender-train`) · `fix/t5a-max-seq-len-honoured` · blockers: S0-11 · host: any (CPU test) · expiry 2026-09-19 · `K̂` `[U]`
- **A:** `cargo test -p apr-cli --test finetune_seq_len_truth` — for `--max-seq-len ∈ {256, 512, 1024, 2048}` the engine's effective value equals the request or the command refuses; a mutation that restores the `512` literal makes the 1024 and 2048 rows RED
- **contract:** `contracts/apr-finetune-config-truth-v1.yaml` — invariant: `effective == requested ∨ exit == refusal_code`
- **mutation → RED:** restore the literal at `:317` → RED. Discriminates: T-0h's validator is not exercised by this test
- **quorum:** review-only

**T-0 · the four WT receipts** (report row 6)
- `pmat work add "T-0: Unsloth QLoRA and apr finetune -m qlora on Qwen2.5-7B-Instruct, both hosts, n=5 interleaved, receipts in evidence/train-parity/; first two training ledger rows" --description "Paired at the highest ladder rung apr completes on each host (T-0h pilot); Unsloth also at its own max, REPORTED; the receipt names the rung"`
- aprender · `evidence/t0-wt-receipts` · blockers: T-0h, T-1, T-2 · host: lambda, gx10 (queue slot 2) · expiry 2026-10-03 (v1.6: 09-26 → 10-03, zero slack vs T-1; §12 rule) · `K̂` from T-0h's `--dry-run` budget (`basis=pilot`)
- **A:** `scripts/train_parity.sh --validate evidence/train-parity` exit 0 with four receipts; `evidence/train-parity/LEDGER.md` rows 1–2 appended (schema as master Appendix C with `validity_by_engine`)
- **contract:** as T-0h
- **mutation → RED:** a receipt with `partial=true` → validate RED
- **quorum:** review-only; the *numbers* are reported with `[C]` ratio and no adjective

**T-3 · training gate, REPORTING + self-ratchet** (report row 18 re-shaped per P-6; pending D-2)
- `pmat work add "T-7 (REPORTING): train_tok_per_sec, peak_vram, |dloss|<=tau in perf-matrix.yaml as WT cells; ratchet seeded at the T-0 achieved value; arms on first P-5 PASS, never by date; must-fire mutation = deliberately unfused build below the ratchet" --description "P-6 applied to training; D-2 decides whether parity vs Unsloth is a target at all"`
- aprender · `feat/t7-train-gate-reporting` · blockers: T-0, **R-3** (a gate that can arm over a misleading banner is theater — review issue 3.5), master 8 (ratchet shape) · host: both · expiry 2026-10-17 · `K̂` `[U]`
- **A:** `scripts/perf_gate.sh --phase merge --workload WT` prints `REPORTING` with the ratio and `ratchet: seeded@<sha>`; `scripts/perf_gate.sh --selftest wt_ratchet_{fire,hold}` both present (PP-29)
- **contract:** `contracts/apr-train-gate-v1.yaml` — invariants: no `ARMED` state without `armed_by` pointing at a receipt whose `lcb95 ≥ 1 − δ`, `δ = 0` unless authored
- **mutation → RED:** set `armed_by` by hand → selftest RED. Discriminates: inference gate selftests untouched
- **quorum:** teamwork (this is a policy artifact)

### Track B — backends: owned cells and one gate (lanes pending D-3)

**B-A1 · AMD gets an owned cell** (report row 21; re-shaped by S0-3)
- `pmat work add "B-A1: hosts.amd (or hosts.intel.wgpu) cell in perf-matrix.yaml; compute_class wgpu; status UNMEASURED with owner; expiry anchored on the blocker S0-3 names" --description "If S0-3 finds a Radeon on intel: anchor = D-3 (CI home), not hardware acquisition. If not: anchor = AMD-HOST-ACQUISITION in expiry_anchors, status open, merged_on null"`
- aprender · `chore/ba1-amd-cell` · blockers: S0-3, S0-6 · host: any (a YAML edit) · expiry 2026-09-19 · `K̂` `[U]`
- **A:** `scripts/check_backend_firstclass.sh --matrix-only` exit 0 (the cell parses, has an owner, has exactly one expiry form, declares no class the registry does not print on that host — PP-16, now checkable: `apr devices --json` on the host is the oracle once R-0 lands); `python3 -c "…"` prints the cell
- **contract:** `contracts/apr-perf-matrix-cell-v1.yaml` — invariant: every `UNMEASURED` cell has `owner` and exactly one of `expires|expires_after{anchor}`; every anchor exists in `expiry_anchors`
- **mutation → RED:** type a date instead of the anchor → RED. Discriminates: yes
- **quorum:** review-only

**B-G1 · first-class scorecard as a gate** (closes F-2 for the §2 definition)
- `pmat work add "check_backend_firstclass.sh: compute clauses 1-7 per backend from the tree (workflows, BACKEND_VALUES, perf-matrix.yaml, receipts, dogfood, formats, pins); print N/7; FAIL if any doc/README claims a backend first-class with N<7" --description "The report scores cuda 4/7, wgpu 1/7, metal 0/7, amd 0/7, wasm32 0/7 by hand; this makes the score a tree fact. wasm32 clause 4 (batch invariance) is N/A-by-construction at m=1 and is printed as such, not as a pass"`
- aprender · `feat/bg1-firstclass-gate` · blockers: **R-0** (clauses 2 and 3 are read from the registry: refusal-by-enumeration, and `compute_class` = what the binary prints), R-2 (clause 5) · host: any · expiry **2026-10-03** (moved from 09-26: blocked by R-0/R-2) · `K̂` `[U]`
- **A:** `scripts/check_backend_firstclass.sh` exit 0 at HEAD and prints `cuda 6/7` (clauses 2 and 5 close with R-0/R-2; clause 3 stays "UNMEASURED with owner" which is a pass of the clause) or lower — the number is derived, never typed here; wired into `ci / gate` beside `check_no_claim_literals.sh`; case table `tests/fixtures/firstclass/*.yaml` with a must-match and must-not-match row per clause; vacuity floor: fewer than 5 backends enumerated from `BACKEND_VALUES` + matrix = FAIL
- **contract:** `contracts/apr-backend-firstclass-v1.yaml` — invariant: `claimed_firstclass(b) ⇒ score(b) == 7`
- **mutation → RED:** add "wgpu is a first-class backend" to `README.md` → RED. Discriminates: `check_no_claim_literals.sh` matches ratio literals, not this phrase — confirm by running both under the mutation
- **quorum:** teamwork on the clause-to-artifact mapping, review-only on the diff

Deferred to 0.67 pending D-3: B-W1..B-W5, B-M1..B-M4, B-S1..B-S4, B-A2 (§6). If D-3 resolves "intel may host
a wgpu lane" before 2026-09-19, B-W1 re-enters 0.66 with the S0-3 falsifier: adapter `deviceType ==
DISCRETE_GPU`, `vendorID` recorded, `lavapipe|llvmpipe` in `driverName` = FAIL.

### Track G — guards and counts

**G-1 · crate-name guard, allow-list full** (report row 25; mutation corrected per F-19; universe corrected per review MAJ-08)
- `pmat work add "check_crate_names.sh: [lib] name == directory with - -> _ over the cargo-metadata member set (root workspace + crates/facades workspace); every other Cargo.toml with a [package] found by find must be in an explicit non-member registry or FAIL (unregistered manifest); allow-list = 51 rows + the D-5 row (52); fail-closed on unparseable [package] manifests, [workspace]-only shells skipped by construction; case table; must-fire mutations" --description "Ratchet 51 -> 0 starts here; zero renames in 0.66. The report's git-ls-files universe finds 106 manifests incl. [workspace] shells (aprender-present, aprender-test), fuzz/, wasm-pkg/, bins/trueno-ublk, experiments/ and would be red on day one [A] review MAJ-08"`
- aprender · `feat/g1-crate-name-guard` · blockers: S0-10 · host: any · expiry 2026-09-26 · `K̂` `[U]`
- **universe (two layers, both mechanical):** (1) *members* = `cargo metadata --no-deps --format-version 1 | jq -r '.workspace_members[]'` for the root workspace **and** for `crates/facades` (a separate workspace created to avoid the `libprovable_contracts.rlib` collision `[A]`) — apr-dogfood G0.2: crate identity from `cargo metadata`, never from `sed`; (2) *strays* = `find . -name Cargo.toml -not -path './target/*'` filtered to files containing `[package]`, minus members, minus `scripts/crate_names_nonmembers.txt` — an explicit registry that today must list at least the eight auxiliary Spanish-named manifests the review found (`trueno-ublk`, `trueno-db-wasm`, `trueno-viz-wasm`, `trueno-gpu-fuzz`, `renacer-fuzz`, `entrenar-fuzz`, `certeza-fuzz`, `batuta-quick-start` `[A]`), `fuzz/`, `crates/aprender-train-canary`, `experiments/`. Any stray not in the registry ⇒ FAIL. This keeps `feedback_tracked_only_universe_gives_a_free_pass` (a new untracked crate is a stray) without choking on shells
- **rule set (pending S0-10):** (a) `[lib] name == basename(dir)` with `-`→`_`; (b) if S0-10 confirms dep-key renames, every `[dependencies]` key equals its `package` name (no `trueno = { package = "aprender-compute" }`), reported as a second counter `depkeys_remaining`; (c) feature strings reference no old dep key (`trueno/gpu`, `dep:realizar`, `apr-cli/trueno-explain` `[A]` review MIN-11) — `features_remaining`. Three ratchets, each printed, each down-only
- **A:** `scripts/check_crate_names.sh` exit 0 printing `remaining=51 depkeys_remaining=<n> features_remaining=<n>`; `bash tests/guards/crate_names_cases.sh` runs the case table (bad name under `crates/aprender-compute` ⇒ FAIL; commented `# name = "trueno"` ⇒ PASS; absent `[lib]` ⇒ PASS; `[[bin]] name` ignored; `[workspace]`-only shell ⇒ skipped and counted as `shells=`; root `Cargo.toml` scanned and reported under the D-5 row; `crates/facades/*/Cargo.toml` scanned via its own workspace; duplicate `[lib] name` across two manifests ⇒ reported, allowed only by the D-5 allow-list line; an unregistered stray ⇒ FAIL); wired into `ci / gate`
- **contract:** `contracts/apr-crate-names-v1.yaml` — invariants: members from `cargo metadata`; strays ⊆ registry; all three `remaining` counters computed, never typed
- **mutation → RED (three, all in the PR body):** (i) add an untracked `crates/zz-probe/Cargo.toml` with `[package]` and `[lib] name = "probe"` → RED as an unregistered stray; (ii) delete one allow-list line → RED (`remaining` would read 50 while 51 offend); (iii) add `zz-probe` to `workspace.members` with a bad name → RED via layer (1). The report's "re-introduce an old name" is vacuous at landing and is **not** a registered mutation. Discriminates: (i) and (iii) leave every other guard GREEN — confirm by running the full guard set under each mutation
- **quorum:** review-only

**G-2 · D-5 decision ticket** — `pmat work add "DECISION D-5: [lib] name = aprender declared by root Cargo.toml and crates/aprender-core; keep front door or rename to aprender_core; the guard's allow-list line for this row cites the decision"` · owner spec-owner · artifact: one line in §0 with `decided_by` and date · no code.

**G-3 · rename cost, measured** (the number that sequences 0.67's PRs; in-workspace half now `[A]` from the review)
- `pmat work add "G-3: re-run the review's use-site count (4,097 in-tree [A]; realizar 1,428, presentar_core 355, trueno_gpu 295, provable_contracts 291, trueno 218) to [V] at HEAD with the command committed; add dep-key and feature-string counts (S0-10); count sites in sibling checkouts apr-cookbook, qwen-train-canary, pmat; crates.io reverse deps of the 51 names; write docs/audits/crate-rename-cost.md with basis=" --description "No rename PR is sequenced until this exists (report §6.7). The review's figure is [A] until the committed command reproduces it"`
- aprender · `docs/g3-rename-cost` · blockers: — · host: intel (has the sibling checkouts; network for crates.io) · expiry 2026-10-03 · `K̂` `[U]` · non-blocking track
- **A:** `scripts/crate_rename_cost.sh` (committed) reproduces the review's regex `git grep -cE '^[[:space:]]*use[[:space:]]+(::)?<name>(::|[[:space:]]*;)' -- '*.rs'` per name and prints the total; `pmat query --regex 'use (trueno|realizar|entrenar|batuta|pacha|certeza|verificar|alimentar|repartir|renacer|simular|presentar|probador|cbtop|cgp)(_[a-z_]+)?::' --path crates --count` as the second estimator (two estimators, disagreement reported, neither silently preferred); `git grep -cE '(dep:|/)(trueno|realizar|entrenar|batuta|pacha|alimentar)' -- '*/Cargo.toml' Cargo.toml`; `curl -s https://crates.io/api/v1/crates/<name>/reverse_dependencies` count per name (rate-limited; record the timestamp)
- **contract:** none (a measurement document); the number enters `check_crate_names.sh --report` as `sites=` per row so the ratchet shows cost beside count
- **quorum:** none

**G-4 · the obligation DAG as data, with invariants in CI** (closes F-10, review MAJ-03/MAJ-04 mechanically)
- `pmat work add "check_dag_invariants.sh over docs/specifications/pp-066-dag.yaml: 0 cycles; min slack 6 days between a row and every row it blocks; per-host queue order consistent with expiry; owner present; exactly one expiry form; report which rows are past expiry; the §5/§6 tables in this spec are rendered from the yaml, never hand-edited" --description "The report's slack claim was checked by hand and missed the physical queue; the review found W-G before W-B on gx10 and W-H expiring the day of the row it should block. A DAG whose invariants are prose is re-checked by every reader and by none of them"`
- aprender · `feat/g4-dag-invariants` · blockers: — · host: any · expiry 2026-09-19 · `K̂` `[U]`
- **A:** `bash scripts/check_dag_invariants.sh docs/specifications/pp-066-dag.yaml --min-slack-days 6` exit 0 — the checker is in-repo (`scripts/lib/dag_invariants.py`, D1..D6, 11-row case table): `pmat comply check` has no `--rule obligation-dag` at pmat 3.37.0 (measured 2026-09-05; a pmat ticket is owed and PV-IMPROVE principle 8 applies the day it lands); `scripts/render_dag.py` regenerates §5/§6 tables byte-identical to the committed spec (drift = RED); wired into `ci / gate` beside `spec_conformance.sh` (master row 0e)
- **contract:** `contracts/apr-obligation-dag-v1.yaml` (kind: pattern) — invariants: acyclic; `expires(blocker) + 6d ≤ expires(blockee)`; `queue_pos(host, a) < queue_pos(host, b) ⇒ expires(a) ≤ expires(b)`
- **mutation → RED (three):** set W-H's expiry equal to C1's → RED (slack); swap two gx10 queue positions → RED (order); add a self-edge → RED (cycle). Discriminates: `spec_conformance.sh` reads the master's invariant table, not this yaml
- **quorum:** review-only

### Track D — documentation (non-blocking)

**D-1 · `cuda-backend-architecture.md`** (report §6.6, row 28; oracle re-shaped per F-16)
- `pmat work add "docs: cuda-backend-architecture.md — PTX backend, sm_121 JIT (GH-480), fused NF4 paths, cuBLAS backend and CUBLAS_GEMM_THRESHOLD, FP8 prefill on sm_89, graph-capture cuBLAS bypass, select_prefill_path dispatch, CU_STREAM_DEFAULT ordering, flag-to-kernel map; every NVIDIA API guarantee cited to a vendored pinned doc artifact" --description "Sovereign oracle: evidence/cuda-docs/<file>.<sha256>.pdf pinned; nvidia-cuda-docs MCP is a lookup aid, not the citation; UNVERIFIED{…} inline where neither is available"`
- aprender · `docs/cuda-backend-architecture` · blockers: none (row 28 stays off the tag's blocker set) · expiry 2026-10-03 · `K̂` `[U]`
- **A:** the existing `claims-cite` check (it passed on #2868 `[A]`) extended to `API:` sentences rather than a new script — `scripts/check_doc_citations.sh` only if `claims-cite` cannot express the rule, and then it says why — over `docs/specifications/cuda-backend-architecture.md`: every sentence tagged `API:` carries a citation `{artifact sha256, page}` or an `UNVERIFIED{reason, date}`; exit 0 iff `UNVERIFIED` count ≤ the count declared in the receipt header (ratchet down-only); the nine sections of the report's table each cite the code path it names
- **contract:** `contracts/apr-doc-citation-v1.yaml` — invariant: an `API:` sentence without a citation or an `UNVERIFIED` tag fails
- **mutation → RED:** strip one citation → RED
- **quorum:** teamwork (a doc)

---

## §6 0.67 lane — carried rows (compact; falsifiers as the report wrote them, blockers corrected)

| id | deliverable | blockers (corrected) | host | kill if (report) |
|---|---|---|---|---|
| W-B kernel | multi-warp Q4_K GEMV, M ∈ {1,4,8} | S-3 (bench ≤ 1.3×), master 1 | lambda → gx10 | no gain at M ≤ 8, or per-layer cosine witness fails |
| W-D | f16 KV + per-layer cosine witness; reserve from measured peak | S-2, **master 6** (F-21) | lambda, gx10 | any layer cosine < 0.999 at 4096 ctx, or PP-26 regression |
| W-E | scheduler: `slots_admitted` concurrent decodes under captured graphs — **one graph per M-bucket {1,2,4,8}** or padding, decided in the card (a captured graph freezes shapes; cuBLAS is bypassed under capture at `attention.rs:995,1022` `[A]` — review issue 3.1) | S-1, W-B kernel, master 18 (a receipt to reconcile) | lambda | target `agg(4)/dec(1) ≥` the comparator's own measured ratio (2.83× `[C]`, F-14), kill if `< 2.5×` (§5's `3×` and §10's `2.5×` in the report disagreed — review MIN-06); frozen slot (CB-008); intra-batch divergence |
| W-G | GB10: NEON Q4_K GEMV; batched prefill default on sm_121 after master 21's root cause; pre-compiled kernels (trueno#203) | master 2 (probe), master 21, W-B kernel | gx10 | prefill not linear in prompt tokens, no collapse under batched prefill |
| W-H | batch-invariant reduction; logprob margin on the wire (**instrument `[U]` until S0-1**) | W-E | both | c=16 witness < 64 after the epilogue change |
| W-F attach | `apr run` attaches to a running `apr serve`; W5 resident arm | R-4 | both | resident completion slower than Ollama |
| T-1..T-5 | pre-compiled sm_121; fused RMSNorm/RoPE/SwiGLU/chunked CE **with their backward kernels** (review 2.5 — the report counted forward launches only); NF4-in-the-matmul **needs a kernel design first**: cuBLAS/cuBLASLt have no NF4 GEMM, the existing fused NF4 PTX path is 15.5 tok/s `[A]`, the 298 tok/s path materialises f16/f32 weights (`cuda_block.rs:4114-4145` `[A]` review 2.4) — a registered per-kernel prediction (mma.sync + register dequant) precedes any T-3 code; bf16 + checkpointing (offload must be double-buffered and overlapped, PCIe ≈ 31.5 GB/s `[A]`); packing (T-2 in 0.66 already covers `--max-seq-len`); the 15.5 / 298 tok/s and 31.5 GB/s figures are quoted from entrenar-spec §perf (UNRECEIPTED [C] — provenance only: evidence/reports/0.66-parity-report-provenance.json) | T-0, T-2, D-2 | per report | per report §6, corrected above |
| T-7 arm | first P-5 PASS on WT | T-3, T-1..T-5 | both | passes on a mutated (unfused) build |
| **B-W0** (new) | quantized-weight upload path in serve: wire the existing `Q4K_GEMV_SHADER` (`aprender-compute/src/backends/gpu/shaders/basic_ops.rs` `[A]`) into `OwnedQuantizedModelWgpu` (a stub today `[A]`); packed 4-bit weights on device (4.68 GB), chunked to `maxStorageBufferBindingSize` (128 MiB default, ≤ 2 GiB−4 on desktop `[A]` review seg-3 F3); `Features::SUBGROUPS` requested and the grant/denial **recorded in the receipt** (mechanism engaged); f32 dequant path (30.4 GB for 7.61 B params `[C]`, cannot fit any proof host — review MAJ-02) kept only as a ≤ 1B test arm. #2826 measured wgpu-on-Metal decode at 0.123 tok/s (8.14 s/token) on mini — the f32 path was run; B-W0 confirmed as prerequisite (issue-sourced figure, UNRECEIPTED, provenance only: evidence/reports/0.66-parity-report-provenance.json) | **D-3**, **R-0** (wgpu adapters are registry entries; the software-rasteriser refusal is the registry's `Unavailable` reason), S0-13 | intel | device bytes > 1.3 × model bytes; adapter not hardware; a `SUBGROUPS` denial unrecorded |
| B-W1..W5 | wgpu lane on intel; Vulkan comparator pin; witnesses; W1 receipt; clauses 5–6 | **B-W0**, **D-3**, R-0, R-2 | intel | per report; adapter falsifier per S0-3/S0-4; B-W4's "wgpu loses to CPU" branch is a **recorded outcome**, not a broken plan |
| B-M1..M4 | `metal` becomes a **Ready registry entry** on `mini` (R-0 already registers the kind as `Unavailable(NotCompiled)`): wgpu-Metal adapter first, `manzana::metal` device enumeration as the source line; NA→UNMEASURED supersession recorded in-cell **when the registry prints Ready on mini and not before** (#2841 by construction); llama.cpp-Metal + Ollama pins; clauses 5–6 | **D-3**, **R-0**, S0-12 | mini | per report. **Correction withdrawn (S0-12, v1.6):** `metal_shaders.rs` holds MSL *source strings only* — no dispatcher, no `metal` feature, no `manzana` (#2849 removed it); `MetalBackend` is a placeholder whose own test asserts it is unavailable. There is no third kernel family today; B-M1 starts from the wgpu-Metal adapter, and any native MSL execution is new code with its own PP-26 witness |
| B-S1..S4 | 0.5B GGUF+APR decode under `wasmtime`; browser harness; size budget + self-ratchet; clauses 5–6 (clause 4 N/A at m=1, printed) | **D-3**, R-0 | CI runner | per report. **Scale exclusion enumerated under clause 6, computed not typed:** wasm32 linear memory is 2³² B = 4.00 GiB (browsers commonly 2 GiB); the 7B Q4_K_M weights alone are 4.68 GB, so W1 (7B) is excluded on wasm32 by address space (review MAJ-01) and the cell key is `ci/W1_0.5b`. The bound is `model_bytes + kv_bytes + activations < 2³²` per receipt, not "≤ 2B parameters". `aprender-serve` is `#[cfg(not(target_arch = "wasm32"))]` entirely `[A]` — B-S1 is a port of the loader/decoder, not a harness |
| B-A2 | vendor-neutral Vulkan lane receipt on AMD silicon; ROCm/HIP row with its blocker | B-A1, B-W4, **S0-3/D-6** | intel or acquired host | NVIDIA-specific assumption in the wgpu lane |
| PVI 1.3–4.3 (#2556) | kill the empty macro arm (329 sites) and scoped codegen (379,866 → < 30,000 lines); `PV-ENF-003` vacuous-predicate lint; one binding registry both-ways (#2797/#2704); Kani resolution gate + `kani-nightly.yml` receipts; Lean pinned Mathlib + `lean-nightly.yml` receipts (PVI-D-4; runner = D-3's question again); `registry: true` removed (#2557) + `kind: descriptor`; prose `test:` → `prediction` (2,474); score-floor ratchet; `contracts/work/` out, `v1-legacy-skip/` gone, fleet dir; course-repo downstream smoke; facade freshness; `pv` fail-closed fixture suite | P-0.x, P-1.1, PVI-D-2..5, D-3 (nightly Lean/Kani runner) | intel (clean-room) | FALSIFY-PVI-008..018 each observed RED once on its committed mutant; a rename PR (§6.7) that touches a `generated_contracts.rs` must not precede PVI-1.5 or it re-generates 26 k lines per crate |
| §6.7 renames | 51 renames, phased per **D-8** (leaves may batch; core singly; `realizar` alone in a freeze window); **shim package only** — the report's in-crate `pub mod trueno { pub use crate::*; }` does not resolve `use trueno::…` for a consumer of `aprender-compute` (extern-prelude resolution, E0432 `[A]` review MAJ-07) and is **not** advertised as compatibility; consumers of the current package get an announced break with the migration line `trueno = { package = "aprender-compute", version = "…" }`; every rename PR also updates dep keys and feature strings (G-1 counters b, c); shim crates live in `crates/facades/` (the pattern the tree already uses) with their own versioning rule written down | G-1, G-2, **G-3**, D-8 | — | guard green under a rename without its shim; a rename PR that leaves `depkeys_remaining` or `features_remaining` unchanged |
| bar gating | qwen-coder-deploy TTFT/ITL/`agg(4)` bar GATED | re-derivation per F-14 | lambda | the comparator fails its own bar |

---

## §7 Registered predictions (carried, corrected; falsifiers live in `PP-LLAMA-001-RATIONALE.md` §10)

| subject | prediction | **prediction killed if** (⇒ re-plan from the measurement; never a track kill — the report's column header let this be read both ways, review MAJ-05) | measured by | note |
|---|---|---|---|---|
| S-1 W-A | m=1 host+kernel ≤ 3.5 ms/tok on the 4090 | > 3.5 ms | `AbRecord`, then W1 receipt | ratio predictions `[U]` joint with W-B |
| S-2 W-C | copies+allocs < 10 % of CUDA API time; prefill > 5,700 tok/s | share > 30 % | nsys on W4 | 5,700 = 0.55× comparator `[C]`; nothing arms |
| S-3 W-B bench | `t(M=4)/t(M=16) ≤ 1.3` | ≥ 1.5 | `cargo bench` | kernel row waits on this |
| T-0 | Unsloth 7B QLoRA at seq 2048 / mb 4 fits 24 GB; apr's f32 uncheckpointed tape does not (≈ 23 GB activations before scores `[C]`) | apr fits at 2048/4, **or** Unsloth does not | WT receipts + T-0h pilot ladder | either way the paired rung is whatever both complete; the rung is in the receipt (T-0h). Not a track kill |
| T-1 | τ_loss from 3 seeds is < 0.05 nats on the canary set | ≥ 0.10 | `tau_loss.json` | `[U]` — no prior seed-variance figure exists; the number is the instrument's first reading |
| REG-2 / S0-20 | apr touches no `cudart` anywhere; cuBLAS is the only version-pinned runtime library it loads | a `cudart` string in `crates/` or in `ldd apr` | S0-20 | if false, REG-2 becomes a ticket, not a property |
| REG-6 / S0-21 | on gx10 (GB10, unified) `cuMemGetInfo.free` falls when host RAM is consumed by another process; on lambda it does not | the opposite on either host | S0-21 | decides how `mem_kind: Unified` is computed |
| R-0 | `(build=wgpu, ask=cuda)`, `(build=cuda, ask=wgpu)` and `(build=cuda, host=no-libcuda, ask=cuda)` all refuse with the registry's reason and the refusal code (9 today `[A]`, D-7); `apr devices` prints ≥ 1 Ready entry on all four hosts; printed cuda device name = `nvidia-smi -L` on lambda/gx10 | any row accepted, the `cfg!` mutation leaves the table GREEN, or a printed device name that the host's `nvidia-smi` does not know | case table + `check_backend_registry.sh` on the dogfood run | |
| R-5 | the aarch64 release asset run through H12 on gx10/mini **before** promotion reproduces PMAT-964's floor breach (the gate now fires where it was missing) | H12 passes on the asset while the cargo-installed binary failed it — which would mean the two are different builds and D-10 is wrong | promotion job receipts | |
| P-0.3 | `181 lean proved` → **8**; L4/L5 counts fall to single digits; #673's "9 below L5" becomes far larger | the number does not move (the summary block was not the source of the credit) | `pv proof-status` declared vs discharged | PV-IMPROVE's own prediction, carried |
| S0-14 / D-9 | `--features cuda` builds on intel and mini with no CUDA toolkit; `ldd`/`otool` shows 0 CUDA links | a link error, or a `libcuda`/`libcublas` in the dynamic section | S0-14 output | decides R-2's shape |
| R-2 | post-publish dogfood records `backend: cuda` on both hosts | a CPU value under `--gpu` | dogfood receipts | |
| B-G1 | `cuda` scores 6/7 at HEAD after R-0, R-2 (clause 3 UNMEASURED-with-owner counts) | 7/7 printed for any backend before a CONFORMANT receipt exists | the gate's own output | the number is derived; if it prints something else, the derivation is what is audited |
| G-1 | both mutations RED in the guard's PR | either GREEN | PR body | |
| S0-3 | `intel` has a discrete AMD GPU | no display device, or an NVIDIA/Intel part | `lspci`, `vulkaninfo` | flips B-A1 anchor and B-W1 falsifier |
| `max_batch ≥ 22` (G3) | — | — | — | **not registered**: no `basis=`; re-derive from master row 6's `vram_peak` |

A prediction that fails is recorded with the measurement that killed it; the row is re-planned from the
measurement, never from the prediction.

---

## §8 Refusals (0.66)

- No re-arming of `HW_DP4A_Q4K` or `FUSED_GATE_UP=1` (F2 floor; master §10). No `BATCHED_PREFILL=1` as a fix.
- No published ratio without a CONFORMANT receipt (PP-12); no "≈96 % of llama.cpp" prose; no training ratio
  with an adjective — `[C]` and the two receipts, nothing else.
- No comparator configured away from `llama_pin.toml`; batching never turned off to make a band pass (PP-3).
- No `ARMED` state set by hand or by date, on any track (P-6). No training parity target asserted before D-2.
- No wgpu/Metal/wasm figure through a software rasteriser or an unnamed adapter; no lane on `intel` or `mini`
  before D-3; no `mini` cell flipped before a build on `mini` has taken the class (#2841 class).
- No `[lib]` rename in 0.66 (guard only). No workspace-wide sed, ever. No in-crate module alias advertised as backward compatibility (review MAJ-07).
- No refusal exit code typed into a test; the constant is read from `error.rs` (D-7).
- No PP-066 contract with `registry: true`, a hand-typed `verification_summary`, a prose `test:` field, or a `lean_theorem`/`harness:` name that does not resolve at HEAD; no `#[contract]` attribute counted as enforcement before PVI-1.1 (F-26).
- No `pv` invocation against the binary on PATH; `scripts/pv_bin.sh` builds it from the tree.
- No release promoted to non-prerelease without four host receipts naming the asset sha256; no install script that places a binary before both verifications pass; no third-party verification service (sigstore/Rekor/Fulcio) in the install path.
- No README install line that names backends by hand; the line is generated from the registry's kinds (R-7).
- No backend decision in `apr-cli` from a build fact (`cfg!`) after R-0 lands; no `compute_class` in `perf-matrix.yaml` that the registry does not print on that host.
- No runtime compilation (`nvcc`, `hipcc`, Xcode) and no payload extraction to `$TMPDIR` — ever; no `libcudart` dependency; no silent downgrade from a requested backend, device, reserve or offload to a lesser one (REG-3, REG-2, REG-7, REG-8, REG-14).
- No vendor-gated fallback: one backend's probe failure never changes another's entry (REG-4). No cross-vendor graph (REG-10). No persisted discovery state (REG-12).
- No tensor split or partial offload in 0.66 except as an owned `NotImplemented` refusal naming the 0.67 row (REG-9, REG-14).
- No `default = ["cli", "cuda"]` before R-0 is merged and S0-14 is green on all four hosts (D-9) — the countermeasure is not permitted to precede the root fix, because without the registry it turns a silence into a crash on three hosts.
- No "≤ N parameters" wasm scope line; the wasm bound is computed per receipt from bytes (review MAJ-01, narrowed).
- No paged-KV or sub-f16 KV-quant row until a measured `max_batch` shows KV as the binding constraint (review issues 3.2/3.3 are scope, not defects — EV rule).
- No hosted service as the oracle for a citation (F-16); no `nvidia-cuda-docs` reachability as a tag blocker.
- No qwen-coder-deploy bar gating until the comparator passes it (F-14).
- No expiry moved without an Appendix D amendment naming who and why (D-4).
- No CPU-class training claims; no vLLM comparator in 0.66.
- `git reset --hard` forbidden; no background "rerun failed legs" monitors; a red leg is a root cause, not a retry.

---

## §9 Toyota Way targets (0.66)

| principle | target | basis / instrument |
|---|---|---|
| jidoka | **0** c>1 bands published without a PP-26 PASS in the receipt | master L0; C1 validator |
| jidoka | **0** `ARMED` gates without `armed_by` → a receipt with `lcb95 ≥ 1` | T-3 selftest `wt_ratchet_fire`; PP-29 join |
| andon | **0** §5 rows past expiry without RED in `pmat comply`; RED on the non-blocking track is a finding, never a hold | `pmat comply` derived expiries (master §12) |
| poka-yoke | **100 %** of 0.66 tickets with an acceptance *command*; report baseline 0 % `[C]` (0 of 39) | this document §5; C9 |
| poka-yoke | **100 %** of guards with two observed REDs (mutation + discrimination) before merge | G-1, B-G1, R-0 PR bodies |
| poka-yoke | refusal table **derived**, never authored: `|rows| = |builds| × |host device facts| × |BACKEND_VALUES|`; a fourth `BACKEND_VALUES` entry grows the table with no edit | R-0 |
| jidoka (design) | **0** `cfg!(feature = "cuda"\|"wgpu")` reads in `apr-cli` backend decisions; **1** printed line per registered backend on every start; dogfood `backend` == registry entry on **4/4** hosts | C11; R-0; R-2 |
| poka-yoke (discovery) | **14/14** REG requirements with a committed fixture; **5** kinds printed on every host, Ready or with a reason; **0** silent downgrades (every fallback is a printed `selected: … (reason: …)` line); **0** runtime compilers or extracted payloads; **0** `cudart` links | R-0 failure catalogue; FX-1..14; S0-20 |
| genchi genbutsu (discovery) | memory read from the device at discovery **and** at admission; reserve printed with `basis=`; unified-host arithmetic from S0-21, not from a discrete-GPU assumption | REG-6, REG-7 |
| kaizen (ratchet) | backends the registry can print Ready: cuda → cuda + wgpu (0.66, R-0) → + metal (0.67, B-M1); a kind is added to `BACKEND_VALUES` only with its registry entry in the same PR | B-G1 clause 3; #2841 class |
| genchi genbutsu | `compute_class` in `perf-matrix.yaml` is **defined as** the registry's printed value on that host — a declared class the host does not print is a guard failure, not a spec-owner ruling | B-A1 A; PP-16 |
| heijunka | **≤ 1** speed row in flight per host; gx10 = one queue ordered by expiry | §2; master §9 |
| genchi genbutsu | **23/23** Step-0 premises answered by a pasted command output before ticket #1 (`docs/audits/pp-066-s0-ledger.md`, 2026-09-05) | S0 ledger |
| genchi genbutsu | every training number from a receipt in `evidence/train-parity/`; **0** numbers from README summaries (the canary README's 2.5× vs its table's 0.13× is the live example) | S0-9; T-0 |
| kaizen (ratchet) | `check_crate_names.sh remaining`: 51 → **51** in 0.66 (guard lands, ratchet armed), → 0 across 0.67+ one PR per crate | G-1; G-3 sizes it |
| kaizen (ratchet) | `check_backend_firstclass.sh`: cuda 4/7 → **6/7** in 0.66 (clauses 2, 5); 7/7 only with a CONFORMANT receipt | B-G1 |
| kaizen (ratchet) | `check_doc_citations.sh UNVERIFIED` count: down-only from the first receipt | D-1 |
| instrument-first | τ_loss, W5 `n`, T-0 time budget, `max_batch` floor: **0** typed thresholds; each carries `basis=` or `[U]` | T-1, R-4, T-0h, §7 last row |
| five whys | every S1 finding in §1 terminates in a mechanism, not a person | §1 root-cause column |
| poka-yoke | **0** DAG invariant violations (cycles, zero-slack pairs, queue-vs-expiry inversions) at every push — the review found three by hand; G-4 finds them by machine | `check_dag_invariants.sh` in `ci / gate` |
| genchi genbutsu | guard universe from `cargo metadata`, **0** manifests classified by glob; every stray manifest either a member or in the registry | G-1 layers (1), (2) |
| instrument-first | T-0 config ladder descended by a pilot, **0** rungs asserted; requested ≠ effective ⇒ `INVALID-CONFIG` | T-0h, T-2 |
| jidoka (contracts) | **100 %** of PP-066 contracts' `test:` fields executable and in the strict set; **0** `registry: true`; **0** hand-typed proof credit; **0** new Grade F in kernel/pattern scope (score REPORTED per contract until PVI-2.6 ratchets) | C12; `check_contract_test_binding.sh` baseline 13 → may not grow |
| genchi genbutsu | proof credit **8 / 8** Lean, **47 / 47** Kani — discharged equals declared, or the declaration is deleted (PVI-D-5) | P-0.3; PVI-2.1/2.3 (0.67) |
| poka-yoke (release) | **5/5** targets with asset + sha256 + signature on every tagged release; **4/4** host receipts before `prerelease=false`; installer exit 0 ⇒ registry printed | C13; R-5 promotion job; R-6 case table |
| andon (release) | the prerelease flag is the cord: **0** releases promoted by hand | R-5 contract (ii) |

---

## §10 Verification ledger for this document

| claim | mark | source / command |
|---|---|---|
| Report line refs (`accel.rs:28`, `dispatch.rs:173`, `serve/mod.rs:236`, `commands_enum.rs:10`, `handlers.rs:912-953`, `Cargo.toml:217,220,100`, `kv_cache_gpu_init.rs:28-62`, `cublas_prefill/mod.rs:29-34`, `attention.rs:995,1022`, `gpu_profile.rs:517-531`) | `[A]` at `587ad0797` | S0-7 re-verifies via `pmat query` at implementing HEAD |
| Ceilings 215 / 58 tok/s; 74 % / 48 %; 3.4 ms; 9,476 MiB; 0.13×; KV 448 MiB/slot; 2.83× comparator bar; 5,700 = 0.55× | `[C]` | arithmetic in §1 F-11, F-14; S-2 note; measured inputs: evidence/parity-http/findings.json (comparator prefill median = 10,399 in that receipt); the 0.13× inputs (2,101 / 16,118) (UNRECEIPTED [C] — provenance only: evidence/reports/0.66-parity-report-provenance.json) |
| Master is v3.1 and §12 has row 22 (line 364) | `[V]` (S0-1, 2026-09-05) | `docs/specifications/PP-LLAMA-001-MASTER.md` — the tracked file; v1.5 read an untracked `-v3.md` copy and said "no row 22" |
| Master expiries rows 19/20/21 = 10-16 / 10-23 / 11-06 | `[V]` | same |
| Unsloth throughput race conceded 2026-07-05 | `[V]` | `fable-architectural-review.md` §7c; `aprender-pillar-vision-v2.md` §P3 |
| `intel` = Xeon W-3245, 283 GB, clean-room runner, Linux (systemd/ufw/Docker) | `[V]` | guide §3 host table, §4.4 |
| `intel` carries a discrete AMD GPU | `[U]` | S0-3 |
| lambda is/is not a self-hosted runner | `[U]` (skill says retired 2026-05-10; report says `cuda-nightly.yml` runs there) | S0-5 |
| Report §11 expiry slack ≥ 6 days on the DAG as dated | `[C]` | pairs 31→35, 14/15→18, 22→26, 20→22 |
| `apr-cookbook`, `qwen-train-canary`, `pmat` depend on old `[lib]` names | `[A]` | `fable5-pmat-roadmap-prompt.md` (`aprender-*`/`batuta-common`); G-3 measures |
| Review tree facts adopted here: `error.rs:106` FeatureDisabled → 9 (v1.5: `:86-90,218`); `finetune.rs:717` wgpu-pipeline clamp to 512 (the instruct path honours the flag since #2247; v1.5: `:317,718`); `accel.rs:114` three-surface test; 13 MSL *source strings* + a placeholder `MetalBackend` in `aprender-gpu` (S0-12: no dispatcher); `Q4K_GEMV_SHADER` in compute; `OwnedQuantizedModelWgpu` stub; `aprender-serve` `#[cfg(not(wasm32))]`; 106 manifests under `git ls-files '*/Cargo.toml'`; 8 auxiliary Spanish-named manifests; 4,097 `use` sites; `aprender_ml` alias at root `Cargo.toml:509` | `[A]` from `0_66-review.md` (explorer at `d6c6c6f8`, spec at `587ad0797`) | S0-10..S0-13 and G-3 re-run them to `[V]`; the review's own segment reviewers had no tree access and their numbers are `[A]` until the explorer's or ours reproduce them |
| 30.4 GB f32 expansion of 7.61 B params; 23 GB f32 activations at seq 2048 / mb 4 before scores; 4.00 GiB wasm32 linear memory; 2.83× comparator scaling; ~127 / ~113 per-prefill copies / allocs | `[C]` | 7.61e9 × 4; 7 × 28 × 4 × 2048 × 3584 × 4 B; 2³²; 484.7 / 171.5; 1,018 / 8, 904 / 8 |
| PV-IMPROVE-001 numbers (1,807 files; 343 kernel; 500 `registry: true` (S0-18; the doc said 481); 0 err / 1,082 warn / threshold 0.00; 181 / 8 Lean; 1,257 / 47 / 0 Kani; 10 / 10 dangling `#[contract]`; 379,866 lines; ≈ 95 % vacuous; 329 / 688 empty call sites; 2,474 / 2,704 prose tests; two binding registries; upstream archived 2026-04-06) | `[A]` at `b1a6324b8`, measured by its author with a tree-built `pv` | S0-18 reproduces the three cheapest |
| #2869 facts (no `apr-*` asset on `v0.65.2`/`v0.64.0`; `binary-release.yml` scoped to `pv`; `nightly.yml` builds 5 targets to the `nightly` prerelease; README install = `cargo install` only) | `[A]` from the issue as fetched 2026-09-05 | S0-16 |
| PP-LLAMA-001-MASTER.md is committed on `main` | `[U]` — review segment 2 asserts it is uncommitted at the explorer's checkout | S0-1 |
| Every 0.66 card's `A:` script exists at HEAD | `[U]` | each card's first phase is "the script exists and its case table runs"; `scripts/perf_gate.sh`, `check_multiplatform_dogfood.sh`, `check_no_claim_literals.sh`, `llama_pin.toml`, `perf-matrix.yaml` are `[A]`; `perf_ab.sh`, `perf_w4.sh`, `perf_w5.sh`, `train_parity.sh`, `check_backend_firstclass.sh`, `check_crate_names.sh`, `crate_rename_cost.sh`, `check_backend_refusal_surfaces.sh`, `check_dag_invariants.sh`, `check_doc_citations.sh` are **new in their tickets** |

This document is a snapshot at 2026-09-05 (v1.6). Where it disagrees with HEAD, HEAD wins; where it disagrees with
PP-LLAMA-001-MASTER §4–§7, the master wins; where it disagrees with the report, this document wins only
where §1 names the finding.

---

## §11 Adjudication of `0_66-review.md` (six reports, 2026-09-04)

Verdicts: **SUSTAINED** (finding correct, disposition applied) · **NARROWED** (a true fact wrapped in a wrong
conclusion; the fact is kept, the conclusion is not) · **REJECTED** (with the reason) · **INDETERMINATE** (needs
a Step-0 command). Only the explorer segment had tree access; segment reviewers' tree claims are `[A]` via it.

| review id | verdict | why | disposition here |
|---|---|---|---|
| MAJ-01 wasm32 4 GiB | **SUSTAINED**, narrowed on the fix | 2³² B is the bound; the report's `W1` on wasm is a 7B model it cannot address. The review's proposed "≤ 2B parameters" line is an invented threshold | §6 B-S: bound computed per receipt; cell key `W1_0.5b`; clause-6 exclusion enumerated; B-S1 re-scoped as a port (serve is `cfg(not(wasm32))`) |
| MAJ-02 30.4 GB f32 on wgpu | **SUSTAINED**, one error struck | arithmetic holds; the f32 path cannot serve 7B on any proof host. The review's "lambda 24 GB VRAM OOM" and "intel Vulkan iGPU" are its own assumptions — the report never puts wgpu W1 on lambda, and `intel` has no iGPU (Xeon W-3245); what it has is S0-3's question | §6 B-W0 prerequisite; `Q4K_GEMV_SHADER` already exists in compute (`[A]`), so B-W0 is wiring. #2826 measured wgpu-on-Metal decode at 0.123 tok/s (8.14 s/token) on mini — the f32 path was run; B-W0 confirmed as prerequisite (issue-sourced figure, UNRECEIPTED, provenance only: evidence/reports/0.66-parity-report-provenance.json) |
| MAJ-03 W-H must block row 17 | **NARROWED** | True: the report's §8 clause 1 ("PP-26 PASS on every band") plus row 17's blocker set is self-contradictory, and W-H expiring on the tag day is zero slack. False: pulling a kernel-epilogue rewrite into the release criterion is the wrong repair. The master's L0 layer already handles this — a band that fails PP-26 is `INVALID-CORRECTNESS`, unarmed, printed. W-H blocks **arming c=16 on gx10**, not the receipt | §4 C1 arm-what-passes; §8 clause 1 replaced by validity-by-band; W-H stays 0.67; G-4 makes the slack rule mechanical |
| MAJ-04 gx10 queue | **SUSTAINED** (= F-10) | W-G before W-B violates its own blocker; T-0 (09-26) behind W-E (10-10) | §2 one gx10 queue ordered by expiry; G-4 asserts `queue_pos ⇒ expires` order |
| MAJ-05 T-0 falsifier "inverted" | **NARROWED** | §10's column is a *prediction* kill (report L565: re-plan from the measurement), not a track kill — the review read "kill" as cancellation. The column header did permit that reading | §7 column relabelled; T-0 row rewritten with the ladder |
| MAJ-06 T-0 cannot run at seq 2048 on 24 GB | **SUSTAINED**, the "cyclical" conclusion **REJECTED** | ≈ 23 GB of f32 activations before scores `[C]`; and the wgpu pipeline (`finetune.rs:717`) silently clamps to 512, which would make an apr receipt on that path lie about its config — the more serious half. Not cyclical: parity is same-config, so the baseline is the highest rung **both** complete, found by a pilot, with Unsloth's own max REPORTED | T-2 (honour-or-refuse), T-0h ladder + effective-config witness, memory prediction registered |
| MAJ-07 in-crate alias fails E0432 | **SUSTAINED** | extern-prelude resolution; `pub mod trueno` inside `aprender_compute` serves `use aprender_compute::trueno::…` only | §6 renames: shim package only; announced break with the `package =` migration line; §8 refusal |
| MAJ-08 guard universe = 106 manifests | **SUSTAINED**, the fix re-derived | `git ls-files '*/Cargo.toml'` hits `[workspace]` shells, fuzz, wasm-pkg, bins. The review's fix (parse `workspace.members`) drops the untracked-crate defense the report rightly wanted | G-1 two-layer universe: `cargo metadata` members ∪ facades workspace; strays must be registered or FAIL |
| MAJ-09 4,097 use sites | **SUSTAINED** as a measurement (`[A]` until reproduced); phasing → **D-8** | the count is exactly the UNMEASURED the report named; batching leaves vs one-PR-per-crate is a doctrine call, not a reviewer's | G-3 re-runs to `[V]` with two estimators; D-8 default written; realizar alone in a freeze window |
| MAJ-10 dangling `§7.3`/`§9 #n`/`§12 row n` | **SUSTAINED**; the "master is uncommitted" claim **INDETERMINATE** | unqualified copies from the master; if the master is not on `main`, PP-29's conformance script has nothing to parse | S0-1 extended; I-00 conditional ticket; this spec prefixes every master reference |
| MIN-01 ceiling formula inverted | **SUSTAINED** (typo; the numbers were right) | `bytes / (bytes/s)` is seconds | report fix; §1 F-11 already computed it correctly |
| MIN-02 4/7 vs five ✓ | **NARROWED** | clause 4 says the witnesses *run*; they run nightly and one band fails — that is L0 doing its job, not a clause miss. The hand count is exactly what B-G1 replaces | B-G1 derives the score; §7 prediction "6/7 after R-1, R-2" |
| MIN-03 nsys totals are per-session | **SUSTAINED** | 1,018 / 904 span startup + 8 requests; ~127 / ~113 per prefill (receipt: evidence/parity-http/findings.json `fixed_cost_root_cause.api_time.cuMemcpyHtoD_v2.calls` = 1018, `cuMemAlloc_v2.calls` = 904) | S-2 wording and contract |
| MIN-04 asymmetric refusal | **SUSTAINED** (= report §1.5 D) | already the reason for R-1 | — |
| MIN-05 13 MSL kernels + `MetalBackend` via `manzana` | **REJECTED (v1.6, S0-12)** | source strings and a placeholder, not kernels: no `metal` feature, no dispatcher, `manzana` removed by #2849; the report's "no Metal backend" was right in substance | §6 B-M; S0-12; the kernels are a third family and a PP-26 liability before they are a speed-up |
| MIN-06 W-E 3× vs 2.5× | **SUSTAINED**, target re-derived | and the comparator itself scores 2.83× (F-14), so "3×" was a bar the incumbent fails | §6 W-E: target ≥ comparator's own ratio, kill < 2.5× |
| MIN-07 exit code 9 vs 2 | **SUSTAINED** → **D-7** | one refusal code exists; the report invented a second | tests read the constant; §8 refusal |
| MIN-08 `apr chat` omitted | **SUSTAINED** | `accel.rs:114` already tests three surfaces | R-1 enumerates from `--help`; floor ≥ 3 names them |
| MIN-09 em-dashes in tables 1.1/1.2 | **SUSTAINED** (report fix) | values exist in `findings.json:184-188` and `bands/*.json` | report populates; not carried here |
| MIN-10 MCP endpoint as oracle | **SUSTAINED** (= F-16) | — | D-1 vendored pinned docs |
| MIN-11 feature-string cascades | **SUSTAINED** | `trueno/gpu`, `dep:realizar`, `apr-cli/trueno-explain` | G-1 counter (c); S0-10 |
| MIN-12 G1 paths | **SUSTAINED** (report fix) | `model.write()` is in `api/cuda_batch_scheduler.rs:248,314` `[A]` | S-1 card cites both files at implementation time |
| seg-2 2.1 §8 "DONE iff both hosts" conflates workstream and cell | **SUSTAINED** (already separated in v1.0) | each card has its own `A`; PROVEN is a cell property | §4 C1 vs §5 cards; no change |
| seg-2 2.4 no cuBLAS NF4 GEMM | **SUSTAINED** | T-3 as written is contradictory; the 15.5 tok/s PTX path (entrenar-spec §perf; UNRECEIPTED [C] — provenance only: evidence/reports/0.66-parity-report-provenance.json) is the only non-materialising route today | §6 T-lane: kernel design + registered prediction before code |
| seg-2 2.5 backward kernels for fused ops | **SUSTAINED** | forward launch count is half the cost | §6 T-lane |
| seg-2 3.1 graphs freeze shapes | **SUSTAINED** as a design constraint | per-bucket graphs or padding; cuBLAS bypass under capture is already in the tree | §6 W-E decision named in the card |
| seg-2 3.2 / 3.3 KV quant beyond f16, paged attention | **REJECTED** for 0.66/0.67 | scope suggestions with no measurement showing KV as the binding constraint; EV rule | §8 refusal; re-opens on a measured `max_batch` finding |
| seg-2 3.5 T-6 not a T-7 blocker | **SUSTAINED** | a gate that can arm over a false banner | T-3 blockers += R-3 |
| seg-3 F3 WGSL limits (128 MiB binding, optional subgroups, no f32 atomics) | **SUSTAINED** as constraints; "guarantees failure" **REJECTED** | B-W4's losing branch is a recorded outcome by design | B-W0 chunking + `SUBGROUPS` grant recorded |
| seg-5 F5 13 workstreams absent from §10 | **NARROWED** | §10 registers *numeric predictions*; refusal/guard rows have falsifiers (mutations), not predictions | every card carries a mutation; §7 only where a number is predicted |
| seg-5 F7 / RB-05 staffing "Lead A/B/C, QA Lead" | **REJECTED** | there is no such org; the owner identities are lanes executed by paiml-implement under one approver. The real capacity constraints are the Fable weekly cap, one gx10, one merge queue | §2 WIP caps; `K̂` `basis=` from `impl-estimates.jsonl` after the first three tickets; no invented org chart |
| RB-04 wgpu "killing row 35" | **NARROWED** | wgpu not being promoted is the falsifier working; row 35's 7/7 claim was the theater | B-G1 derives the score; nothing is killed by a measurement |
| Rec 1 `check_dag_invariants.sh` | **SUSTAINED** (adopted) | the one recommendation that is a gate rather than a re-wording | G-4 |
| Rec 8 `docs/references/cuda-12.6/` offline docs | **SUSTAINED**, path changed | vendored **pinned by sha256** under `evidence/`, cited by artifact; version chosen by the CUDA the fleet runs (gx10 CUDA 13.0 `[A]`), not 12.6 by default | D-1 |

**Five findings no reviewer raised** (standard deliverable) — a sixth, found 2026-09-05 by five whys after the 0.65.2 post-publish measurement: the GPU defect is a design gap (no runtime backend registry), not a default-feature setting; every reviewer, the report, and v1.0/v1.1 of this spec treated it as configuration (F-25, R-0) — and two from the same day's inputs: #2869 shows there is no release artifact for a gate to bind to (F-27), and PV-IMPROVE-001 shows the contract-per-card requirement in v1.0–v1.2 was checked by a tool that cannot fail (F-26). Both are the "producer is the gate" class wearing different clothes.

**Five findings no reviewer raised** (v1.1):
1. The training parity *target* contradicts a standing concession (`fable-architectural-review.md` §7c) — D-2. Six reports treated T-7 as a schedule problem; it is a doctrine problem.
2. Row 17's blocker set contains no instrument row from the master (0a–0e, 6, 7, 12, 15) — the reference cell cannot exist until six speed rows land, and no `AbRecord` can reconcile (F-3). MAJ-03 argued for *adding* a seventh speed row to that set.
3. Three master expiries were moved earlier without an amendment (F-3b / D-4).
4. `intel` is the Mac Pro 2019 chassis; "no AMD host in the fleet" is a memory-derived claim, not an `lspci` fact (F-18 / S0-3). The review repeated "no AMD device" from `perf-matrix.yaml` — a file that lists hosts, not silicon.
5. The rename guard's registered mutation was vacuous at landing (F-19); the review corrected the universe and kept the mutation.

---

## §12 Prior-art register — GPU discovery in three shipped systems

Source: a third-party comparative synthesis supplied 2026-09-05 (`[X]`: excluded from any published claim; every row must be re-verified against the named project's current source or docs before it is cited in `docs/specifications/cuda-backend-architecture.md` — D-1's citation rule applies). Two rows are already known to be dated and are marked. Guide §5.1.8 satisfied: three world-class systems, lessons adopted or rejected with a reason, each adopted lesson bound to a REG-id and a fixture.

| system | mechanism (as described) | what apr adopts | what apr rejects | REG |
|---|---|---|---|---|
| **llama.cpp / ggml** | C backend registry (`ggml_backend_register`, `ggml_backend_dev_count`, `ggml_backend_dev_get_props` → name, `memory_total/free`, `caps{async, unified}`), buffer types with alignment/max-size; `-ngl` manual offload; VirtGPU/APIR proxied backend across the hypervisor. **Dated in the source:** "must be compiled with `nvcc`; pre-built binaries fail on `libcudart` mismatch" — current ggml ships `GGML_BACKEND_DL` dynamic backends (`load_backend: loaded CUDA backend from …/libggml-cuda.so`, verified 2026-09-05 search); the cudart-vs-driver mismatch class is real for the CUDA backend but the loading is dynamic | registry-as-the-only-authority; per-device props with memory and caps; extensibility by interface (the VirtGPU precedent) | fatal abort on driver mismatch (REG-1); `cudart` dependency (REG-2); hand-tuned `-ngl` as the user's job (REG-14, 0.67) | 1, 2, 5, 6, 13, 14 |
| **Ollama** | Go control plane discovers before the engine runs (`gpu.go`, NVML dlopen with soft dependency, `/sys/module/amdgpu`, GTT = 0.5 × (RAM − VRAM) on APUs — a `[X]` reading of a third-party source, UNRECEIPTED, provenance only: evidence/reports/0.66-parity-report-provenance.json); picks a runner variant (`cuda_v12`, `rocm_v6`, `cpu_avx2`); computes `num_gpu` layers from measured free VRAM; `OLLAMA_GPU_OVERHEAD` reserve; `OLLAMA_LLM_LIBRARY` override; API stays up on driver failure. **Possibly dated:** runner *extraction to `/tmp`* describes older releases; current builds bundle `libggml-*` under `lib/ollama` `[U]` — the antivirus-quarantine lesson stands either way | control-plane-first discovery; measured-memory admission; explicit override that prints; API survives a broken driver; per-process VRAM accounting via NVML | payload extraction (REG-3); a reserve knob that silently forces CPU when mis-set (REG-7); one vendor's failure gating another (#11849, REG-4); Intel opt-in-or-invisible (REG-4); VRAM cache that goes stale until restart (REG-12); per-GPU silos vs split left unmeasured (REG-9) | 1, 3, 4, 6, 7, 8, 9, 12 |
| **llamafile** | Cosmopolitan APE, weights zipped into the executable; probes `$PATH` for `nvcc`/`hipcc`/Xcode at first run, JIT-compiles `ggml-cuda.so` / `ggml-rocm.so` / `ggml-metal.dylib` into `~/.llamafile`; tinyBLAS SGEMM in PTX to avoid shipping cuBLAS; Metal cap = RAM − safety buffer; **silent CPU fallback** unless `--gpu nvidia\|amd\|vulkan` forces a loud failure; size-optimised CUDA lib drops IQ-quant kernels (split placement, unannounced) | own PTX GEMV (apr already has this — no cuBLAS dependency for decode); unified-memory cap as RAM − reserve; the *existence* of a loud-failure flag | silent fallback as the default (REG-8, REG-11); runtime compiler dependency (REG-3); silently splitting placement when a kernel is missing (REG-5: a missing capability is printed and changes the path visibly) | 3, 5, 6, 8, 11 |
| **all three** | none prints a fixed-shape device list with absent backends as lines; none makes the reserve's basis visible; none exposes "which prefill path and why" | the printed block (R-0) — five kinds always, `selected:` always, `basis=` always | — | 5, 7, 11 |

Where apr is ahead by construction: driver-API-only via `libloading` (no cudart), Rust-generated PTX (no toolchain at run time, no vendor BLAS for decode), one static binary. Where apr is behind: everything above the kernel layer — which is R-0.

## Appendix A — Changelog

| version | date | change |
|---|---|---|
| 1.6 | 2026-09-05 | The 14 defects of the S0 ledger (`docs/audits/pp-066-s0-ledger.md`) and the #2872 review: F-9/§10 master is v3.1 with row 22; §3 commands that could not run (S0-1 bold row, S0-2 `--status all`, S0-12 `metal`, S0-15 `cli` on apr-cli); 23 premises; snapshot date; `error.rs:106`; `finetune.rs:717` (instruct path fixed by #2247); S0-13's non-existent `handlers.rs`; `\|` escaping note + C1 as a command; R-6/§8 public-key scheme, "no third scheme" (S0-19); `registry: true` 500; S0-12/B-M/MIN-05 no Metal dispatcher (#2849); Track I carded in the DAG, Appendix E has no `pmat work add` lines; Track P sourced from #2870; §4 andon expiry parse (I-26). **§4 "credited first" given exactly one reading** (falsifier `tests/spec/pp066_v16_defects.sh`). Expiries: P-0.6 → 09-26, T-0 → 10-03, P-1.1 → 10-03, I-17 → 10-16. G-4's checker is in-repo. |
| 1.5 | 2026-09-05 | F-28, S0-23, C0 (`pmat comply` at HEAD: CB-2100 required check unreadable, CB-1700 `strict=false`, CB-1701 no blocking cargo-deny, CB-1658; #2830 polarity); C0 credited before any other C-row; §6 B-W0 and §11 MAJ-02 carry #2826's wgpu-on-Metal measurement (no "wgpu never measured" sentence existed at v1.4 to strike); claim-literal citations on F-11, F-27, S0-9, S-2 note, T-1..T-5, §10, MIN-03, seg-2 2.4, §12 Ollama (`evidence/` receipts or the report's UNRECEIPTED marker; provenance index extended with the spec's lines); driver prompt v1.5 (epic #2873) |
| 1.4 | 2026-09-05 | **GPU discovery story**: R-0 gains REG-1..REG-14 with a 14-fixture failure catalogue (`registry_failure_catalogue`), `apr-devices-schema-v1.yaml`, and the normative printed block; §12 prior-art register (llama.cpp/ggml, Ollama, llamafile; `[X]`, two dated rows flagged); S0-20 (no cudart), S0-21 (unified-memory `cuMemGetInfo` on gx10/mini), S0-22 (multi-GPU facts); C11 extended; §7 two predictions; §8 four refusals; §9 two targets |
| 1.3 | 2026-09-05 | **#2869**: F-27; D-10 per-target features; R-5 release assets gated by prerelease promotion + host receipts + H12 on the asset; R-6 verifying installer ending in `apr devices`; R-7 README order; C4 re-based on the release asset; C13. **PV-IMPROVE-001**: F-26; D-11 (phase 0 + 1.1/1.2 in 0.66, rest under #2556); contract-discipline block at the top of §5; Track P (P-0.1..0.6, P-1.1, P-1.2); C12; G-4 as a `pmat comply` rule and D-1 on `claims-cite` (principle 8); S0-16..19; §6 PVI row; §7 R-5/P-0.3 predictions; §8 five refusals; §9 four targets; §10 two ledger rows |
| 1.2 | 2026-09-05 | **R-0 backend registry** as ticket #1 (F-25 five whys: design, not configuration); R-1 retired into R-0; R-2 reshaped around D-9 (cuda in defaults iff S0-14) and moved 09-12 → 09-26; B-G1 moved 09-26 → 10-03 (blocked by R-0/R-2), clauses 2/3/5 derived from the registry; D-9; S0-14, S0-15; C3 rewritten, C11 added; B-A1's PP-16 oracle = `apr devices --json`; §6 B-W0/B-W1/B-M1/B-S1 re-pointed at R-0, `metal` Ready-by-registry rule; §7 R-0 and S0-14 predictions; §8 two refusals; §9 three targets; §11 sixth finding |
| 1.1 | 2026-09-04 | §11 adjudication; D-7, D-8; S0-1 extended, S0-10..S0-13; C3/C4 exit codes derived, C10 DAG invariants; R-1/R-2/R-3 exit code from `error.rs`; S-2 nsys per-session; T-0h effective-config witness + memory prediction + ladder; new T-2 (`--max-seq-len` honoured-or-refused); T-3 blockers += R-3; G-1 universe re-derived (cargo metadata ∪ facades; stray registry; three counters; three mutations); G-3 measured 4,097 `[A]`; new G-4 DAG-as-data guard; §6 B-W0, B-S scale bound, B-M third family, T-lane kernel-design gate, W-E graphs and target, renames shim-only + D-8; §7 column relabelled, T-0 row; §8 four refusals; §9 three targets; §10 ledger rows |
| 1.0 | 2026-09-04 | initial rewrite of the 0.66 report into paiml-implement units |
