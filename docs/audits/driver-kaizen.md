# Driver kaizen — one line per prompt defect met

- 2026-09-06: a capped paiml-impl-worker keeps its slot lock until it is told to stop (the hook proves pid liveness against the HARNESS pid, which never dies mid-session); every orchestrator push/PR was refused until a SendMessage asked the worker for its receipt — the hook needs SubagentStop-on-cap, or the cap must count as a stop
- 2026-09-06: I12 `gh pr checks <n> --watch --fail-fast` exits immediately whenever an advisory check (present / pr-review-receipt) is failing, so it cannot serve as the blocking wait; a bounded until-loop on the run's or the guard job's conclusion is what works
- 2026-09-06: the delegate ran every N-lane and rescope quorum on ONE model family (Gemini 3.1 Pro ×3) although the driver requires three families; the brief must name the families and the delegate must refuse otherwise
- 2026-09-06: v4/v5 'nothing is written after a merge' met G-10a's receipt, which was partial when #3011 merged under v3; flipped by the docs commit once, applied from G-11a on
- 2026-09-06: the driver's G-10b baseline '281' is the pre-PR-A count; measured on the PR-A tip it is 243 — baselines are measured at the commit named, never carried from a prompt
- 2026-09-06: the L0-1 report was named 'verbatim' but was not in-tree; v5.1 named #2971 — the ticket IS the report; my #3017 was closed as its duplicate
- 2026-09-06: I13's write set does not name evidence/<row>/ (row records) or .pr/<row>/accept.sh; G-11b gitignores .pr/** except accept.sh and treats row evidence as row-owned
- 2026-09-06: C3 (--backend on every surface) moved to 0.67 while R-0b (the resolution) stays in 0.66: the rescope quorum read that as scope-fails until the spec text was re-read; the criteria table now says why
- 2026-09-06: the claims ratchet turned RED on the rescope record itself (it quoted the literals it was cataloguing); records cite file:line, never repeat a number
- 2026-09-06: the CI job stops at its first failing step, so one mutant commit with three guards needed three RED runs (one per guard) — one mutant per PR-run, or the case tables in separate jobs
- 2026-09-06: bashrs' multi-file ratchet counts a branch's INHERITED lint debt (G-11b based on G-11's pre-fix commit): re-cut onto main before judging the count
- 2026-09-06: apr-agent --help creates a worktree named --help (no flag parsing before the slug); removed by hand; a slug guard for leading dashes belongs in the launcher
- 2026-09-06: a Bash call refused by the subagent-lock hook aborts the WHOLE command line, including the file writes before the gh call — writes and gh calls in separate calls
- 2026-09-06 (A): a hotfix's DONE-IF must re-list PRs (`gh pr list --search <file/keyword>`) and `git log origin/main -- <file>`, not only issues — #3030 duplicated #3027's byte-identical dedup (I0 miss; both merge cleanly, #3030 carries the guard).
- 2026-09-06 (A): an instrument change is still a data defect when the new rule is right — pmat 3.39.0's duplicate-id rule exposed 12 stale nested subtask records; the fix was the data plus an in-repo pin-independent guard, never a pin downgrade.
- 2026-09-06 (A): the pre-commit complexity hook fails any staged file whose `pmat analyze complexity --file` expansion (the file, its `mod` children, its `include!`s) carries an over-threshold function — an edit to a clean line in a debt-carrying file is refused; plan the decomposition BEFORE writing into such a file (L0-1b's CPU tap sits in forward_single_with_cache 26/55 + single_cache_ffn_block 69).
- 2026-09-06 (A): measure the arms BEFORE building the instrument — seven env arms (7 s each) excluded every switchable GPU path in a minute; the per-op table then named layer 26 and a float64 recomputation from the dequantised weights named the SIDE (the CPU Q8_K reference), which no CPU-vs-GPU comparison alone can do.
- 2026-09-06 (A): `git checkout -- <file>` restores the INDEX, not HEAD — after a failed commit the index still holds the rejected edits; use `git checkout HEAD -- <file>`.
- 2026-09-06 (A): a three-family agy quorum needs `--model` per lane (agy-lane.sh cannot express it) and one lane can be a rubber stamp (cited a path that does not exist, named no falsifier) — count only lanes that read code; a delegate that hits its turn cap holds the worker lock, and every `gh pr` is refused until it returns its receipt (SendMessage resume once).
- 2026-09-06 (A): before designing a fallback, measure the criterion's distribution on the dump trees — per-block max/rms saturates at 16 (= √256) on ordinary blocks and cannot separate; max/second-largest separates the crushed first-token blocks (≥ 20) from every ordinary block (≤ 6).
- 2026-09-06 (A): a runner's PATH tool is not a pin — G-10a's pinned analyser became "no analyser at the pin" the moment the fleet moved PATH pmat; provision consumer pins as versioned roots on every runner host (infra#468 ask 4).
- 2026-09-07 (A): the pre-commit hook's `pmat analyze complexity --file` expands a file to its `mod` children and siblings — parallel_k.rs was refused for q5k_q6k_matvec.rs's debt; when the hot path is debt-carrying, route the change through a NEW module and a clean caller (quantize/direct_f32.rs + ffn_block.rs's choke point) and prove the routing changes nothing with a bitwise dump-tree oracle.
- 2026-09-07 (A): a zero-code env arm on the shipped binary (`DIRECT_FP32_GEMV=1`) was a stronger falsifier than any lane's reading — ask the delegate for the arm the code already carries before designing an instrument.
- 2026-09-07 (A): `git push` after `gh pr update-branch` is non-fast-forward — pull the update-branch merge first; `git pull` aborts when an untracked local copy exists of a file main now tracks (delete the copy).
- 2026-09-07 (A): NEVER bare `git stash` — the stack is shared across worktrees; I stashed R-0b's 16-file WIP and it landed at stash@{0} above four other worktrees' entries. Recover by SHA: `git stash apply <sha>` then drop that exact slot. Prefer a WIP commit.
- 2026-09-07 (A): the pre-commit complexity hook charges a staged file for EVERY function in its `include!` expansion (not just the file's own). lib.rs `include!`s the whole CLI, so any lib.rs edit is blocked by pre-existing debt three modules deep. Declare a new module from a hook-clean already-included file via `#[path = "x.rs"] pub mod x;` instead of adding `mod x;` to lib.rs.
- 2026-09-08 (A): a delta's `blocked_by:` splits two ways and the delta says which — an artifact the row *stays blocked by* is carded as its own DAG row with an owner, an expiry and a DONE-IF command (the signing key → KEY, #3045), because D1 refuses a blocker that is not a row; one the delta says the row *notes* stays a `blocked_by: <NAME>` note on the row it gates (the gpu-release runner group) and never becomes an edge.
- 2026-09-08 (A): `acceptance_criteria` is NOT a lifecycle field — G-6's additive guard admits only status/updated/notes/github_issue/labels/assigned_to/priority, so a delta that rewrites a row's A records it in the DAG and the issue and leaves the roadmap entry's `notes:` to point there; editing the criteria in roadmap.yaml turns a docs commit RED.
- 2026-09-08 (A): a `grep -rL 'cargo publish' .github/workflows` DONE-IF is defeated by a comment — all three matches in this tree are comment lines, so the probe strips comments and reads `run:` bodies; a text probe over a surface that carries prose about the thing it forbids must say which lines are executable.
- 2026-09-08 (A): run `render_dag.py --check` and `check_dag_invariants.sh` at session START — a row merged on main (#3020, G-11) flips its DAG-derived status, and the orchestrator branch's typed status and rendered block are RED before the session writes a line; the next docs commit carries the flip.
- 2026-09-08 (A): an id is TAKEN the moment a branch stamps it, minted or not — PMAT-1078 was free in every roadmap yet `agent/R-5`'s `contracts/apr-publish-cascade-v1.yaml:4` already carried it; grep the sibling worktrees and their branches, not only `docs/roadmaps/roadmap.yaml`, before minting (pmat#1169 class).
- 2026-09-08 (A): a delta clause can carry a false premise — "sm_89 SASS in the asset" is unmeasurable here because apr generates PTX text in Rust and the driver JIT-compiles it (cubins land in `~/.cache/trueno/ptx`, never in the binary); re-base the clause on what the artifact actually is and keep the intent, rather than writing an A nobody can run.
- 2026-09-08: TWO GUARDS DISAGREED ABOUT ONE FILE AND THE FIRST FIX SATISFIED THE WRONG ONE. `check_roadmap_diff_additive.sh` called PMAT-1077 "re-serialised", so `roadmap_trim.py` restored BASE ORDER — and base order then broke `check_roadmap_sorted.sh` ("PMAT-1069 sorts before PMAT-1077 earlier in the file"). Neither ordering was the cause: `classify_pair` compares the entry BLOCK, a block runs to the next `- id:`, and a blank line before an appended entry put a trailing empty line inside its predecessor's block. One blank line. When two guards disagree about one file, neither verdict names the cause — diff the bytes the guard actually compares.
- 2026-09-08: `. scripts/apr_bin.sh` DOES NOT HONOUR `CARGO_TARGET_DIR`. It derives its candidates from `cargo metadata`'s `target_directory`, which `.cargo/config.toml` redirects, so a binary freshly built into an exported CARGO_TARGET_DIR is reported STALE with a "foreign" candidate list. `APR_BIN=<path>` is the documented pin and works; it is still a pin, not a bare PATH lookup.
- 2026-09-08: EVIDENCE NEED NOT CARRY A MACHINE PATH — INVOKE THE TOOL RELATIVELY. `check_hardcoded_paths.sh` refused +25 shipped paths, 24 of them `"model": "/home/noah/models/…"` inside `apr parity --json` output. Re-running as `cd ~/models && apr parity ./<model>.gguf` with the SAME pinned binary produced records whose `metrics` array and every other key are byte-identical — only `model` differs. Neither a redaction nor an exemption: the path was never part of the measurement. Compare the two outputs field by field before claiming that.
- 2026-09-08: N IDENTICAL EVIDENCE FILES ARE ONE FILE PLUS A SHA256. The n=5 parity series was five BYTE-IDENTICAL copies per model (and identical to the canonical record) — `stdev 0` understates it; the whole file was the same file. Ten files per host became `DETERMINISM.md` with the hash and the distinct-count, which is a stronger claim and 20 fewer machine paths.
- 2026-09-08: A GATE'S RED LEG IS CHEAP — BUILD THE MERGE BASE IN A SECOND WORKTREE. `git worktree add --detach <dir> origin/main` plus a build into its own target dir took under two minutes with a warm cache, and turned "this gate would have caught the defect" from a claim into a measurement: same script, same fixture, two binaries, RED on main and GREEN on the branch with the three unaffected cells GREEN on both (so the gate discriminates rather than merely failing).
- 2026-09-08: A SECOND HOST CAN REFUTE A HYPOTHESIS CLASS BEFORE A LANE IS DISPATCHED. Measuring the 1.5B/7B parity pair on gx10 (GB10, sm_121, aarch64, running an sm_121-only JIT patch) landed within 2.2e-4 of lambda (RTX 4090, sm_89, x86_64). That single table refutes every "an sm_89 kernel is wrong" and "the sm_121 JIT corrupts a branch" hypothesis at once — the divergence is selected by the MODEL, not the device — and it cost one ssh and 90 seconds.
- 2026-09-08 (2): ASK OF YOUR OWN PR WHICH WORKFLOW EXECUTES ITS FALSIFIER. R-0b's regenerated tree-reader ledger grew by a line, and the line was `apr-cli --test backend_refusal_case_table` — R-0b's OWN case table, the five tests asserting a forced backend is never resolved to cpu, wired into nothing. Asking the same question of L0-1a found `reg15_admission` (card item (i)'s refusal proof) named by no workflow either: `grep -rc reg15_admission .github/workflows/` → no match. Both would have merged as theater. apr-cli carries 64 integration targets and ci.yml's gated line names 7 (#2341). One grep, per PR, over the targets the PR adds.
- 2026-09-08 (2): A NEW TEST-SELECTION TIER SURFACING A RED IS THE TIER WORKING. BSE-17's quick tier made `cli_integration` run on PRs for the first time and `test_qa_016_validate_quality_score` failed — on CLEAN main, reproduced in two worktrees. The test demanded the `/100 points` wording #2394 deliberately REMOVED as dishonest. Do not "fix the new tier"; fix the defect it found, and rewrite the stale assertion as a GUARD for the fix (the retired wording must be ABSENT) rather than deleting it.
- 2026-09-08 (2): AN ANCHORED GREP IS NOT AUTOMATICALLY THE SAFER ONE. Replacing a pass-grep (`| grep -qE "0 error"`, where `0 error` matches `10 error(s)`) with `grep -cE "^[[:space:]]*[^0-9]*\[error\]"` produced a leg that reported **0 on a script with a real finding** — bashrs prints `✗ 86:1-214 … [error]`, and the digits defeat the anchor. The anchor has to match the tool's ACTUAL output, and the only way to know is to run the predicate against a case that must fail. Keep both polarities in the receipt.
- 2026-09-08 (2): A SIGNING SCHEME MAY HAVE EXACTLY ONE KEY PATH. R-5 wrote and verified against `.github/release-assets.pub` while #3045 and the R-6 installer name `keys/apr-release-minisign.pub` — two candidate roots of trust, and the human action would have created the wrong one. `grep -rn <old path> .github scripts contracts` until empty, then say so on the issue that owns the human action.
- 2026-09-08 (2): WHEN A TEXTUAL GUARD FLAGS YOUR FILE, ALLOWLIST WITH A REASON — DO NOT REWORD. PERF-009's competing-harness predicate flagged `check_format_command_matrix.sh` through its own must-match fixtures (`0.0 tok/s` IS the defect row of its case table). Rewording to dodge the regex is the workaround PMAT-1074 already records as a known weakness of textual classifiers, and it leaves no statement of why the file is not a harness. The entry states the reason AND the condition for removing it ("if it ever derives a throughput or a ratio, delete this line"), plus a row in the detector's own case table.
