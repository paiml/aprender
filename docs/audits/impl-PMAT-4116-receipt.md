# PMAT-4116: 0.69.1 post-release follow-up (receipt)

PR #4115, branch `chore/0.69.1-follow-up-2`, stacked on the merge-back #4046 (`chore/0.69.1-merge-back`). The diff to judge is `origin/chore/0.69.1-merge-back...HEAD`.

## What is in it
- **merge** `origin/fix/prepub-skip-cfg-test-includes` @ 942437eca (f5), conflict-free: publish-preflight R4 (the cut's changes are contained in main, `--via <PR head>`), R7 (`--scope-tree`, `--crux DIR`), `scripts/release/r7_scope_judge.sh`, `scripts/release/adjudicate_known_red_receipt.py`, and `scripts/lib/lock_contained.py`.
- **merge** `origin/fix/ladder-emergency-crux-smoke` @ f19a3ee6d, conflict-free: `crux_smoke_scope.py` skips `*.meta.json` by name; `check_model_ladder.sh` gains the case.
- **cherry-pick** d5cecdee6: `book/src/cli/capability.md` (generated by `scripts/gen-cli-chapter-stubs.sh` against an apr pinned to HEAD; its example is `apr capability --json`, and its prose describes the three sections the JSON actually has: ops, quant_types, op_implementation), `contracts/apr-page-cli-capability-v1.yaml` (cloned from the qa page contract), and the SUMMARY entry between canary and cbtop.
- **6434f4585**: one bashrs SEC010 in the preflight's new `--crux` self-test fixture (`$tmp`, a `mktemp -d` of the script's own), suppressed per line with the reason above it. This is the class ruled a verified false positive on #4046.

## Measured on this tree
- `check_publish_preflight.sh --selftest`: 59/59 rows.
- `check_model_ladder.sh --self-test`: 147 cases, 0 bad.
- `check_bashrs_gate.sh`: PASS, 413 files, 0 SEC/DET/IDEM.
- The shell-lint ratchet, claim literals, fabricated baselines, perf-claim citations and tree-reader: all PASS.
- `check_book_cli_parity.sh` with APR_BIN = `apr 0.69.1 (6434f4585)` built from this HEAD: 113/113, PASS.
- `pv validate contracts/apr-page-cli-capability-v1.yaml`: valid. The page contract's three falsifiers (exists, mentions `apr capability`, has a bash block) pass.

## Not in it
- No crate code changes. f5's and 8b6b78's commits are carried as they were reviewed on their branches.
