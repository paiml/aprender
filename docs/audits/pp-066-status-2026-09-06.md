# PP-066 status — 2026-09-06 (PHASE 0 reconcile)

Epic #2873 · spec `docs/specifications/PP-066-release-spec.md` (v1.6 on main via #3000) ·
DAG `docs/specifications/pp-066-dag.yaml` · driver prompt of 2026-09-06 · origin/main `027ed889d`.
Every number below names the command that produced it; re-run the command, never quote the number.

## 0. Verdict

- **origin/main is not green; it is vacuously green.** The whole-tree machine-specific-path
  ratchet ran on main under an analyser that cannot run it and reported `SKIPPED (proven)`.
  The runner's analyser flipped between the last main run and the first PR run of the day,
  and every PR run since is RED on the same step. Root cause = the instrument, not the tree.
  Ticket **PMAT-1059** (DAG row G-10, #2999); fix branch `agent/G-10` (PHASE 1, PR-A).
- **Nothing is unpushed except `agent/G-10`** (5 commits, deliberately held: it is PR-A).
- **C0 is uncredited.** `scripts/release_criteria.sh` does not exist on main
  (`git ls-tree --name-only origin/main scripts/release_criteria.sh` → empty); every C0–C13
  is `[U]` until SPEC-1.6's executability fold lands it (PHASE 3) and `release_criteria.sh C0` exits 0.
- Decisions D-2…D-11 recorded on #2873 today (one comment each, `decided_by: noahgift via driver prompt 2026-09-06`).

## 1. Main: the ratchet evidence (cited from the job logs)

| run | branch | started | analyser line | ratchet outcome |
|---|---|---|---|---|
| `34011762858` | main @ `027ed889d` | 2026-09-06T04:35Z | `pmat: /home/noah/.cargo/bin/pmat (pmat 3.31.0)` | `SKIPPED (proven): pmat 3.31.0 cannot run this analysis.` → step success |
| `34018449576` | PR #3008 (I-25) | 2026-09-06T07:53Z | `pmat: /home/noah/.cargo/bin/pmat (pmat 3.37.0)` | `pmat is capable (pmat 3.37.0) -- the ratchet is ARMED and blocking.` → `FAIL: shipped machine-specific paths grew 277 -> 317.` |

Derive: `gh api repos/paiml/aprender/actions/jobs/<job id>/logs | grep -E 'pmat: |SKIPPED|ARMED|FAIL: shipped'`
(job ids `101428716226` and `101446481475`, job `guard-runner-labels`).

What the PR run counted: 317 findings over 14,769 files — the top files are `evidence/dogfood/0.65.2/lambda.json` (19),
`evidence/spec-review/0.66/plan-quorum-receipt.json` (8), `crates/aprender-serve/examples/imp_1010_full_cuda_benchmark.rs` (6).
None of them is in any armed PR's diff: the PRs inherit main's 317 under the new instrument against a
baseline (277) that was stamped under no instrument at all. The baseline file carries no `pmat_version:`.

**Clean origin/main under the armed analyser is RED by the same mechanism.** Measured locally on a
pristine `origin/main` worktree with the pinned binary: `bash scripts/check_hardcoded_paths.sh --full`
under `~/.local/pmat/3.37.0/bin/pmat` → 317 vs 277 (also 317 under 3.38.0). Enumerated red on clean main:

| red | where | class | ticket |
|---|---|---|---|
| Whole-tree machine-specific-path ratchet | `guard-runner-labels` → `gate` (transitive) | ENV/instrument: analyser version is part of the number and was never stamped | PMAT-1059 (G-10, #2999) |

Fleet pin: `machines/intel/forjar.yaml` (paiml/infra) pins `pmat` at 3.37.0 (`stack-tool-pmat`, lines 765–785, BSE-10b / PMAT-231).
Local `~/.local/pmat/3.37.0/bin/pmat` is installed; `bash -c '. scripts/pmat_bin.sh'` on `agent/G-10` resolves it.
PATH `pmat` on this box is 3.38.0 — one more reason the pin, never PATH, is the instrument.

## 2. Worktrees (`git rev-list --count origin/<branch>..HEAD` per worktree, after `git fetch`)

| worktree | branch | unpushed | note |
|---|---|---|---|
| G-10 | `agent/G-10` | **5** (no remote branch yet) | PR-A, pushed in PHASE 1 |
| C0-3, C0-4, I-24, I-25, I-26, R-0, R-0-amend, R-3, T-2, pr-triage, report-066, pp-066-b1/b2/plan/s0, pp-066-spec | `agent/*` | 0 | — |
| C0-7, G-4, G-6, SPEC-1.6 | `agent/*` | remote branch deleted | merged through batches #2987 / #3000 (`gh pr list --state all --head agent/pp-066-b1`) |

The main checkout and ~120 `.claude/worktrees/wf_*`, `agent-wt/*` worktrees belong to other sessions
(detached or on pre-PP-066 branches); out of this driver's scope and untouched.
`target-review/.rustc_info.json` shows as deleted in several worktrees: a tracked build artefact (not a PP-066 file).

## 3. Armed PRs (`gh pr list --author noahgift --state open --json number,mergeStateStatus,statusCheckRollup`)

| PR | row | mergeState | red legs | class |
|---|---|---|---|---|
| #3009 | I-26 | BLOCKED (auto) | guard-runner-labels → gate; pr-review-receipt; present | ratchet (PMAT-1059) · receipt/quorum advisory |
| #3008 | I-25 | BLOCKED (auto) | same | same |
| #3007 | C0-4 | BLOCKED (auto) | same | same |
| #3006 | I-24 | BLOCKED (auto) | guard-runner-labels → gate; present | ratchet |
| #3005 | T-2 | UNSTABLE (no auto) | present; pr-review-receipt (last ci.yml run cancelled `34014856834`) | advisory; needs a fresh run after main is green |
| #3004 | R-0a | BLOCKED (auto) | guard-runner-labels → gate; present | ratchet |
| #3003 | R-0 amend (DAG) | BLOCKED (auto) | guard-runner-labels → gate; pr-review-receipt; present | ratchet |
| #3001 | R-3 | UNSTABLE (no auto) | present; pr-review-receipt | advisory |
| #2998 | pr-triage ledger | draft | present; pr-review-receipt | DRIVER docs; lands after G-11 |

`present` (pr-review-quorum) and `pr-review-receipt` are not in `required_status_checks.contexts`
(`gh api repos/paiml/aprender/branches/main/protection` → `["ci / gate","workspace-test"]`, `strict: true`); they are advisory here and are
re-judged from the base after each merge.

Triage set (`docs/audits/pr-triage-0.66.yaml`, on `agent/pr-triage` #2998, 29 rows): MERGE-NOW #2793 #2802 #2807 #2848
(#2858 landed); MERGE-0.66 #2575 (R-2) (#2871 landed); HOLD #2659 (on #2985, landed → releasable) and #2720 (on D-3 → decided today);
CARRY-0.67 #2800 #2801 #2838; PRIOR-ART-CLOSE 12 rows all CLOSED (`gh pr view <n> --json state`); STOP:author #2794 OPEN.

## 4. DAG and receipts (`python3 -c` over `git show origin/main:docs/specifications/pp-066-dag.yaml`)

- rows 91 · status complete 5 (G-6 PMAT-980, G-4 PMAT-987, SPEC-1.6 PMAT-988, C0-5 PMAT-1054, C0-7 PMAT-1056) · open 86 · lane 0.66: 61, 0.67: 30.
- Receipts with `status: complete` on main: exactly those five (`impl-PMAT-{980,987,988,1054,1056}-receipt.md`). DAG ⇔ receipts consistent.
- `docs/audits/impl-estimates.jsonl`: 29 rows, keys `{repo,ticket,phase,mode,est,actual,basis}` — no `turns/tokens/wall_clock_s` yet; takt `basis=` waits for three merged rows of a class (PHASE 2).
- pmat#1200 (hollow `work cot derive`) is **OPEN** (`gh issue view 1200 -R paiml/paiml-mcp-agent-toolkit --json state`); C0-3 stays blocked on U-1 (PHASE 4).
- Expiries nearest (0.66, open): DEC-D-3, DEC-D-11, R-3 at 2026-09-12; twenty rows at 2026-09-19 (B-A1, C0-1, C0-3, C0-4, C0-6, DEC-D-9, G-2, G-5, G-7, I-1, I-24, I-25, I-26, P-0.3, R-0, R-4, T-0h, T-2). DEC-D-3/D-11/D-9 are discharged by today's comments; R-3's PR is #3001.

## 5. Phase sequence from here

1. PHASE 1 — push `agent/G-10`, three-lane review-only quorum on the diff, receipt (partial), PR-A with the mutation table, auto-merge; the only PR in the queue until it lands. Then G-10b/G-10c as ticketed.
2. PHASE 2 — G-11 write-set guard; rebase #3003–#3009 in expiry order; serial merges; takt after the third.
3. PHASE 3 — SPEC-1.6 executability fold (`release_criteria.sh`, `run_clean_room.sh`), G-4/G-6 residuals.
4. PHASE 4 — C0 (C0-1, C0-2, C0-4 armed as #3007; C0-3 behind U-1).
5. PHASE 5 — R-0a (#3004) → R-0b → R-2 → B-G1 → R-5 → R-6 → R-7 → R-8.
