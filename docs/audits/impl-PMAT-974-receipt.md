---
status: partial
partial_reason: "this PR is not yet merged on the required check; flip to complete with the DAG status write-back after merge"
ticket: PMAT-974
row: I-26
issue: 2889
epic: 2873
model: "orchestrator claude-fable-5-1; worker sonnet (paiml-impl-worker), resumed once at maxTurns=40 (finished on the resume); the mutation re-run, contract and receipt by the orchestrator"
tokens_used: "worker 103607 (first pass; resume not separately reported); orchestrator [U] (not instrumented)"
wall_clock_s: "[U] (not instrumented); dispatch ~07:15Z -> contract commit ~07:45Z on 2026-09-06 (about 1,800 s)"
---
# impl-PMAT-974 — I-26 · PP-29 scanner: a §12 expiry is the `Expires **date**` marker, never the first date; rows past expiry go RED (#2889; S0-2)

## Identity
ticket PMAT-974 · kind code · branch `agent/I-26` (worktree, claim held) · base `027ed889d` · `discover.json` at `$XDG_RUNTIME_DIR/paiml-implement/discover-I-26.json` (`gate_cmd_fallback=true`; no Rust in the diff) · quorum: review-only (`--quorum never`) · K̂ = 3 (`basis=first-run[U]`) · owner spec-owner.

## What lands
- `scripts/lib/spec_conformance.py` (the scanner `scripts/spec_conformance.sh` runs): a root cell's expiry is the `Expires YYYY-MM-DD` marker (`EXPIRES_MARKER`), never the first date in the cell; a non-discharged row whose derived expiry is before today is RED (the §4 andon; `SPEC_CONFORMANCE_TODAY=YYYY-MM-DD` pins the clock for the case table).
- `evidence/parity/derived_expiries.json` regenerated by the script's own `--write`: row 1 expires **2026-09-19** (was 2026-09-02, the witness date); rows 13, 15, 19 derive from row 1 and row 18 from row 15 — all 2026-09-19.
- `--selftest` rows `expiry_marker_wins` and `expired_row_is_red` (RED first at c37fe3b8a; 42 rows green after the fix).
- `contracts/pp-llama-001-spec-conformance-v1.yaml`: an invariant under `expiries_derive_from_the_dag` and `FALSIFY-PP-LLAMA-001-CONFORMANCE-011` (existing contract; no README count change).

## Verification (orchestrator, every command re-run at 69d6a5a62)
| check | result |
|---|---|
| `bash scripts/spec_conformance.sh --selftest` | rc 0 — `expiry_marker_wins r1 = Expires marker = 2026-09-19`, `expired_row_is_red D6` among 42 rows |
| `bash scripts/spec_conformance.sh` (HEAD scan, real clock) | rc 0 — nothing expired today (2026-09-06) once row 1 reads 2026-09-19 |
| `python3` over `evidence/parity/derived_expiries.json` | row 1 → 2026-09-19; 13/15/19 ← 1; 18 ← 15 |
| `bashrs lint scripts/spec_conformance.sh` | 0 errors |
| `pv validate` · `pv lint` on the contract · `check_contract_test_binding.sh` · `check_contract_enforcement.sh` · `check_no_claim_literals.sh` · `check_perf_claims_cite_receipts.sh` · `check_roadmap_diff_additive.sh` · `check_readme_claims.sh` | valid · PASS · rc 0 ×6 |

## Mutation (RED, then restored GREEN)
`EXPIRES_MARKER` replaced by a bare date regex (the first date wins again) → `expiry_marker_wins BROKE: expected 2026-09-19 got [2026-09-02]`. Restored → green.

## Dispatch ledger
| phase | mode | agent | turns | maxTurns hit | resumed | outcome |
|---|---|---|---|---|---|---|
| P_1 | subagent:sonnet | abfcf0e08d7381150 | 40 + resume | yes | once | both commits landed; receipt partial=false; every command re-run green by the orchestrator |
| P_2 | direct | — | — | — | — | mutation, contract, receipt |
Slots ≤ 1 live; denials 0.

## Jidoka
- The brief named `scripts/lib/spec_conformance_scan.py`; the scanner is `scripts/lib/spec_conformance.py` (the worker followed the hedge and said so).
- With the expiry parsed correctly nothing is expired today, but rows 1, 13, 15, 18 and 19 all expire **2026-09-19** — thirteen days out. The andon now fires the day after; C1's blockers inherit that date (recorded for the release cut, D-4 "master dates stand").

## Gaps
- Receipt for this PR: advisory, not produced (driver A1).

## Estimates
K̂ 3 (`basis=first-run[U]`); actual: worker 40 + resume, orchestrator ≈ 3 bash calls (`basis=this receipt`).

## Verdict
PENDING-MERGE (`status: partial`).
