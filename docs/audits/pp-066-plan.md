# PP-066 plan — first wave (P1 plans per row) and the operating rules

**Inputs:** `docs/specifications/PP-066-release-spec.md` v1.5 (§5/§6 cards), `docs/audits/pp-066-s0-ledger.md` (S0-1..S0-23), the 2026-09-05 triage (class parents #2876–#2880, rows G-5/G-6/G-7/I-24/I-25 proposed on #2873), and the C0 obligations (F-28, S0-23). **Data:** `docs/specifications/pp-066-dag.yaml` — 85 rows (48 in the 0.66 lane incl. the release-cut row, 7 decisions, 30 carried 0.67 rows), every blocker→blockee pair with ≥ 6 days slack, both host queues ordered by expiry, acyclic, every #2873 sub-issue attached to a row (checked by the generator; G-4 makes the check a CI gate). **Epic:** #2873; Track P rows carry `parent: 2870`.

## What S0 changed (applied in the DAG)

- S0-1 CONFIRMED → no I-00. Master rows 0a–0e, 3, 6, 7, 9, 10, 12, 14 are LANDED; the spec's first-wave items "master 0a/0d" are already discharged and are not carded. Row 1 (I-1) is the only open instrument-chain root; rows 15 and 18 derive from it (I-15 → 2026-09-26, I-18 → 2026-10-03).
- S0-3 FALSIFIED → D-6 = yes. B-A1 anchors on DEC-D-3, not hardware; B-W1's rasteriser falsifier is live (llvmpipe beside two RADV adapters).
- S0-12 FALSIFIED → no Metal dispatcher and no `metal` feature; B-M1..M4 start from the wgpu-Metal adapter (0.67 lane, unchanged).
- S0-19 FALSIFIED → PP-21 receipt keys are symmetric; R-5/R-6 use the minisign public-key scheme (the `.github/pr-review.pub` class); §8's refusal becomes "no third signing scheme" (SPEC-1.6).
- S0-14 CONFIRMED on intel and mini; S0-15 measured on the facade (+5.5 % wall, +7.2 % size, `basis=cold-build-under-CI-load`) → DEC-D-9's evidence exists; R-2 carries the cost line.
- S0-23: `strict=true` applied; CB-1701, CB-2100, CB-1658 and #2830 become C0-1..C0-4. **C0 is credited first; until it holds every other C-criterion is recorded `[U]`.**
- S0-2 expiry finding → I-26 (PP-29 scanner takes the `Expires **date**` marker; rows past expiry go RED).
- S0-18 → 500 `registry: true`; S0-11 → T-2 narrows to the wgpu pipeline (`finetune.rs:717`).
- The 14 spec defects → SPEC-1.6 (expiry 2026-09-12, lands with STEP 5's PR). Four expiries moved with it: P-0.6 09-19 → 09-26 (zero slack vs P-0.3), T-0 09-26 → 10-03 (zero slack vs T-1), P-1.1 10-10 → 10-03 (so P-1.2, its own row, keeps 10-10 with slack), master row 17 tightened 10-23 → 10-16 (blocker of S-2).
- Decisions are blockers in the DAG, not prose: DEC-D-9 → R-2, DEC-D-10 → R-5, DEC-D-2 → T-3, DEC-D-11 → every Track P row, DEC-D-3 → B-A1 (expiry anchored on the decision). A `TAG-0.66.0` row (the cut; 2026-10-30 = §2's 10-23 target plus the slack rule) is blocked by C0-1..C0-4 and every C-criterion producer, so C0 precedes the tag structurally.
- §6 rows are carded one per row (B-W1..B-W5, B-M1..B-M4, B-S1..B-S4, T67-1..T67-5) so every spec row resolves to one DAG id; #2841 (closed) rides B-M1. S0 premises a card named as blockers (S0-7, S0-14, S0-15) are discharged and recorded on the row as `[premises discharged: …]`, not as open blockers.
- `pmat comply check` has no `--rule obligation-dag` (pmat 3.37.0) → G-4's checker is in-repo; a pmat ticket is owed for the rule. Master Appendix E carries no `pmat work add` lines → Track I rows are carded here. `improve-provable-contracts.md` is not in the tree → Track P rows cite #2870.

## Decisions (defaults per spec §0; no override on #2873 at planning time)

D-1 split · D-2 not revoked (T-3 REPORTING) · **D-3 escalate** (DEC-D-3 row; B-W*/B-M*/B-S* stay 0.67) · D-4 master dates stand (the three moves above are spec-row dates, amended in SPEC-1.6) · D-5 → G-2 · **D-6 yes** · D-7 exit code from `error.rs` · **D-8 escalate** · D-9 yes-iff (DEC-D-9) · D-10 per target (DEC-D-10; R-5 runs S0-14 per target) · D-11 phase 0 + 1.1/1.2 (DEC-D-11).

## WIP caps and host queues (spec §2)

≤ 1 speed row in flight per host. **lambda window queue:** R-4 (09-19) → S-3 (09-26) → I-18 (10-03) → S-1 (10-16) → S-2 (10-23). **gx10 queue:** I-1 (09-19) → I-15 (09-26) → the T-0 window (T-1 09-26 bundled with T-0 10-03; spec §5 T-1 "queue slot 2 = T-0's window") → S-3g (10-10) → master 21 (0.67). intel carries no new lane before DEC-D-3 (P-0.6's lint step is CI, not a lane); mini is dogfood only. Merge queue: one workspace-test at a time; docs PRs batched.

## Operating rules for every row (the driver, restated once)

Ticket first (`gh issue` under #2873 or #2870, then `pmat work add`, output trimmed per G-6); feature branch in its own `apr-agent <row>` worktree; **RED test commit first** — the test named below is committed failing and is the contract's `falsification_tests[].test`; it is distinct from the **guard mutation**, which is applied to the *tree* after the guard is green, must turn the *guard* (not its selftest) RED, and is then reverted — both are shown in the PR body; the contract lands in the same PR (`kind: pattern`; no `registry: true`; no hand-typed `verification_summary`; the `test:` is an invocation); every `A_i` re-run by the orchestrator (a worker PASS is a claim); every guard's mutation shown RED then GREEN in the PR body; `K̂ [U]` on first runs, seeding `docs/audits/impl-estimates.jsonl`; quorum per the row's class (`--quorum never` + manual pr-review-quorum until the agy slash surface is verified); `ci / gate` → auto-merge → `pmat work complete` → `docs/audits/impl-<ticket>-receipt.md` with `partial=false` → `status: complete` in the DAG.

## First wave (by expiry; P1 per row)

### SPEC-1.6 (2026-09-12, docs)
RED: `bash tests/spec/pp066_v16_defects.sh` — a case table of the 14 ledger defects, each asserting the corrected text is present; RED at v1.5, GREEN at v1.6; the mutation is reverting one correction → its row RED. Phases: (1) apply the 14 ledger defects + S0 consequences + the three expiry moves to the spec, changelog row 1.6 [A: `bash scripts/check_no_claim_literals.sh && bash scripts/check_perf_claims_cite_receipts.sh` exit 0]; (2) render §5/§6 tables from the DAG once G-4's `render_dag.py` exists — until then the tables are hand-edited and G-4's `--check` is the drift gate that arms later [A: byte-diff of the rendered tables]. Mutation: none (docs); the claim guards are the gate. K̂ [U].

### G-6 (2026-09-12) — roadmap diff is additive
RED: `bash scripts/check_roadmap_diff_additive.sh --selftest` with the un-trimmed 2,531-line re-serialisation of `6d9ba274a` as the RED fixture and the trimmed 17-line hunk as GREEN. Phases: (1) the script [A: `bash scripts/check_roadmap_diff_additive.sh $(git merge-base origin/main HEAD) HEAD` exit 0 on a trimmed hunk, exit 1 on the fixture; `bash scripts/check_roadmap_diff_additive.sh --selftest` 2/2]; (2) wire beside `check_roadmap_completion_is_cited.sh` in `guard-runner-labels` [A: `bash scripts/check_guards_are_wired.sh`]; (3) contract `apr-roadmap-additive-diff-v1.yaml`. Mutation: commit an un-trimmed `pmat work add` output → RED. K̂ [U].

### R-3 (2026-09-12) — honest training banner
RED: `cargo test -p aprender-train --test banner_truth` — the banner is emitted iff a device-side backward kernel launched (today `cuda_trainer.rs:533` prints on a stream-bind *failure*, and the "cuBLAS backward" line is emitted by path, not by event). Phases: (1) executor counter `backward_kernel_launches` [A: the test compiles and is RED]; (2) banner gated on the counter; `--gpu-backend cuda` on a CPU-only build exits with the refusal code read from `error.rs` (D-7) [A: test GREEN on lambda and on a CPU-only build]; (3) contract `apr-train-banner-truth-v1.yaml` [A: `scripts/pv_bin.sh validate`]. Mutation: print unconditionally → RED on the CPU-only arm. Quorum 3-lane. K̂ [U].

### C0-1, C0-3, C0-4 (2026-09-19) — the required check
- **C0-1** RED: `pmat comply check | grep CB-1701` is ✗ at HEAD. Phases: (1) a `deny` job in `ci.yml` running `cargo deny check advisories bans licenses sources` (wasmtime 47 per #2806 if advisories need it) [A: job green at HEAD]; (2) the context added to `required_status_checks.contexts` (pre-authorised class: the S0-23 PATCH) [A: `gh api …/protection --jq .required_status_checks.contexts` lists it; CB-1701 ✓]; (3) contract `apr-required-checks-v1.yaml`. Mutation: remove the deny context from the live `required_status_checks.contexts` while the merge queue is empty, run `pmat comply check` → CB-1701 ✗, restore it, run again → ✓ (a live, reversible mutation; a dry run cannot turn this rule RED, so the before/after API reads are pasted in the PR body). K̂ [U].
- **C0-3** RED: `pmat comply check | grep CB-1658` (main checkout) is ✗. Phases: (1) author the ten `GH-663..672.cot.yaml` from their `cot-digest.json` [A: CB-1658 ✓]; (2) reconcile with PVI-3.1's `contracts/work → .pmat-work` direction in a note on #2870. Mutation: delete one → ✗. K̂ [U].
- **C0-4** RED: `bash scripts/perf_gate.sh --selftest` gains `arm_a_c1_only_not_pass` (RED at HEAD). Phases: (1) `arm_a_scaling` emits `REPORT ArmA scaling: c=1 only, no scaling measured` and the gate never prints `VERDICT PASS` from an arm that emitted nothing [A: selftest both polarities]; (2) contract invariant in `pp-llama-001-perf-gate-v1.yaml`; §6 case names for `spec_conformance.sh`. Mutation: restore the silent `continue` → RED. K̂ [U].

### I-24, I-25, I-26 (2026-09-19) — instrument refusals
Per the #2873 comments (I-24, I-25) and the S0-2 finding (I-26): RED tests named in the DAG rows; contracts `apr-parity-block-refusals-v1.yaml`, `apr-workload-corpus-binding-v1.yaml`, and an amended `pp-llama-001-spec-conformance-v1.yaml` invariant (marker wins; expired row RED). Mutations as in the rows. K̂ [U].

### G-4 (2026-09-19) — DAG invariants in CI
RED: `bash scripts/check_dag_invariants.sh --selftest` with fixtures (cycle, zero-slack pair, queue inversion, missing owner, two expiry forms). Phases: (1) `scripts/lib/dag_invariants.py` — acyclic; `expires(blocker) + 6d ≤ expires(blockee)`; per-host queue monotone in expiry; owner present; exactly one expiry form; rows past expiry listed [A: selftest 5/5 both polarities; `check_dag_invariants.sh docs/specifications/pp-066-dag.yaml --min-slack-days 6` exit 0]; (2) `scripts/render_dag.py --check` renders §5/§6 tables byte-identical to the spec (drift = RED; arms with SPEC-1.6) [A: exit 0]; (3) wired into `guard-runner-labels` beside `spec_conformance.sh` [A: `check_guards_are_wired.sh`]; (4) pmat ticket filed for the upstream rule, linked from the row. Mutations: W-H expiry = C1's → RED; swap two gx10 positions → RED; self-edge → RED. K̂ [U].

### G-5, G-7 (2026-09-19) — universe and CI-death classification
Per the #2873 comments: G-5 RED = `bash scripts/check_target_universe.sh` at HEAD lists the dark targets (#2482 #2674 #2784 #2751) and exits 1; G-7 RED = `bash scripts/cargo_classify.sh --self-test` wired to nothing. Phases as the comments state; contracts `apr-target-universe-v1.yaml`, `apr-ci-failure-classification-v1.yaml`. K̂ [U].

### T-2 (2026-09-19) — `--max-seq-len` honoured or refused
RED: `cargo test -p apr-cli --test finetune_seq_len_truth` over {256, 512, 1024, 2048} on the instruct **and** wgpu pipelines (S0-11: the instruct path honours since #2247; the wgpu pipeline clamps at `finetune.rs:717`). Phases: (1) thread `max_seq_len` into `WgpuInstructPipeline::new` or refuse with the code [A: 1024/2048 rows GREEN]; (2) print `effective max_seq_len=` on every path and write it to the receipt field T-0h defines [A: stdout grep]; (3) contract `apr-finetune-config-truth-v1.yaml`. Mutation: restore `512,` → RED. K̂ [U].

### P-0.3 (2026-09-19, #2870) — proof credit from evidence only
RED: `cargo test -p aprender-contracts --lib proof_credit` — a contract with hand-typed `l4_lean_proved: 7` and no resolving theorem scores 0 (today `lint/gates_extended.rs:401` and `explain_render.rs:133` trust the integer). Phases: (1) `proof_status` credits only a resolving `#[kani::proof]` (expect 47) / sorry-free theorem under `crates/aprender-contracts-staging/lean/` (expect 8); declared vs discharged columns [A: `$PV proof-status contracts/ --binding contracts/aprender/binding.yaml` prints discharged 8 / 47 beside declared 181 / 1,257]; (2) FALSIFY-PVI-003 fixture [A: the lib test GREEN]; (3) contract `pv-p-0-3-v1.yaml`. Mutation: re-trust the integer → RED. Registered prediction: Lean 181 → 8. K̂ [U].

### P-0.6 (2026-09-26, #2870) — `pv lint` as a CI step
RED: `bash scripts/check_pv_lint_step.sh --selftest` (a contract adding one warning → RED; baseline 1,082 shrink-only; `--min-score 0.48` — `basis=` the measured mean score 0.48 (Grade D) at `b1a6324b8` in #2870's ledger, a floor at today's value, not an aspiration; Gate 7 not skipped). Blocked by P-0.3 and DEC-D-11. Phases: (1) step in `guard-runner-labels` via `scripts/pv_bin.sh` [A: exits 0 at HEAD, prints `warnings=1082 score>=0.48`]; (2) baseline file classified in `check_baseline_ratchets.sh` [A: ratchet PASS]; (3) contract. Mutation: a hollow contract with a new warning → RED. K̂ [U].

### G-1 (2026-09-26) — crate-name guard
RED: `bash tests/guards/crate_names_cases.sh` (bad `[lib]` name → FAIL; commented name → PASS; absent `[lib]` → PASS; `[[bin]]` ignored; workspace shell skipped and counted; root D-5 row; facades workspace scanned; duplicate `[lib] name` reported; unregistered stray → FAIL). Phases: (1) universe = `cargo metadata` members (root 79 + facades 3) ∪ `find` strays (102 `[package]` manifests today) − `scripts/crate_names_nonmembers.txt` [A: prints `members=82 strays=<n> remaining=51 depkeys_remaining=<n> features_remaining=21`]; (2) three shrink-only baselines wired into `guard-runner-labels` [A: `check_guards_are_wired.sh`; `check_baseline_ratchets.sh` PASS]; (3) contract `apr-crate-names-v1.yaml`. Mutations (three, in the PR body): stray `crates/zz-probe` → RED; drop one allow-list line → RED; bad-name member → RED. K̂ [U].

### R-0 (2026-09-19) — backend registry (ticket #1 of the code rows)
**N-lane design quorum before P2** (≥ 3 lanes: systems, Apple/Metal-forward, evidence; the lanes verify §12's lessons against llama.cpp/Ollama/llamafile source). RED: `cargo test -p apr-cli --test registry_failure_catalogue` (FX-1..FX-14, each with a must-RED twin under `tests/fixtures/registry/defective/`) and `--test backend_refusal_case_table` (rows generated from {build features} × {host facts} × `BACKEND_VALUES`; verdict `refuse ⇔ request ∉ Ready`). Phases: (1) `BackendRegistry::discover()` in `crates/aprender-serve/src/gpu/backend.rs` over `crates/aprender-gpu/src/driver` (libcuda.so.1 dlopen — S0-20; cuBLAS as a capability) and compute's wgpu adapters (`backends/gpu/device/mod.rs` enumerate; software rasteriser → `Unavailable(SoftwareRasterizer)`) [A: `apr devices --json | jq -e '[.[]|select(.status=="ready")]|length>=1'` on all four hosts; `contracts/apr-devices-schema-v1.yaml` validates every output]; (2) resolution replaces `accel.rs:27-29`, `dispatch.rs:173`, `serve/mod.rs:656` — zero `cfg!` in apr-cli backend decisions [A: `git grep -c 'cfg!(any(feature = "cuda"' crates/apr-cli/src` prints 0; both case tables GREEN]; (3) printed block and effective-config agreement [A: `apr devices | grep -cE '^backend: (cpu|cuda|wgpu|metal|hip) ' ` prints 5 on every host; `curl -s localhost:$PORT/v1/effective-config | jq -er .backend` equals `apr devices --json | jq -r '.selected.name'`; on lambda/gx10 `apr devices --json | jq -r '.[]|select(.name=="cuda")|.device_name'` equals the name in `nvidia-smi -L`]; (4) contract `apr-backend-registry-v1.yaml` (invariants i–v). Mutations: resolution via `cfg!` → table RED; print from the request → C11 RED on a CPU host; restore `accel.rs:28` → `(build=wgpu, ask=cuda)` RED. K̂ [U] (first design row; estimate after the N-lane review).

### Quorum record
Round 2 (2026-09-05, revised brief): teamwork = do-not-implement-as-written (3 measured, 2 cited, 1 asserted — the round-1 folds confirmed resolved), Gemini 3.1 Pro = do-not-implement-as-written (8 measured, 1 cited, 1 asserted), GPT-OSS 120B = INDETERMINATE (vacuous PASS: 0 findings, summary describes another task), Claude Opus 4.6 and the replacement Gemini 3.8 Flash lane recorded in `pp-066-plan-quorum.md`. Folded after round 2: T-3's master-8 premise noted as LANDED; R-0 carries S0-21's stress-ng half; S-1/S-2/S-3 thresholds carry `basis=` (registered predictions, master §12 rows 19/20, report §10); `$PV` spelled as `. scripts/pv_bin.sh && "$PV"`; R-5's blockers match the spec (R-2 dropped); D-2doc (S0-9 README defect) added; TAG-0.66.0 blocked by every 0.66 code row; G-6 and R-0 acceptance commands made exact. Recorded, not applied (asserted): "C0 must block every producer" — the spec credits C0 first, it does not sequence work behind it.
Round 1 (2026-09-05): teamwork = do-not-implement-as-written, Gemini 3.1 Pro = do-not-implement-as-written, GPT-OSS 120B = INDETERMINATE (no structured output), Claude Opus 4.6 (via agy) = PASS. Every cited/measured finding was folded before round 2; asserted findings are recorded in `docs/audits/pp-066-plan-quorum.md`. **Data:** 85 rows after unbatching (48 in 0.66 incl. the release-cut row, 7 decisions, 30 in 0.67).

### Also in the first wave (P1 from the spec card verbatim)
B-A1 (09-19), T-0h (09-19), I-1 (09-19, gx10 q1), R-4 (09-19, lambda q1), DEC-D-3/DEC-D-11/DEC-D-9 (decision issues), G-8 (09-26, #2856), P-0.2/P-0.4/P-0.5 (09-26), R-2 (09-26, after R-0).

## Filing record (STEP 5)
Every DAG row is a GitHub issue (#2882–#2980, label `pp-066`; decisions also `decision`; 0.67 rows also `pp-067`) with a pmat ticket (PMAT-967..PMAT-1053, `gh_issue`/`pmat_id` written back into the DAG). GitHub caps a parent at 100 sub-issues and #2873 already carried the 31 triage sub-issues, so rows hang under eleven track parents (#2960–#2970: I, C0, G, R, S, T, B, D, DEC, REL, 0.67 lane) which hang under #2873; Track P rows hang under #2870 and PVI-1.3–4.3 under #2556. The roadmap diff is additive: 87 new entries, 0 existing lines changed (G-6's rule, applied by hand until G-6 lands).

## Verification of this plan
- `python3` over `pp-066-dag.yaml`: `slack violations: []`, `lambda ordered`, `gx10 ordered`, acyclic, every #2873 sub-issue cited (output pasted in the PR body).
- `bash scripts/check_no_claim_literals.sh && bash scripts/check_perf_claims_cite_receipts.sh` exit 0.
- STEP 4's teamwork pass and three quorum lanes (`docs/audits/pp-066-plan-quorum.md`) precede STEP 5.
