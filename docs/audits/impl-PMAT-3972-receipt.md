# PMAT-3972 receipt: ONT-4c5 capability cells (aprender #3972 + #4047)

## Identity
- ticket: PMAT-3972 (derived from GitHub #3972); kind: code; also closes the #4047 half
- spec row: ONT-4c5, paiml/infra `docs/specifications/paiml-ontology.md` v4.12, merged 948ae923 (infra#950). Row text diffed identical to the PMAT-921-v412 draft the plan was written against.
- branch: `feat/ont-4c5-capability-cells`, based on `6db770d2f` (#4046 head at start, per cop ruling); HEAD `77a1baeb6`
- discover.json sha256 prefix `522b9f358798394a`; repo_root `/home/noah/src/aprender-ont4c5` (a linked worktree; the shared checkout was left on its own branch)
- orchestrator model: opus-5-5 (model-gate: `class=opus decision=admit basis=file`)
- hooks: `core.hooksPath=/dev/null` in this repository, so `pmat hooks install --strict` cannot apply (it also fails in a linked worktree: `.git` is a file). The shared config was not changed, and every commit carries `Pmat-Ticket: PMAT-3972` by hand.

## What landed
- `ontology/capability_cells.rs`: D = required rungs × `receipts::expected_hosts` (ONT-4c1's function, factored out and called, not copied). Cells are measured only in receipts of the current release V*. A label beats the measurement. Duplicate V* rows combine NotRun > Fail > Pass (not the lattice meet). A non-numeral version and a duplicate required rung id are refused by name (exit 3). One `apply` serves both the corpus and the plant.
- `lint/capability_cells_gate.rs`: the plant goes through the ARMED shape; `wiring_holds` compares focus nodes at run time; PV-ONT-013 names each NotRun cell; `pc_shapes`.
- `lint/shapes_gate.rs`: `cells_corpus` / `cells_controls`; `ShapesOutcome::EmptyDomain` (exit 2 decline).
- `lint/mod.rs`: `ShapesControls {pc_extract, pc_shapes, capability_cells}` is boxed and flattened, so the JSON keys sit at the top level and the variant stays under clippy's large_enum_variant bound.
- `contracts/ont-capability-cells-v1.yaml`; `armed_shapes += capability-cells`; `contracts.nt` / `shapes.ttl` regenerated by `pv extract`.
- Fixtures `tests/fixtures/ont/capcells-{clean,defer,manual,noverdict,missing,badlabel,failadmit,emptydomain,badversion}` plus `capability-cells-plant.yaml` (byte-identical to the embedded plant, enforced by a test).
- CLI test target `ont4c5_capability_cells`: the row probe on this repository, the row's data mutations in symlinked scratch trees, and one exit path each. It is wired through `ci/explicit-test-commands.d/480-*.cmd`.
- `ont4b_shapes_gate` shapes_n ratchet 18 → 19, naming capability-cells.

## Plan and routing
| phase | mode | trigger | route |
|---|---|---|---|
| plan grill (3 rounds) | quorum:grillme via paiml-agy-delegate | Q2 | `route=agy-plan w=1.00 basis=absent` |
| P1 refactor, P2 cells, P3 gate, P4 contract, P5 tests | direct | - | self |
| P6 diff review | quorum (Claude fallback, see below) | Phase 4 | self-dispatched Sonnet lanes |

## Verification (claimed vs re-run: every result below is the orchestrator's own run)
| check | result |
|---|---|
| row probe on this tree (pinned pv via scripts/pv_bin.sh) | GREEN: verdict Pass, V* 0.69.1, D=16 ⊇ B(14), not_run [], pc_shapes fired |
| row mutations | disarm → exit 2; required:false → B ⊄ D; DEFER/MANUAL/NO-VERDICT → exit 1 naming the cell; deleted row → exit 1 naming the cell; a Fail cell is admitted |
| code mutants | combine→lattice meet: 1 test RED; V* filter removed: 3 RED; fold missing into Fail: 7 RED + pv declines on the real tree (`pc_shapes.capability-cells did not fire`) |
| `cargo test -p aprender-contracts --lib` | 1723 passed |
| `cargo test -p aprender-contracts-cli` (all targets) | all pass (ont4c5_capability_cells 11, ont4b_shapes_gate 12, ont4c1_model_receipts 11, …) |
| `cargo clippy -p aprender-contracts -p aprender-contracts-cli --lib --tests -- -D warnings` | clean |
| `pv lint contracts/` (all gates) | PASS; armed_shapes monotone 3 → 4 |
| `pv extract contracts/ --check` | exit 0 |
| `make gate` (gate_cmd) | exit 2: 122 checks, 6 failed. **All 6 also fail on the base `6db770d2f`** (each run on a detached worktree of the base): check_baseline_ratchets, check_complexity_ratchet (RED set identical to base), check_no_pipe_into_grep_q, check_no_silent_truncation, check_release_bump_pr_body, check_release_models_t1. This branch adds 0 failures. `make gate` stops at guard_tree under `set -e`, so its touched-crate test stage was covered by the two crate suites above. |

## Quorum
- Plan grill: agy, 3 gemini lanes (measured), 3 rounds. R1: 2 do-not-implement + 1 PASS. The hole was real: a cross-version witness let 0.68.2 green mask a 0.69.1 DEFER, so the design moved to V*. R2: 2 PASS + 1 do-not-implement (the plant bypassed the corpus loop), fixed by one `apply` and a run-time wiring check. Every lane exited 3 from sibling-worktree commits; verdicts were read, not relaunched. Artifacts: `$XDG_RUNTIME_DIR/paiml-implement/agy/PMAT-3972/45be20e8-…/ph1/{round-1/,lane-*.json,reduce.json}`.
- Diff review: agy 429 on every family (ph6: 3 lanes BLIND, NO-VERDICT). Per the operator's standing fallback rule, **3 Claude Code lanes, sonnet-5, `degraded: same-family`**, author opus-5-5 (no lane on the author's id). Round 1: A FAIL, B PASS, C do-not-implement. Findings fixed: the test target was unwired, and the plan note falsely claimed CI ran it; exit 3 had no CLI assertion; top-level JSON was not asserted in lib; duplicate rung ids merged cells; wiring was compared by id rather than by node. Round 2: **A PASS, B PASS (R1), C PASS**. The full verdicts are in `docs/audits/quorum-PMAT-3972.json`.

## Findings filed or surfaced
- #4087, #4088, #4089 (from the #4035 measurement, same session).
- `guard_tree.sh` vacuous PASS: two concurrent `make gate` runs on lambda made its mktemp dir vanish, and it printed `0 checks, 0 failed` and exited 0. Surfaced to the cop and not filed yet.

## Estimates
- K̂ 64 (basis impl-estimates.jsonl:L49-L52, 3 rows). Actual: **126 turns**, measured as distinct main-thread assistant message ids in session 45be20e8 from 2026-09-23T18:17:18Z (ticket creation) to this receipt. That includes three grill rounds, two review rounds and the 0.69.1 pause. Overrun ×2.0.
- Gap: rung-level receipts only. The v2 per-verb / per-thinking `cells[]` axis (#3712) is out of this row's scope.

## Verdict
DONE to the quorum receipt. Not armed (batching is the default; the cop folds). Rebase onto main is owed once #4046 lands.
