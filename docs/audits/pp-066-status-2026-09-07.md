# PP-066 status — 2026-09-07 (instance A, session end ~08:00Z)

Standard: docs/specifications/PP-066-release-spec.md v2.0 + docs/specifications/pp-066-dag.yaml. State is read from
receipts and PRs, never remembered; this file is the human-readable snapshot the session left.

## Main
GREEN at e4b423790 (#3030: twelve legacy nested roadmap records dropped; pin-independent duplicate-id guard). Both
reds of 2026-09-06 traced to the fleet's pmat move 3.37.0 → 3.39.0 (#3028, infra#468): the upstream `roadmap-valid`
step and G-10a's pinned analyser (remedy: versioned root `~/.local/pmat/3.37.0` on the runner host).

## Rows (18-row scope)
| row | state | PR | note |
|---|---|---|---|
| G-10a | merged | #3011 | |
| G-11a | merged | #3020 | |
| G-10b | armed | #3021 | baseline re-stamped on the merged tree (main gained analyser mentions) |
| G-11b, G-10c | branches on old main | — | instance C's rows; C stopped; re-cut owed |
| SPEC-2.0 | armed | #3025 | + `release_criteria.sh --self-test` wired (the wiring guard refused it unwired) |
| C0-2 / C0-4 / R-3 | open, auto-merge armed by C | #3031 / #3007 / #3001 | BEHIND; C0-2 receipt partial (CB-2100 reachability finding filed); A reconciles after the queue drains |
| L0-1a | GREEN leg pending | #3026 | rounds 1 and 2 RED captured (34049865821, 34052968770); effective-config key set now carries `parity` |
| **L0-1b** | steps 0–2 done, receipt partial | #3032 | per-op instrument names L26; the CPU Q8_K reference was the wrong side; crushed-block f32 fallback: 1.5B 0.9508 → 0.999761; gx10 twin and the `selected:` line (R-0b) owed |
| R-0a | armed by B | #3004 | receipt deliberately partial (G-11 derives DAG status); B stopped; update-branch done |
| R-0b | P0/P1 done | agent/R-0b | claimed by A; P2 after #3004 |
| R-2, R-5, R-6, R-7, R-8, release-cut | not started | — | R-6/R-7/R-8 were B/C rows |

## C0
Not credited: CB-2100 (nine gate steps must be reachable from a required context) — #3031 pins sovereign-ci by sha and
files the reachability finding; the vendoring is open.

## Blockers
- The CI fleet is shared across paiml repos (9 of 17 runners on paiml-mcp-agent-toolkit / interactive.paiml.com /
  rust-mcp-sdk at 07:35Z); aprender runs queue 40–60 min before starting. Merge queue depth is kept ≤ 2.
- Instances B and C stopped (K=150); their PRs are in flight with auto-merge; A reconciles.

## Kaizen (this session)
See docs/audits/driver-kaizen.md — three-family quorum mechanics; measure a criterion's distribution before designing a
fallback; a PATH tool is not a pin; `pkill -f` matches the tool's own shell; `git checkout -- f` restores the index.
